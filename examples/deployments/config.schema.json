{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "AauthResourceMetadataConfig": {
      "additionalProperties": true,
      "description": "AAuth resource metadata (`/.well-known/aauth-resource.json`, draft-hardt-oauth-aauth-protocol \"Resource Metadata\"). Field names follow the spec so an operator can paste a spec-shaped document; unrecognized spec fields ride through `extra` verbatim.",
      "properties": {
        "accept_signature_algs": {
          "default": [
            "Ed25519",
            "ES256"
          ],
          "description": "Exactly the fully-specified JWS algorithms the verifier accepts. Defaults to what `dev.mcpg.identity.aauth` implements.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "access_mode": {
          "default": "agent-token",
          "description": "The credential flow agents should expect. Defaults to `agent-token` (identity-based access — what `dev.mcpg.identity.aauth` verifies); set `person-token` when the plugin's person-token mode is the primary credential.",
          "type": "string"
        },
        "additional_signature_components": {
          "description": "Covered components agents MUST include beyond the profile's base set. Mirror the identity plugin's `additional_covered_components`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "insecure_dev_mode": {
          "description": "Development escape hatch for the revocation endpoint's outbound person-server key fetch: admit `http://` and private addresses. NEVER set in production.",
          "type": "boolean"
        },
        "issuer": {
          "description": "The gateway's AAuth server identifier — HTTPS scheme+host only, no port/path/trailing slash, lowercase (e.g. `https://gw.example`). MUST match the origin the document is fetched from.",
          "type": "string"
        },
        "resource_token_ttl_secs": {
          "default": 300,
          "description": "Resource-token lifetime, seconds (protocol ceiling 300).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scope_descriptions": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Scope values this resource grants, each with a Markdown description the person server shows at consent. A resource token may only request scopes declared here (plus the standard OpenID identity scopes); tool `required_scopes` should name values from this map.",
          "type": "object"
        },
        "signature_window": {
          "description": "Signature validity window advertised to agents, seconds. Mirror the identity plugin's `signature_window_secs` when overriding the 60 s default.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "signing_key": {
          "anyOf": [
            {
              "$ref": "#/definitions/AauthSigningKeyConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "The Ed25519 key that signs resource tokens (`aa-resource+jwt`); its public half is published at `jwks_uri`. Required for `access_mode: auth-token`, unused otherwise."
        }
      },
      "required": [
        "issuer"
      ],
      "type": "object"
    },
    "AauthSigningKeyConfig": {
      "additionalProperties": false,
      "description": "Where the AAuth resource signing key comes from — exactly one source.",
      "properties": {
        "ephemeral": {
          "description": "Generate a fresh key at every start. Development only: tokens minted before a restart cannot be verified after it.",
          "type": "boolean"
        },
        "seed": {
          "description": "The seed inline as unpadded base64url (typically `${env.X}`).",
          "type": [
            "string",
            "null"
          ]
        },
        "seed_file": {
          "description": "File holding the 32-byte Ed25519 seed — raw bytes or base64url text.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AccessConfig": {
      "additionalProperties": false,
      "properties": {
        "authorization_server": {
          "anyOf": [
            {
              "$ref": "#/definitions/AuthorizationServerConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Embedded Enterprise-Managed Authorization server (MCP `io.modelcontextprotocol/enterprise-managed-authorization`). When set, the gateway acts as the OAuth Resource Authorization Server for ID-JAG grants: it serves RFC 8414 metadata at `GET /.well-known/oauth-authorization-server` advertising the `urn:ietf:params:oauth:grant-profile:id-jag` grant profile, and redeems Identity Assertion JWT Authorization Grants issued by the configured trusted enterprise IdPs at `POST /oauth/token` (`urn:ietf:params:oauth:grant-type:jwt-bearer`), minting audience-restricted access tokens the gateway itself accepts. Only this grant is supported — there is no authorization endpoint, no refresh tokens, and no dynamic client registration."
        },
        "jwks": {
          "anyOf": [
            {
              "$ref": "#/definitions/JwksConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "oidc_oauth": {
          "anyOf": [
            {
              "$ref": "#/definitions/OidcOAuthConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "resource_metadata": {
          "anyOf": [
            {
              "$ref": "#/definitions/OAuthResourceMetadataConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "OAuth 2.1 Protected Resource Metadata (RFC 9728). When set, enables `GET /.well-known/oauth-protected-resource`. If omitted but oidc_oauth providers are configured, metadata is auto-derived."
        }
      },
      "type": "object"
    },
    "AdminAuthConfig": {
      "oneOf": [
        {
          "properties": {
            "bearer_token_env": {
              "type": "string"
            },
            "type": {
              "enum": [
                "static_bearer"
              ],
              "type": "string"
            }
          },
          "required": [
            "bearer_token_env",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Security: trusted-header mode requires a value match via `trusted_value_env`. Header-presence-only is insecure and generates warnings on every request.",
          "properties": {
            "header_name": {
              "type": "string"
            },
            "trusted_value_env": {
              "default": null,
              "description": "Env var whose value must match the header's value. Comparison is constant-time.",
              "type": [
                "string",
                "null"
              ]
            },
            "type": {
              "enum": [
                "trusted_header"
              ],
              "type": "string"
            }
          },
          "required": [
            "header_name",
            "type"
          ],
          "type": "object"
        },
        {
          "properties": {
            "type": {
              "enum": [
                "disabled"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "AdminConfig": {
      "additionalProperties": false,
      "properties": {
        "auth": {
          "allOf": [
            {
              "$ref": "#/definitions/AdminAuthConfig"
            }
          ],
          "default": {
            "type": "disabled"
          }
        },
        "base_path": {
          "default": "/admin/v1",
          "type": "string"
        },
        "bind_address": {
          "default": "127.0.0.1:9090",
          "type": "string"
        },
        "disclosure": {
          "allOf": [
            {
              "$ref": "#/definitions/DisclosureLevel"
            }
          ],
          "default": "summary"
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "Align": {
      "description": "Horizontal cell alignment.",
      "enum": [
        "start",
        "center",
        "end"
      ],
      "type": "string"
    },
    "AppColumn": {
      "additionalProperties": false,
      "description": "A table column bound to a JSON-path over each row.",
      "properties": {
        "align": {
          "anyOf": [
            {
              "$ref": "#/definitions/Align"
            },
            {
              "type": "null"
            }
          ]
        },
        "field": {
          "description": "JSON-path into the row object.",
          "type": "string"
        },
        "format": {
          "allOf": [
            {
              "$ref": "#/definitions/ColumnFormat"
            }
          ],
          "default": "text"
        },
        "header": {
          "type": [
            "string",
            "null"
          ]
        },
        "visible_if": {
          "description": "Client-evaluated visibility expression over the row.",
          "type": [
            "string",
            "null"
          ]
        },
        "width": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "field"
      ],
      "type": "object"
    },
    "AppCspDecl": {
      "additionalProperties": false,
      "description": "Per-app author CSP declaration. Each axis is still intersected by the egress `csp_policy`; declaring an axis can only narrow, never widen.",
      "properties": {
        "connect_domains": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "frame_domains": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "redirect_domains": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource_domains": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "AppDensity": {
      "description": "Layout density.",
      "enum": [
        "comfortable",
        "compact"
      ],
      "type": "string"
    },
    "AppField": {
      "additionalProperties": false,
      "description": "A detail/form field bound to a JSON-path.",
      "properties": {
        "field": {
          "type": "string"
        },
        "format": {
          "anyOf": [
            {
              "$ref": "#/definitions/ColumnFormat"
            },
            {
              "type": "null"
            }
          ]
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "field"
      ],
      "type": "object"
    },
    "AppProvidedTool": {
      "additionalProperties": false,
      "description": "A read-only App-Provided Tool: surfaces client state to the agent.",
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Advertised name; the host sees it auto-prefixed `app.<id>.<name>`.",
          "type": "string"
        },
        "source": {
          "allOf": [
            {
              "$ref": "#/definitions/AppToolSource"
            }
          ],
          "description": "Which whitelisted client reader backs this tool."
        }
      },
      "required": [
        "name",
        "source"
      ],
      "type": "object"
    },
    "AppRowAction": {
      "additionalProperties": false,
      "description": "A per-row action: a `tools/call` with arguments mapped from the row.",
      "properties": {
        "arg_map": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "argName → JSON-path over the row.",
          "type": "object"
        },
        "confirm": {
          "description": "Optional confirmation prompt before firing.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "tool": {
          "description": "The tool this action invokes (re-enters the full pipeline).",
          "type": "string"
        }
      },
      "required": [
        "id",
        "label",
        "tool"
      ],
      "type": "object"
    },
    "AppTheme": {
      "additionalProperties": false,
      "description": "Accent + density theming hints for the shell.",
      "properties": {
        "accent": {
          "type": [
            "string",
            "null"
          ]
        },
        "density": {
          "anyOf": [
            {
              "$ref": "#/definitions/AppDensity"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "AppToolSource": {
      "description": "The whitelisted read-only client state sources for App-Provided Tools.",
      "enum": [
        "selection",
        "visible_rows",
        "form_draft",
        "map_viewport"
      ],
      "type": "string"
    },
    "ApprovalsConfig": {
      "additionalProperties": false,
      "description": "Tool-gate human approval configuration.",
      "properties": {
        "callback_base_url": {
          "default": null,
          "description": "Public base url the gateway hands to notifiers as the callback URL prefix (e.g. `\"https://gw.example.com\"`). The runtime appends `/webhooks/approvals/<id>?expires=...&sig=...`.",
          "type": [
            "string",
            "null"
          ]
        },
        "callback_grace_ms": {
          "default": 60000,
          "description": "Seconds beyond `deadline_at` during which late callbacks still authenticate. Defence-in-depth — the registry's own deadline timer already rejects late resolutions; this just keeps the URL valid for short retries. Default 60s.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "signing_key_env": {
          "default": null,
          "description": "Env var name from which the gateway reads the HMAC signing key for approval callback URLs. The key MUST be at least 32 bytes (256 bits) — the gateway hard-fails boot if shorter. When unset, the gateway falls back to a random per-process key (callbacks won't survive a restart).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AppsConfig": {
      "additionalProperties": false,
      "description": "`apps:` config — SEP-1865 MCP Apps support.\n\nMCP Apps lets a server attach an interactive HTML UI to a tool. The `ui/*` postMessage protocol runs host↔iframe and never reaches the gateway; MCPG's role is passthrough of `_meta.ui`, capability advertisement (both downstream to clients and upstream to federated servers), federation `resourceUri` rewriting, and a tighten-only CSP/permission policy layer.\n\nOff by default. When `enabled: false`, MCPG omits the `io.modelcontextprotocol/ui` extension from its capability advertisement and applies no policy — but `_meta.ui` still round-trips on tool/resource descriptors (passthrough is wire-shape, not capability-gated, so a client with a cached template keeps working). When `enabled: true`, advertisement lights up and the `csp_policy` / `allowed_permissions` / `allowed_domains` clamps below are enforced on egress.",
      "properties": {
        "allowed_domains": {
          "description": "If set, a `_meta.ui.domain` outside this list is dropped (the host falls back to its default sandbox origin); in `strict` mode the whole response is rejected instead. `None` ⇒ any domain allowed.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "allowed_permissions": {
          "default": [
            "camera",
            "microphone",
            "geolocation",
            "clipboard_write"
          ],
          "description": "iframe permissions MCPG will let through; any `_meta.ui.permissions` key outside this list is stripped on egress. Default: all four standard permissions.",
          "items": {
            "$ref": "#/definitions/AppsPermission"
          },
          "type": "array"
        },
        "csp_policy": {
          "allOf": [
            {
              "$ref": "#/definitions/AppsCspPolicy"
            }
          ],
          "default": {
            "base_uri_domains": [
              "self"
            ],
            "connect_domains": [
              "*"
            ],
            "frame_domains": [
              "self"
            ],
            "redirect_domains": [
              "*"
            ],
            "resource_domains": [
              "*"
            ]
          },
          "description": "CSP upper bound. Each axis is **intersected** (never unioned) with the upstream's declared `_meta.ui.csp`. `[\"*\"]` on an axis imposes no bound on that axis (upstream passes through). An *omitted* axis on the upstream is left omitted (the host applies its restrictive default — `frame-src 'none'` / `base-uri 'self'`); policy never materializes an absent axis."
        },
        "enabled": {
          "default": false,
          "description": "Master switch for DOWNSTREAM advertisement + egress policy. Default `false` (opt-in).",
          "type": "boolean"
        },
        "federate_upstream": {
          "description": "Advertise the Apps capability on MCPG's OUTGOING (client→upstream) `initialize` so federated servers emit their UI-enabled tools. A spec-compliant upstream checks the client's `io.modelcontextprotocol/ui` capability before registering UI tools — omit this and such an upstream withholds every UI tool. `None` ⇒ inherit `enabled`. Set `true` explicitly to pull UI tools from upstreams while still withholding the capability from your own clients.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "registry": {
          "description": "Gateway-authored templated apps. Each entry mints a `ui://mcpg/<id>` resource whose behavior is driven by this config; the gateway ships the HTML, the operator supplies only the binding. Empty ⇒ no authored apps (pure proxy posture). Non-empty requires `enabled: true`.",
          "items": {
            "$ref": "#/definitions/GatewayAppConfig"
          },
          "type": "array"
        },
        "strict": {
          "default": false,
          "description": "Reject (vs sanitize) an upstream response whose `_meta.ui` escaped the policy below — a domain/permission/CSP entry outside the operator allow-list. Default `false` (permissive: narrow + log, never reject).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "AppsCspPolicy": {
      "additionalProperties": false,
      "description": "The CSP-axis allow-lists. Defaults mirror a sensible middlebox posture: no bound on what the app may fetch/connect/redirect to (`[\"*\"]`), but frame embedding and `<base>` pinned to `self`.",
      "properties": {
        "base_uri_domains": {
          "default": [
            "self"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "connect_domains": {
          "default": [
            "*"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "frame_domains": {
          "default": [
            "self"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "redirect_domains": {
          "default": [
            "*"
          ],
          "description": "Allow-list for `openExternal` redirect targets. Clamps only the OpenAI `openai/widgetCSP.redirect_domains` alias (there is no `_meta.ui.csp` axis for it). Default `[\"*\"]` ⇒ no bound.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource_domains": {
          "default": [
            "*"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "AppsPermission": {
      "description": "A standard iframe permission. Serialized snake_case in config; maps to the camelCase `_meta.ui.permissions` key on the wire.",
      "enum": [
        "camera",
        "microphone",
        "geolocation",
        "clipboard_write"
      ],
      "type": "string"
    },
    "AuditConfig": {
      "additionalProperties": false,
      "description": "Top-level `audit:` block. A top-level peer (rather than nested under `observability:`), with an inner schema aligned to the OTel signal-triad sinks-list pattern (`logs` / `metrics` / `traces`). Spec §9.12 defines the semantics; the fields here are the Rust projection.\n\nAudit sinks fan out via `sinks: [{kind, config, level?}]`. The built-in `dev.mcpg.builtin.audit.local-file` activates only when its plugin id appears in `sinks[].kind` (there is no `disable_builtins` toggle — just omit the sink to disable it).",
      "properties": {
        "emit_tool_call_allowed": {
          "default": true,
          "description": "Emit `mcpg.tool.call.allowed` after every successful pre-dispatch tool_gate chain. Default `true` for the compliance posture most operators want — every tool call on record. High-volume / low-compliance deploys can set `false`; deny + challenge paths still emit regardless.",
          "type": "boolean"
        },
        "emit_tool_call_completed": {
          "default": true,
          "description": "Emit `mcpg.tool.call.completed` after every successful post-dispatch tool_gate chain. Default `true`. Records `execution_duration_ms` for auditors flagging long-running calls.",
          "type": "boolean"
        },
        "enabled": {
          "default": true,
          "description": "Master toggle for the audit channel. When `false`, no audit sinks are registered (built-in or plugin), no audit events are emitted, and `required` is ignored. Default `true`. Set `false` only for dev/test runs where compliance is out of scope.\n\nAudit is intentionally orthogonal to `observability.enabled` — operators occasionally disable observability for short-lived debugging runs, but compliance audit MUST stay on for production traffic.",
          "type": "boolean"
        },
        "on_failure": {
          "allOf": [
            {
              "$ref": "#/definitions/AuditOnFailure"
            }
          ],
          "default": "fail_closed",
          "description": "Policy for per-event emit failures. Today the fan-out is always best-effort at the registry level (failures are metricsed but don't block the request). This field is captured + validated so the operator's intent is durable; the runtime behavior hookup lands in a future improvement (the emit site needs to translate this into \"return error from the tool-gate chain\" on `fail_closed`)."
        },
        "required": {
          "default": true,
          "description": "When `true` (default), the gateway REFUSES TO START unless at least one audit sink is serving traffic after plugin registration completes. Ignored when `enabled: false`. Operators explicitly set `false` only for dev / CI runs where compliance is not in scope.",
          "type": "boolean"
        },
        "sinks": {
          "default": [
            {
              "config": {},
              "kind": "dev.mcpg.builtin.audit.local-file"
            }
          ],
          "description": "Audit-sink fan-out. Each entry's `kind:` is a plugin id resolved against the registered audit sinks at boot. The built-in `dev.mcpg.builtin.audit.local-file` is the canonical default — listed in [`AuditConfig::default`].",
          "items": {
            "$ref": "#/definitions/SinkConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "AuditOnFailure": {
      "description": "Operator policy when an audit-sink `emit` fails.",
      "oneOf": [
        {
          "description": "(default) On emit failure, refuse to serve the triggering request. Compliance-safe: no action happens without a durable audit trail. Subtle: this can wedge the gateway if every registered sink is broken — operators should always configure at least one sink whose availability they actually monitor.",
          "enum": [
            "fail_closed"
          ],
          "type": "string"
        },
        {
          "description": "On emit failure, log + continue. The triggering request completes even if the audit event does not persist. Dev / CI use only — a compliance auditor will not accept this as SOC2-clean.",
          "enum": [
            "fail_open"
          ],
          "type": "string"
        }
      ]
    },
    "AuthConfig": {
      "additionalProperties": false,
      "description": "How MCPG authenticates to the upstream.",
      "properties": {
        "credential": {
          "description": "Credential-issuer reference for `oauth_client_credentials`: a standard `cred://<plugin_id>/<target>` URI, e.g. `cred://dev.mcpg.credential.oauth-client-credentials/notion`. The referenced issuer plugin mints + refreshes the upstream bearer; no client secret lives in the federation config.",
          "type": [
            "string",
            "null"
          ]
        },
        "credential_config": {
          "description": "Per-issuance config object forwarded verbatim to the credential issuer on the OAuth modes (a template issuer's `audience` / `resource` / `redeem_token_url` overrides). Registry OAuth discovery populates this on synthesized federations; hand-written federations may set it to steer a template provider without a per-target issuer entry."
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/AuthMode"
            }
          ],
          "default": "none"
        },
        "token": {
          "description": "Static bearer token for `service_token` (supports `${env.X}`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AuthMode": {
      "description": "Identity-propagation mode: what the gateway presents to the upstream.",
      "oneOf": [
        {
          "description": "No auth sent.",
          "enum": [
            "none"
          ],
          "type": "string"
        },
        {
          "description": "Static bearer token.",
          "enum": [
            "service_token"
          ],
          "type": "string"
        },
        {
          "description": "Forward the inbound `Authorization` header verbatim.",
          "enum": [
            "pass_through"
          ],
          "type": "string"
        },
        {
          "description": "Machine-identity token via an OAuth provider.",
          "enum": [
            "oauth_client_credentials"
          ],
          "type": "string"
        },
        {
          "description": "Per-caller token-exchange (RFC 8693).",
          "enum": [
            "oauth_impersonation"
          ],
          "type": "string"
        }
      ]
    },
    "AuthorizationServerClientConfig": {
      "additionalProperties": false,
      "description": "One OAuth client registered with the embedded authorization server.",
      "properties": {
        "client_id": {
          "description": "The client identifier the enterprise IdP binds into ID-JAGs (`client_id` claim). For MCP clients identifying via a Client ID Metadata Document, this is the document URL.",
          "type": "string"
        },
        "client_secret": {
          "default": null,
          "description": "Client secret for `client_secret_basic` / `client_secret_post` authentication. Supply via `${env.X}`. Omit to register a public client (`token_endpoint_auth_method: none`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "client_id"
      ],
      "type": "object"
    },
    "AuthorizationServerConfig": {
      "additionalProperties": false,
      "description": "Embedded EMA authorization server (`governance.access.authorization_server`).",
      "properties": {
        "access_token_ttl_secs": {
          "default": 3600,
          "description": "Lifetime of minted access tokens, in seconds.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "allowed_scopes": {
          "default": null,
          "description": "When set, the scopes granted on minted tokens are the intersection of the ID-JAG's `scope` claim with this list (the resource server may narrow, never widen, IdP-granted scopes). When omitted, IdP-granted scopes pass through unchanged.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "clients": {
          "default": [],
          "description": "OAuth clients allowed to redeem ID-JAGs at the token endpoint. Clients with a `client_secret` authenticate via `client_secret_basic` or `client_secret_post`; clients without one are public (`none`) — register an MCP client's Client ID Metadata Document URL as its `client_id` for that case. The ID-JAG's `client_id` claim must match the presenting client either way.",
          "items": {
            "$ref": "#/definitions/AuthorizationServerClientConfig"
          },
          "type": "array"
        },
        "clock_skew_secs": {
          "default": 60,
          "description": "Clock-skew leeway applied to ID-JAG `exp`/`iat`/`nbf` validation, in seconds.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "enforce_single_use": {
          "default": true,
          "description": "Enforce single-use ID-JAG redemption per instance: a `jti` seen once is refused until the assertion expires. Defense-in-depth on top of the assertion's short lifetime.",
          "type": "boolean"
        },
        "issuer": {
          "description": "This authorization server's issuer identifier (RFC 8414). MUST be the canonical external `http(s)` origin the gateway is reached at — enterprise IdPs bind ID-JAGs to it as the `aud` claim, compared exactly. Also the `iss` of every access token this server mints.",
          "type": "string"
        },
        "resource": {
          "default": null,
          "description": "Resource identifier minted access tokens are audience-restricted to (RFC 8707). Defaults to `governance.access.resource_metadata.resource` when that block is configured, else to `issuer`. An ID-JAG carrying a `resource` claim must match this value or redemption fails with `invalid_target`.",
          "type": [
            "string",
            "null"
          ]
        },
        "signing_secret": {
          "description": "HS256 signing secret for minted access tokens (≥ 32 bytes). Supply via `${env.X}`. Every gateway instance in a cluster must share this value so any instance can verify tokens minted by any other.",
          "type": "string"
        },
        "trusted_idps": {
          "default": [],
          "description": "Enterprise IdPs trusted to issue ID-JAGs. An assertion whose `iss` is not listed here is refused (`invalid_grant`).",
          "items": {
            "$ref": "#/definitions/TrustedIdpConfig"
          },
          "type": "array"
        }
      },
      "required": [
        "issuer",
        "signing_secret"
      ],
      "type": "object"
    },
    "BackendAnnotationsConfig": {
      "description": "Tool annotation hints configurable per binding. Maps to MCP `ToolAnnotations`.",
      "properties": {
        "destructive": {
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "idempotent": {
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "open_world": {
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        },
        "read_only": {
          "default": null,
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "BackendConfig": {
      "properties": {
        "annotations": {
          "anyOf": [
            {
              "$ref": "#/definitions/BackendAnnotationsConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "backend": {
          "allOf": [
            {
              "$ref": "#/definitions/BackendImpl"
            }
          ],
          "description": "Implementation backend — discriminated by `kind:`. The backend is an explicit nested object (`backend: { kind: http, url: ... }`) rather than fields hoisted onto the binding itself."
        },
        "cache": {
          "anyOf": [
            {
              "$ref": "#/definitions/KindRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-binding LLM response-cache override. Resolves via `resolve_kind(SlotClass::Cache, ...)` at boot:\n\n- `kind: in-process` (or alias `memory`) — fresh [`mcpg_backend_llm_shared::LruResponseCache`] sized by `config.max_bytes` (defaults to the gateway-wide `storage.response_cache` byte cap when omitted). Each binding that declares an in-process override gets its OWN cache instance — bindings don't share entries across overrides. - `kind: disabled` — explicit opt-out. `cache_get` / `cache_put` become no-ops for this binding regardless of the gateway-wide default. - Full plugin id / short alias — refused at boot today; the `ResponseCache` trait lives in `mcpg-backend-llm-shared`, not in `mcpg-plugin-protocol`, so plugins can't impl it yet. A future `ResponseCachePlugin` trait would make this arm live. - `kind: cluster` — refused; cluster coordinators don't advertise the response-cache role.\n\n`None` (default) inherits the gateway-wide `storage.response_cache:` (which itself can be `kind: disabled`)."
        },
        "content_storage": {
          "description": "Content-store provider this binding routes through. Must match one of the `storage.providers: [{id, ...}]` entries declared at the top level. When unset, the binding falls back to the provider id named in `content_storage.default` (or the conventional `default` id when neither is set).",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": "string"
        },
        "descriptor_meta": {
          "default": null
        },
        "governance": {
          "allOf": [
            {
              "$ref": "#/definitions/BackendGovernanceConfig"
            }
          ],
          "default": {
            "allow_if": null,
            "minimum_trust": "header_asserted"
          }
        },
        "icons": {
          "default": null,
          "description": "MCP 2025-11-25 descriptor extensions — icons and free-form `_meta`. Populated directly on the tool/prompt/resource/template descriptor that this binding produces.",
          "items": {
            "$ref": "#/definitions/BackendIconConfig"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "input_schema": {
          "default": null
        },
        "mcp_app_url": {
          "default": null,
          "description": "MCP App URL — a link to a rich UI for this resource. Populated on `_meta.mcpAppUrl` in resources/list descriptors and resources/read results. Supports CEL interpolation for dynamic segments (e.g., `https://app.example.com/docs/${arguments.id}`). Only meaningful on `kind: resource` or `kind: resource_template`.",
          "type": [
            "string",
            "null"
          ]
        },
        "mime_type": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "output_schema": {
          "default": null
        },
        "prompt_arguments": {
          "default": null,
          "items": {
            "$ref": "#/definitions/PromptArgumentConfig"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "quotas": {
          "anyOf": [
            {
              "$ref": "#/definitions/BackendQuotasRef"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-binding quota policy reference. Names at most one of each kind by id; each id must resolve to a registered policy in `governance.quotas.{rate_limits,budgets,concurrency}[]`. `None` (default) means this binding is exempt from quota enforcement. The runtime gate that consults this field is behind the `governance-quotas` cargo feature — until that feature is on, the field is parsed + validated but inert."
        },
        "resource_annotations": {
          "anyOf": [
            {
              "$ref": "#/definitions/BackendResourceAnnotations"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional resource annotations (`audience`, `priority`, `lastModified`) surfaced on `resources/list` entries. Meaningful only on `kind: resource` or `kind: resource_template` bindings."
        },
        "resource_size": {
          "default": null,
          "description": "Optional per-resource size hint (bytes) surfaced on `resources/list` entries. Meaningful only on `kind: resource` bindings; ignored elsewhere.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "retry": {
          "anyOf": [
            {
              "$ref": "#/definitions/RetryConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "task_support": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "uri_template": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "variable_completions": {
          "additionalProperties": {
            "$ref": "#/definitions/VariableCompletionSource"
          },
          "description": "Optional completion sources per template variable. The `completion/complete` handler returns the filtered subset matching the caller's prefix when the MCP client opens auto-complete on a resource template variable. Keys MUST match a `{variable}` declared in `uri_template`; mismatched keys are dropped at registration with a warning.\n\nEach value is a [`VariableCompletionSource`] — either a bare `[\"v1\", \"v2\"]` array (shorthand for `{ kind: static, values: [...] }`) or a tagged `{ kind: static | dynamic, … }` object. The `dynamic` form names a registered backend; the gateway dispatches to that backend's [`BackendPlugin::complete_template_variable`] at request time and clamps the result to 100 values.",
          "type": [
            "object",
            "null"
          ]
        },
        "watch": {
          "anyOf": [
            {
              "$ref": "#/definitions/ResourceWatchConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "required": [
        "backend",
        "description",
        "name"
      ],
      "type": "object"
    },
    "BackendGovernanceConfig": {
      "properties": {
        "allow_if": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "minimum_trust": {
          "allOf": [
            {
              "$ref": "#/definitions/TrustLevelConfig"
            }
          ],
          "default": "header_asserted"
        },
        "required_scopes": {
          "description": "Scopes the caller's credential MUST carry to invoke this binding (OAuth `scope`, or an AAuth auth token's `scope`). A caller short of them is answered with the step-up challenge naming the missing scopes (`WWW-Authenticate … error=\"insufficient_scope\"`; for an AAuth person or auth-token caller, `AAuth-Requirement: requirement=auth-token` carrying a resource token). Empty (the default) means no scope requirement.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "BackendIconConfig": {
      "description": "Configurable descriptor icon shape, mirrored onto MCP's `Icon` type.",
      "properties": {
        "mime_type": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "sizes": {
          "default": null,
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "src": {
          "type": "string"
        },
        "theme": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "src"
      ],
      "type": "object"
    },
    "BackendImpl": {
      "description": "Implementation backend, identified by `kind:` with an opaque flattened `spec`. The gateway enumerates NO plugin kinds: a binding names a `kind` (a loaded `BackendPlugin::kind()` string) and every other key flattens into `spec`, forwarded verbatim to the plugin's `register_profile` / `execute`. The plugin owns and validates the schema; the gateway resolves `kind` against the registry at boot and fails closed on unknown / non-backend kinds. Mirrors `WatchStrategyConfig::Plugin`.\n\n`spec` is an open object (no `deny_unknown_fields`); unknown keys are forwarded verbatim, so a spec-key typo is caught only by the owning plugin's `register_profile` (`InvalidSpec` at boot), not at config-load.",
      "properties": {
        "kind": {
          "description": "Target `BackendPlugin::kind()` string, resolved against the registry at boot.",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "BackendQuotasRef": {
      "additionalProperties": false,
      "description": "Per-binding quota reference — operator names at most one of each policy kind by id. The runtime gate that consults these refs is gated behind the `governance-quotas` cargo feature.\n\nAll three fields are independent options; an absent field means \"no policy of that kind for this binding\". A binding may name more than one kind at once (e.g. both a rate limit AND a concurrency cap), but at most one policy of each kind.",
      "properties": {
        "budget": {
          "description": "Id from `governance.quotas.budgets[].id`. Optional.",
          "type": [
            "string",
            "null"
          ]
        },
        "concurrency": {
          "description": "Id from `governance.quotas.concurrency[].id`. Optional.",
          "type": [
            "string",
            "null"
          ]
        },
        "rate_limit": {
          "description": "Id from `governance.quotas.rate_limits[].id`. Optional.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "BackendResourceAnnotations": {
      "description": "Operator-configurable MCP `ContentAnnotations` for a resource binding.",
      "properties": {
        "audience": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "last_modified": {
          "type": [
            "string",
            "null"
          ]
        },
        "priority": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "BudgetPolicy": {
      "additionalProperties": false,
      "description": "One named budget policy (cost cap / call count / token count).",
      "properties": {
        "acknowledge_unenforced": {
          "description": "Acknowledge that a `cost` / `token_count` budget is advisory. Per-call USD spend and token usage are not available to the gateway after dispatch, so these caps cannot be enforced at runtime — only `call_count` is. A `cost`/`token_count` budget must set this to load, making the no-op posture an explicit operator choice rather than a silent fail-open. No effect for `call_count`.",
          "type": "boolean"
        },
        "cap_calls": {
          "description": "Call cap when `kind: call_count`. Required for that kind.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "cap_token_count": {
          "description": "Token cap when `kind: token_count`. Required for that kind.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "cap_usd": {
          "description": "Cost cap in USD when `kind: cost`. Required for that kind.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "identity_claim": {
          "description": "JWT claim path for `per_identity` scope.",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "default": "cost",
          "description": "Budget kind: `cost` (USD), `call_count`, or `token_count`.",
          "type": "string"
        },
        "on_exceeded": {
          "default": "deny",
          "description": "Action when the cap is hit (same vocabulary as RateLimitPolicy).",
          "type": "string"
        },
        "scope": {
          "default": "per_identity",
          "description": "Scope discriminator (same vocabulary as RateLimitPolicy).",
          "type": "string"
        },
        "warn_at_pct": {
          "description": "Emit `governance.quota.warn` when drawdown crosses this percentage of the cap. Defaults to no warning. `0..=100`.",
          "format": "uint8",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "window": {
          "description": "Rolling window over which the cap applies. Suffixes `s`/`m`/ `h`/`d`. Maximum `30d`.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "window"
      ],
      "type": "object"
    },
    "BusOverrideConfig": {
      "additionalProperties": true,
      "description": "`<capability>.bus: { kind, … }` — produces an `Arc<dyn mcpg_cluster_api::PubSub>` at boot.\n\nRecognised `kind` values: `cluster`, `memory`. (`redis` and `nats` are not accepted here — set `cluster.kind: redis | nats` and use `kind: cluster` here, or omit the override entirely.)",
      "properties": {
        "kind": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "CancellationConfig": {
      "additionalProperties": false,
      "description": "`cancellation:` config — cluster-wide cancellation fan-out (`notifications/cancelled` and `tasks/cancel`) plus a per-capability `bus:` override. Same shape as `DeliveryConfig`.",
      "properties": {
        "bus": {
          "anyOf": [
            {
              "$ref": "#/definitions/BusOverrideConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "partition_by_principal": {
          "default": false,
          "description": "When true, cancellations publish to `mcpg.cancel.<principal>` and the subscriber listens on the `mcpg.cancel.*` wildcard, so broker-native subject ACLs can fence per-principal cancel traffic. Defaults to `false` (a single flat `mcpg.cancel` topic). **Requires a wildcard-capable pub/sub backend (redis/nats)** — `AppConfig::validate` rejects it on the in-process single-node / memory bus, which is exact-match only and would silently drop every cancellation under a wildcard subscribe.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ChildInvokeConfig": {
      "additionalProperties": false,
      "description": "Governance for the agentic child-dispatch surface (`invoke_tool`).\n\nDirect `tools/call` always runs the full pre-dispatch stack; the child path an LLM binding drives did not. When `enforce_gates` is on, child invocations are routed through the same external policy_engine chain and tool_gate plugin chain as a direct call before reaching the backend, so tool-level access controls are not silently absent on the LLM-driven surface.",
      "properties": {
        "enforce_gates": {
          "default": false,
          "description": "Run the policy_engine chain + tool_gate plugin chain on every child `invoke_tool`. Default false (the agentic surface is ungated, matching prior behaviour) — enable to require the same authorization a direct `tools/call` gets. A child whose identity is unresolved (the LLM path carries no per-call principal today) evaluates against the inherited parent identity.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ClaimMappingConfig": {
      "additionalProperties": false,
      "properties": {
        "attribute_claim_mappings": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "type": "object"
        },
        "group_claim_paths": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "role_claim_paths": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "scope_claim_paths": {
          "default": [
            "scope",
            "scp"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "subject_claim": {
          "default": "sub",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ClientCertMode": {
      "description": "Operator-facing client-cert acceptance mode.",
      "enum": [
        "none",
        "optional",
        "mandatory"
      ],
      "type": "string"
    },
    "CloudConfig": {
      "additionalProperties": false,
      "description": "`cloud:` — present only on managed-fleet (mcpg.cloud) instances. Absent for self-host; every field defaults so a bare `cloud: {}` is inert.",
      "properties": {
        "allow_anonymous": {
          "default": false,
          "description": "Publish-time acknowledgement that this managed instance intentionally serves `/mcp` WITHOUT a configured token verifier (an anonymous / public MCP server). The CP publish guard requires EITHER a verifier (`governance.access.jwks` / `governance.access.oidc_oauth`) OR this opt-out, so a tenant can't expose an unauthenticated gateway on the public edge by omission. The gateway runtime does not read this field — it is a declaration the publish guard checks.",
          "type": "boolean"
        },
        "custom_domains": {
          "description": "Additional customer-owned hostnames that resolve to this instance (CNAME → the instance edge). Developer-owned; each must be a valid DNS hostname. Empty for the default-domain-only case.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "environment": {
          "description": "Environment slug (dev / staging / prod …).",
          "type": [
            "string",
            "null"
          ]
        },
        "instance_id": {
          "description": "Server-assigned stable id. None for self-host. Read-only — set by the CP.",
          "type": [
            "string",
            "null"
          ]
        },
        "isolation": {
          "allOf": [
            {
              "$ref": "#/definitions/CloudIsolation"
            }
          ],
          "default": "shared",
          "description": "Isolation tier for placement."
        },
        "name": {
          "description": "Human-friendly display name for the instance.",
          "type": [
            "string",
            "null"
          ]
        },
        "provenance": {
          "allOf": [
            {
              "$ref": "#/definitions/CloudProvenance"
            }
          ],
          "default": {},
          "description": "Server-managed placement provenance. Stamped by the provisioner/operator; ignored / overwritten if hand-written."
        },
        "region": {
          "description": "Placement region hint (free-form; matched against fleet capacity).",
          "type": [
            "string",
            "null"
          ]
        },
        "subdomain": {
          "description": "Globally-unique DNS label that addresses this instance: `https://{subdomain}.mcpg.cloud/mcp`. Read-only — assigned/reserved by the CP. Defaults conceptually to `instance_id` when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "tenant": {
          "description": "Tenant / org slug — billing + console grouping. Developer-owned.",
          "type": [
            "string",
            "null"
          ]
        },
        "tier": {
          "allOf": [
            {
              "$ref": "#/definitions/CloudTier"
            }
          ],
          "default": "unspecified",
          "description": "Billing tier the instance runs under."
        },
        "workspace": {
          "description": "Workspace slug within the tenant.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CloudIsolation": {
      "description": "Default isolation tier for the instance's placement.",
      "oneOf": [
        {
          "description": "Shares a namespace pool with other tenants (default).",
          "enum": [
            "shared"
          ],
          "type": "string"
        },
        {
          "description": "Dedicated nodes / namespace for the tenant.",
          "enum": [
            "dedicated"
          ],
          "type": "string"
        }
      ]
    },
    "CloudProvenance": {
      "additionalProperties": false,
      "description": "Server-managed placement facts. Never trusted from a published config — the operator overwrites these at render time.",
      "properties": {
        "cluster_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "external_url": {
          "description": "Canonical external URL (`https://{subdomain}.mcpg.cloud/mcp`). Operator-injected into `governance.access.resource_metadata.resource`; OVERWRITTEN at render — never trust a published value.",
          "type": [
            "string",
            "null"
          ]
        },
        "managed_by": {
          "type": [
            "string",
            "null"
          ]
        },
        "namespace": {
          "type": [
            "string",
            "null"
          ]
        },
        "provisioned_at": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CloudTier": {
      "description": "Billing tier the instance was provisioned under.\n\nThe variants mirror the licensing vocabulary (`community` | `pro` | `team` | `enterprise`) so this block and a license claim can be compared without a translation table. `free` is accepted as an alias for `community`, which is what this variant used to be called.",
      "oneOf": [
        {
          "enum": [
            "community",
            "pro",
            "team",
            "enterprise"
          ],
          "type": "string"
        },
        {
          "description": "No tier asserted (self-host / unmanaged).",
          "enum": [
            "unspecified"
          ],
          "type": "string"
        }
      ]
    },
    "ClusterConfig": {
      "additionalProperties": true,
      "description": "Top-level cluster config. The cluster plugin is the unified backbone for MCPG multi-instance state + coordination — it internally instantiates the four primitive impls (`KeyValueStore`, `PubSub`, `Lease`, `Watch`) and exposes them via accessor methods. `kind` is the discriminator; everything else is kind-specific config that flows straight to the plugin's factory as JSON.\n\n```yaml cluster: kind: redis              # single_node | etcd | consul | nats | redis url: ${env.REDIS_URL}   # rest of the fields are kind-specific key_prefix: \"mcpg:cluster:\" ```\n\n`kind: single_node` (the default when `cluster:` is omitted) installs the in-process built-in coordinator and ignores the rest of the block. Other kinds map to `mcpg-plugin-cluster-<kind>`; the cdylib must still be declared under `plugins[]` (the inline `cluster.*` fields override any `config:` block on the matching entry).",
      "properties": {
        "allow_degraded_boot": {
          "default": false,
          "description": "Tolerate a coordinator that advertises `kv`/`bus` roles but fails the boot reachability probe (a live round-trip against the advertised primitives). Default `false`: for a clustered (non-`single_node`) coordinator the gateway probes each advertised primitive at boot and **refuses to start** if the round-trip fails or the accessor is absent, rather than silently de-clustering to per-replica in-process state. Set `true` ONLY when an operator knowingly wants the gateway to boot and run degraded (per-replica state) despite an unreachable coordinator — it logs a loud error and continues. Gateway-only named field — kept out of the flattened plugin `config`.",
          "type": "boolean"
        },
        "allow_insecure_transport": {
          "default": false,
          "description": "Permit a plaintext (non-TLS) coordinator transport for a non-`single_node` coordinator. Defaults to `false`: `validate()` refuses a plaintext redis/consul/etcd/nats coordinator at boot, because the coordinator carries all shared state (sessions, credentials, delivery) in clear. Set `true` ONLY for local/dev/CI. Gateway-only — NOT forwarded to the plugin (it is a named field, so serde keeps it out of `config`).",
          "type": "boolean"
        },
        "kind": {
          "default": "single_node",
          "type": "string"
        },
        "readiness_gate": {
          "allOf": [
            {
              "$ref": "#/definitions/ClusterReadinessGate"
            }
          ],
          "default": "off",
          "description": "Whether coordinator health gates `/ready`. Defaults to `off` (fail-open): a coordinator outage is surfaced only via the `mcpg_cluster_backend_up` gauge + its alert, never on readiness. `degrade` adds an informational not-ready *check* to the readiness body but keeps `/ready` green (no LB flapping). `fail` makes `/ready` return not-ready while the coordinator is unreachable (fail-closed). Gateway-only named field — kept out of the flattened plugin `config`."
        },
        "state_encryption_allow_plaintext_reads": {
          "default": false,
          "description": "Tolerate plaintext (non-envelope) reads while `state_encryption_key_env` is set — a bounded migration window for rolling a key in across replicas. Default `false`: once a key is configured a plaintext value on the coordinator KV/bus is rejected (fail closed), so an unkeyed peer or attacker cannot inject unauthenticated capability state. Set `true` only transiently during a rollout; turn it off once every replica writes envelopes. Inert without `state_encryption_key_env`. Gateway-only named field — kept out of the flattened plugin `config`.",
          "type": "boolean"
        },
        "state_encryption_key_env": {
          "description": "Opt-in application-layer AEAD (XChaCha20-Poly1305) of ALL coordinator-backed *capability* state — sessions (incl. SSE replay), delivery, cancellation, tasks, pipelines, idempotency, request-state, subscriptions, quota, and the approvals backstop. Names the **env var** holding a URL-safe-base64 32-byte key (the key itself never sits in the config artifact). Unset = plaintext serde on the wire/at-rest; confidentiality then rests on the transport guard. Values are sealed per-key/per-topic (swap-resistant); keys/topics stay cleartext for routing. Does NOT cover the credential cache — it has its own `encryption_key` under the credentials config. Gateway-only named field — kept out of the flattened plugin `config`.",
          "type": [
            "string",
            "null"
          ]
        },
        "state_encryption_key_id": {
          "description": "Key id (kid) stamped on state envelopes for rotation visibility. Defaults to `mcpg-cluster-state` when a key is configured. Inert without `state_encryption_key_env`. Gateway-only named field.",
          "type": [
            "string",
            "null"
          ]
        },
        "tenant_segment": {
          "description": "Optional per-deployment tenant segment. When set, EVERY coordinator-backed capability KV key and bus topic is prefixed with `t.<segment>/` (keys) / `t.<segment>.` (topics) so a single coordinator namespace can be fenced per-tenant by broker-native ACLs — NATS subject perms `t.<segment>.>`, redis key-pattern ACLs (`~…t.<segment>/*`), consul/etcd path ACLs. Unset = today's flat, un-prefixed keys/topics (one coordinator namespace == one trust domain). This is a **deployment-level** label, not a per-request tenant — the gateway process serves one tenant segment; the runtime carries no per-request tenant at key/topic-formation time, so per-request multi-tenancy remains future work. Turning it on is a key-namespace cutover (existing flat-keyed state goes invisible). Must be a single token (no `. * > / : ` or whitespace). Gateway-only named field — kept out of the flattened plugin `config`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ClusterReadinessGate": {
      "description": "How coordinator health affects `/ready`.",
      "oneOf": [
        {
          "description": "Coordinator health never affects `/ready` (fail-open). Default.",
          "enum": [
            "off"
          ],
          "type": "string"
        },
        {
          "description": "Surface a not-ready *check* in the readiness body when the coordinator is down, but keep the overall `/ready` status green.",
          "enum": [
            "degrade"
          ],
          "type": "string"
        },
        {
          "description": "`/ready` returns not-ready while the coordinator is unreachable.",
          "enum": [
            "fail"
          ],
          "type": "string"
        }
      ]
    },
    "ColumnFormat": {
      "description": "How a cell value is rendered client-side.",
      "enum": [
        "text",
        "number",
        "currency",
        "date",
        "badge",
        "link"
      ],
      "type": "string"
    },
    "ConcurrencyPolicy": {
      "additionalProperties": false,
      "description": "One named concurrency cap.",
      "properties": {
        "id": {
          "type": "string"
        },
        "identity_claim": {
          "type": [
            "string",
            "null"
          ]
        },
        "max_concurrent": {
          "description": "Maximum simultaneous in-flight calls.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "on_exceeded": {
          "default": "deny",
          "description": "Action when the cap is hit. `deny` returns immediately; `queue` waits up to `queue_timeout_ms`.",
          "type": "string"
        },
        "queue_timeout_ms": {
          "default": 30000,
          "description": "Timeout for queued callers when `on_exceeded: queue`. Default 30s.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "scope": {
          "default": "per_identity",
          "description": "Scope (typically `per_tool`, `global`, or `per_identity`).",
          "type": "string"
        }
      },
      "required": [
        "id",
        "max_concurrent"
      ],
      "type": "object"
    },
    "ConfigWatchConfig": {
      "additionalProperties": false,
      "description": "`gateway.config_watch:` — operator-tunable file-watch reload trigger. Same semantics as SIGHUP and the admin endpoint: full `GatewayRuntime` rebuild via `ArcSwap`; session store preserved; credential cache rebuilt fresh; `list_changed` emitted per category for operational sessions on inventory delta.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "When true, the gateway watches its config files on disk and triggers a hot-reload when contents change. Polling- based — handles editor-write-via-rename (vim/emacs) and K8s ConfigMap atomic-symlink-swap transparently because the watcher reads through the symlink chain regardless of how the write landed. Defaults to disabled — operators must opt in.",
          "type": "boolean"
        },
        "poll_interval_ms": {
          "default": 5000,
          "description": "Poll interval in milliseconds. Lower = faster reload after edit; higher = lower disk I/O. Default 5000 (5s) is imperceptible for config changes and trivial in I/O cost. Values below 1000 (1s) are clamped to 1000 at validate time with a warning — sub-second polling burns I/O for no human-perceivable benefit.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "ConflictPolicy": {
      "description": "Conflict policy on a body-hash mismatch with a stored record. Today only `Reject` is implemented; `permissive_replay` is not offered.",
      "oneOf": [
        {
          "description": "Same key + different body hash → JSON-RPC error `-32010 IdempotencyConflict` (HTTP 422).",
          "enum": [
            "reject"
          ],
          "type": "string"
        }
      ]
    },
    "ControlPlaneAttachConfig": {
      "additionalProperties": false,
      "description": "Control Plane attachment config. See `apps/gateway/src/runtime/cp/attach.rs` for the wiring logic.",
      "properties": {
        "bootstrap_ca_pem": {
          "default": null,
          "description": "PEM-encoded CA bundle to trust on the very first connect (Register), before the agent has its own creds. Once Register completes, the CP-issued cert/key/ca_chain trio supplants this. Optional; only required when the CP gRPC listener is TLS and the operator hasn't pre-populated a previous run's `agent-creds.json`.",
          "type": [
            "string",
            "null"
          ]
        },
        "capture_payloads": {
          "default": false,
          "description": "**Enterprise opt-in.** When `true`, the gateway captures the JSON-serialized request arguments + response of each tool call and ships them in the `MetricsReport` (Channel-encrypted; the CP further encrypts at-rest with a per-tenant KMS-derived key). The captured bytes can contain PII / secrets, so this is off by default; the CP also gates ingest on the active license carrying the `payload_capture` feature flag, so flipping this `true` without a matching license is a no-op (samples ship but CP drops the payload bytes).",
          "type": "boolean"
        },
        "enrollment_url": {
          "default": null,
          "description": "One-time enrollment URL minted by the CP UI. Required on first boot; subsequent boots reuse the cached creds in `state_dir`.",
          "type": [
            "string",
            "null"
          ]
        },
        "heartbeat_interval_ms": {
          "default": null,
          "description": "Seconds between heartbeats. Default 30s.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "instance_uid": {
          "default": null,
          "description": "Stable per-instance id. Defaults to `${HOSTNAME}-${uuid7-prefix}` when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "state_dir": {
          "default": "./mcpg-cp-state",
          "description": "Where to persist agent creds (`agent-creds.json`) and the LKG cache. Defaults to `./mcpg-cp-state`.",
          "type": "string"
        },
        "url": {
          "description": "gRPC URL of the Control Plane (e.g. `\"https://cp.example.com:7844\"`).",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "CredentialsClusterConfig": {
      "additionalProperties": false,
      "properties": {
        "allow_plaintext": {
          "default": false,
          "description": "Explicit, INSECURE opt-in to publish credential events in plaintext (no `encryption_key_env`). Required to enable cluster credential pub/sub without a key — otherwise the gateway refuses to boot rather than silently broadcasting plaintext credentials. On this path `published_by` is forgeable, so plaintext mode also REQUIRES a non-empty `allowed_publishers`; that allowlist plus broker write-ACLs are the integrity boundary. AEAD via `encryption_key_env` is the only integrity-providing mode.",
          "type": "boolean"
        },
        "allowed_publishers": {
          "description": "Optional allowlist of peer node ids whose credential-cache events this instance will apply. When `Some`, an `Issued` / `Revoked` event whose `published_by` is not in the list is dropped. This is a genuine control on the **AEAD** path (`published_by` is inside the sealed payload, so it is authenticated) — it bounds the blast radius of a compromised-but-keyed peer. On the plaintext path (`allow_plaintext: true`) it is best-effort only: `published_by` is attacker-forgeable there, so the allowlist raises the bar but is NOT a substitute for `encryption_key_env`. MANDATORY (non-empty) when `allow_plaintext` is set. Unset = accept events from any peer (only valid on the AEAD path).",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "encryption_key_env": {
          "description": "Env var holding a base64-encoded 32-byte key for application-layer AEAD (XChaCha20-Poly1305) of the credential events published on the cluster topic. STRONGLY recommended: without it, per-caller credentials are published as plaintext JSON and confidentiality rests entirely on transport TLS . All peers sharing the topic must use the same key.",
          "type": [
            "string",
            "null"
          ]
        },
        "encryption_key_id": {
          "description": "Key id (kid) stamped on encrypted envelopes so operators can rotate keys. Defaults to `mcpg-cred-cache` when a key is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "topic": {
          "description": "Override the default cluster topic (`mcpg.credentials.events`). Operators with multiple independent MCPG deployments sharing one `cluster_backend` MUST namespace per-deployment so peer caches don't pollute each other.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CredentialsConfig": {
      "additionalProperties": false,
      "description": "Defaults are safe for single-node deploys; multi-instance deploys with per-caller dynamic credentials (e.g. Vault dynamic DB) MUST configure `cluster.enabled: true` to avoid cache divergence.",
      "properties": {
        "cluster": {
          "allOf": [
            {
              "$ref": "#/definitions/CredentialsClusterConfig"
            }
          ],
          "default": {
            "allow_plaintext": false,
            "enabled": false
          },
          "description": "Optional cluster pub/sub wrapper. When `enabled: true` AND a `cluster_backend` is bound, the gateway wraps the L1 cache with `ClusteredCredentialCache` so every peer instance sees Issued / Revoked events. Drops to local-only behaviour with a warning when `enabled: true` but no coordinator is bound."
        },
        "key_attributes": {
          "description": "Identity attribute (token-claim) names folded into the credential-cache key so callers differing only by these claims (commonly the tenant claim) get separate cached credentials. Empty (default) excludes attributes from the key — set this to your tenant claim name(s) when a `credential_issuer` derives its principal from an attribute claim, otherwise those callers share one credential. In a clustered cache every peer MUST set the same `key_attributes` (the published event hash is computed with it) — divergence silently produces per-node cache misses, the same all-peers-agree constraint that already governs the hash algorithm.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_cache_ttl_ms": {
          "default": 3600000,
          "description": "Operator-side cap on per-entry TTL. Even if a plugin returns a 24-hour TTL, the cache evicts at this cap to limit blast radius from leaked / compromised credentials. Default 3600 (1 hour).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_entries": {
          "default": 10000,
          "description": "Maximum number of `(identity, plugin, target)` entries kept in the L1 cache. LRU eviction past this. Default 10000 — at ~500 bytes per entry that's ~5MB worst case.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "DebugCommandToolConfig": {
      "additionalProperties": false,
      "properties": {
        "args": {
          "default": [
            "mcpg-debug-command\n"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "default": "printf",
          "type": "string"
        },
        "max_output_bytes": {
          "default": 4096,
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "timeout_ms": {
          "default": 2000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "DebugConfig": {
      "additionalProperties": false,
      "description": "Top-level `debug:` block — diagnostic tools surface only. The master switch lives at `feature_flags.debug_tools_enabled`. When that flag is `false`, every field below is ignored AND the `mcpg.debug.*` tools are stripped from the capability registry regardless of `tools.exposure`.",
      "properties": {
        "tools": {
          "allOf": [
            {
              "$ref": "#/definitions/DebugToolsConfig"
            }
          ],
          "default": {
            "bindings": {
              "command_probe_profile": "default_command_probe",
              "network_json_call_profile": "default_network_probe",
              "network_probe_profile": "default_network_probe"
            },
            "command_profiles": {
              "default_command_probe": {
                "args": [
                  "mcpg-debug-command\n"
                ],
                "command": "printf",
                "max_output_bytes": 4096,
                "timeout_ms": 2000
              }
            },
            "exposure": {
              "command_probe": true,
              "network_json_call": false,
              "network_probe": true,
              "operational_overview_prompt": true,
              "runtime_overview_resource": true
            },
            "network_profiles": {
              "default_network_probe": {
                "expected_status_codes": [
                  200
                ],
                "headers": {},
                "max_response_bytes": 4096,
                "require_json_response": false,
                "timeout_ms": 2000,
                "url": "http://127.0.0.1:8787/health"
              }
            }
          },
          "description": "Operator-defined diagnostic tools surfaced as MCP tools when `feature_flags.debug_tools_enabled: true`. See [`DebugToolsConfig`] for the surface."
        }
      },
      "type": "object"
    },
    "DebugNetworkToolConfig": {
      "additionalProperties": false,
      "properties": {
        "expected_status_codes": {
          "default": [
            200
          ],
          "items": {
            "format": "uint16",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "type": "object"
        },
        "max_response_bytes": {
          "default": 4096,
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "require_json_response": {
          "default": false,
          "type": "boolean"
        },
        "timeout_ms": {
          "default": 2000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "url": {
          "default": "http://127.0.0.1:8787/health",
          "type": "string"
        }
      },
      "type": "object"
    },
    "DebugToolBackendsConfig": {
      "additionalProperties": false,
      "properties": {
        "command_probe_profile": {
          "default": "default_command_probe",
          "type": "string"
        },
        "network_json_call_profile": {
          "default": "default_network_probe",
          "type": "string"
        },
        "network_probe_profile": {
          "default": "default_network_probe",
          "type": "string"
        }
      },
      "type": "object"
    },
    "DebugToolExposureConfig": {
      "additionalProperties": false,
      "properties": {
        "command_probe": {
          "default": true,
          "type": "boolean"
        },
        "network_json_call": {
          "default": false,
          "type": "boolean"
        },
        "network_probe": {
          "default": true,
          "type": "boolean"
        },
        "operational_overview_prompt": {
          "default": true,
          "type": "boolean"
        },
        "runtime_overview_resource": {
          "default": true,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DebugToolsConfig": {
      "additionalProperties": false,
      "properties": {
        "bindings": {
          "allOf": [
            {
              "$ref": "#/definitions/DebugToolBackendsConfig"
            }
          ],
          "default": {
            "command_probe_profile": "default_command_probe",
            "network_json_call_profile": "default_network_probe",
            "network_probe_profile": "default_network_probe"
          }
        },
        "command_profiles": {
          "additionalProperties": {
            "$ref": "#/definitions/DebugCommandToolConfig"
          },
          "default": {},
          "type": "object"
        },
        "exposure": {
          "allOf": [
            {
              "$ref": "#/definitions/DebugToolExposureConfig"
            }
          ],
          "default": {
            "command_probe": true,
            "network_json_call": false,
            "network_probe": true,
            "operational_overview_prompt": true,
            "runtime_overview_resource": true
          }
        },
        "network_profiles": {
          "additionalProperties": {
            "$ref": "#/definitions/DebugNetworkToolConfig"
          },
          "default": {},
          "type": "object"
        }
      },
      "type": "object"
    },
    "DeliveryConfig": {
      "additionalProperties": false,
      "description": "`delivery:` config — delivery bus (the internal pub/sub that fans server-initiated messages out to the SSE stream owning each session) + per-capability `bus:` override. When `bus` is unset, the gateway inherits the cluster's `pub_sub()` primitive.",
      "properties": {
        "bus": {
          "anyOf": [
            {
              "$ref": "#/definitions/BusOverrideConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "DisclosureLevel": {
      "enum": [
        "summary",
        "redacted",
        "full"
      ],
      "type": "string"
    },
    "EnumSource": {
      "additionalProperties": false,
      "description": "A dynamic-enum source: a sibling tool whose result supplies options.",
      "properties": {
        "args": {
          "additionalProperties": {
            "type": "string"
          },
          "type": [
            "object",
            "null"
          ]
        },
        "label_field": {
          "type": "string"
        },
        "tool": {
          "type": "string"
        },
        "value_field": {
          "type": "string"
        }
      },
      "required": [
        "label_field",
        "tool",
        "value_field"
      ],
      "type": "object"
    },
    "FeatureFlagsConfig": {
      "additionalProperties": false,
      "description": "Operator-controlled strictness / compat flags.\n\nEvery field defaults to the safe / standards-compliant value; flipping a flag is an explicit acknowledgement that the operator is taking on the risk the default protects against.",
      "properties": {
        "allow_header_passthrough": {
          "default": false,
          "description": "Forward credential-shaped inbound HTTP headers (`Authorization`, `Cookie`, `X-API-Key`, …) through to outbound bindings. The gateway strips these by default to avoid leaking client tokens to upstreams. Flip to `true` only for deployments that intentionally proxy bearer tokens to the binding (e.g., a pure-router deployment in a trusted network).",
          "type": "boolean"
        },
        "debug_tools_enabled": {
          "default": false,
          "description": "Master switch for the operator-defined diagnostic tools (`mcpg.command.*` / `mcpg.network.*`). When `false`, every field under the top-level `debug:` block is ignored AND the debug tools are stripped from the capability registry regardless of `debug.tools.exposure`. Production deploys keep this off; flip on for CI / dev only.\n\nThis flag lives here (rather than at the previous `debug.enabled` location) so every operator-controlled strictness toggle lives in one block.",
          "type": "boolean"
        },
        "sep2260_panic_on_orphan": {
          "default": false,
          "description": "Upgrade SEP-2260 violations (server-initiated request emitted without an originating client request id) from a warning + metric counter to a process panic. Useful in CI / dev where the violation indicates a bug; should stay `false` in production so a single misrouted code path does not take the gateway down.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "FederationCacheConfig": {
      "additionalProperties": false,
      "description": "Capability-cache behaviour.",
      "properties": {
        "capability_ttl_secs": {
          "default": 300,
          "description": "Re-list the upstream's capabilities every N seconds even without a `list_changed` notification.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "FederationConfig": {
      "additionalProperties": false,
      "description": "One federated upstream MCP server.",
      "properties": {
        "cache": {
          "allOf": [
            {
              "$ref": "#/definitions/FederationCacheConfig"
            }
          ],
          "default": {
            "capability_ttl_secs": 300
          },
          "description": "Capability-cache behaviour (TTL refresh)."
        },
        "filter": {
          "allOf": [
            {
              "$ref": "#/definitions/FilterConfig"
            }
          ],
          "default": {
            "exclude_tools": [],
            "include_tools": [
              "*"
            ]
          },
          "description": "Allow/deny filtering of imported tool names."
        },
        "governance": {
          "allOf": [
            {
              "$ref": "#/definitions/BackendGovernanceConfig"
            }
          ],
          "default": {
            "allow_if": null,
            "minimum_trust": "header_asserted"
          },
          "description": "Governance inherited by every capability imported from this upstream — identical block to a native binding's, so the gate chain treats a federated call exactly like a native one."
        },
        "import": {
          "allOf": [
            {
              "$ref": "#/definitions/ImportConfig"
            }
          ],
          "default": {
            "prompts": false,
            "resource_templates": false,
            "resources": false,
            "tools": true
          },
          "description": "Which capability surfaces to import."
        },
        "name": {
          "description": "Source id. Also the default capability-prefix namespace and the `federated_from` label on synthetic capabilities.",
          "type": "string"
        },
        "naming": {
          "allOf": [
            {
              "$ref": "#/definitions/NamingConfig"
            }
          ],
          "default": {},
          "description": "Prefixes applied to imported capability names / URIs."
        },
        "response": {
          "allOf": [
            {
              "$ref": "#/definitions/ResponseConfig"
            }
          ],
          "default": {
            "max_response_bytes": 2097152
          },
          "description": "Per-call response limits enforced gateway-side."
        },
        "retry": {
          "anyOf": [
            {
              "$ref": "#/definitions/RetryConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-call retry policy for upstream dispatch."
        },
        "session": {
          "allOf": [
            {
              "$ref": "#/definitions/SessionConfig"
            }
          ],
          "default": {
            "idle_timeout_secs": 600
          },
          "description": "Upstream-session behaviour."
        },
        "synthesize": {
          "allOf": [
            {
              "$ref": "#/definitions/SynthesizeConfig"
            }
          ],
          "default": {
            "poll_interval_ms": 30000,
            "resources_updated": "auto"
          },
          "description": "Change-notification synthesis for upstreams that cannot push."
        },
        "upstream": {
          "allOf": [
            {
              "$ref": "#/definitions/UpstreamConfig"
            }
          ],
          "description": "Upstream connection (url, transport, auth, safety)."
        }
      },
      "required": [
        "name",
        "upstream"
      ],
      "type": "object"
    },
    "FilterConfig": {
      "additionalProperties": false,
      "description": "Allow/deny filtering of imported tool names (glob `*` suffix).",
      "properties": {
        "exclude_tools": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include_tools": {
          "default": [
            "*"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GatewayAppConfig": {
      "additionalProperties": false,
      "description": "One gateway-authored templated app. Minted as `ui://mcpg/<id>`.",
      "properties": {
        "allow_credential_values": {
          "default": false,
          "description": "Gate that must be set for `cred://` to appear in `public_values`.",
          "type": "boolean"
        },
        "app_tools": {
          "description": "Read-only App-Provided Tools the app exposes to the agent.",
          "items": {
            "$ref": "#/definitions/AppProvidedTool"
          },
          "type": "array"
        },
        "columns": {
          "description": "Explicit columns. `None` ⇒ derived from `data_tool.outputSchema`.",
          "items": {
            "$ref": "#/definitions/AppColumn"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "csp": {
          "anyOf": [
            {
              "$ref": "#/definitions/AppCspDecl"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-app author CSP declaration (still intersected by the egress `csp_policy` — declaring an axis never widens it)."
        },
        "data_args": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Static argument template passed to `data_tool` on the initial load (named string args; richer typing is a later phase).",
          "type": [
            "object",
            "null"
          ]
        },
        "data_tool": {
          "description": "Data-source tool. Required for `form`; optional for static kinds (e.g. a `signature_pad`). Its `outputSchema`/`inputSchema` seeds the columns/fields when those are omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "fields": {
          "description": "Explicit detail/form fields. `None` ⇒ derived from the schema.",
          "items": {
            "$ref": "#/definitions/AppField"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "id": {
          "description": "Unique id; becomes the `ui://mcpg/<id>` authority path segment. `[a-z0-9]` then `[a-z0-9-]*`.",
          "type": "string"
        },
        "id_field": {
          "description": "JSON-path to a row's stable id. Default `$.id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/GatewayAppKind"
            }
          ],
          "description": "Which shipped shell renders this app."
        },
        "map": {
          "anyOf": [
            {
              "$ref": "#/definitions/MapAppConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Map binding; present iff `kind == map`."
        },
        "page_size": {
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "permissions": {
          "description": "iframe permissions the app requests (clamped by `allowed_permissions`).",
          "items": {
            "$ref": "#/definitions/AppsPermission"
          },
          "type": "array"
        },
        "prefers_border": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "primary_action": {
          "description": "`row_actions[].id` fired on a row click / primary interaction.",
          "type": [
            "string",
            "null"
          ]
        },
        "public_values": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Opt-in non-secret config values injected into the data island. `cred://` here requires `allow_credential_values`.",
          "type": "object"
        },
        "row_actions": {
          "description": "Per-row actions, each re-entering the gateway via `tools/call`.",
          "items": {
            "$ref": "#/definitions/AppRowAction"
          },
          "type": "array"
        },
        "rows_path": {
          "description": "JSON-path to the row array inside the tool's structuredContent. Default introspected from the output schema.",
          "type": [
            "string",
            "null"
          ]
        },
        "theme": {
          "anyOf": [
            {
              "$ref": "#/definitions/AppTheme"
            },
            {
              "type": "null"
            }
          ]
        },
        "title": {
          "description": "Human title shown in the resource descriptor.",
          "type": "string"
        },
        "ui_schema": {
          "anyOf": [
            {
              "$ref": "#/definitions/UiSchema"
            },
            {
              "type": "null"
            }
          ],
          "description": "Widget/layout overlay (highest precedence over columns/fields)."
        }
      },
      "required": [
        "id",
        "kind",
        "title"
      ],
      "type": "object"
    },
    "GatewayAppKind": {
      "description": "The shipped shells a templated app can select.",
      "oneOf": [
        {
          "enum": [
            "table",
            "list",
            "detail",
            "form",
            "confirmation",
            "selection",
            "map"
          ],
          "type": "string"
        },
        {
          "description": "Dense label/value spec sheet (a compact `detail`).",
          "enum": [
            "key_value"
          ],
          "type": "string"
        },
        {
          "description": "Read-only syntax-highlight-free code/text viewer.",
          "enum": [
            "code_viewer"
          ],
          "type": "string"
        },
        {
          "description": "Numeric series chart (bar/line) over the result columns.",
          "enum": [
            "chart"
          ],
          "type": "string"
        },
        {
          "description": "Canvas to draw a signature/sketch; submits a PNG data URL.",
          "enum": [
            "signature_pad"
          ],
          "type": "string"
        },
        {
          "description": "Record audio (microphone) and submit the clip.",
          "enum": [
            "audio_recorder"
          ],
          "type": "string"
        },
        {
          "description": "Capture a still photo (camera) and submit it.",
          "enum": [
            "camera_capture"
          ],
          "type": "string"
        },
        {
          "description": "Play an audio/video asset referenced by the result.",
          "enum": [
            "media_player"
          ],
          "type": "string"
        },
        {
          "description": "Browse a set of images from the result.",
          "enum": [
            "image_gallery"
          ],
          "type": "string"
        },
        {
          "description": "Select/drag files and submit their contents.",
          "enum": [
            "file_upload"
          ],
          "type": "string"
        }
      ]
    },
    "GatewayConfig": {
      "additionalProperties": false,
      "properties": {
        "admin": {
          "allOf": [
            {
              "$ref": "#/definitions/AdminConfig"
            }
          ],
          "default": {
            "auth": {
              "type": "disabled"
            },
            "base_path": "/admin/v1",
            "bind_address": "127.0.0.1:9090",
            "disclosure": "summary",
            "enabled": false
          },
          "description": "Admin HTTP surface — `/admin/*` routes, mutual-TLS or bearer-token auth, the operator-facing `disclosure_level` gate that controls how much detail diagnostic endpoints expose. Defaults to disabled; production deploys mount it behind an internal-only listener."
        },
        "config_overlay": {
          "description": "Ordered list of `config_provider` URIs to snapshot at gateway boot + deep-merge into an overlay value (spec §9.16). Lives here (rather than per-plugin) because it's gateway-process bootstrap config. Each URI must use a scheme bound by a registered config-provider plugin.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "config_watch": {
          "allOf": [
            {
              "$ref": "#/definitions/ConfigWatchConfig"
            }
          ],
          "default": {
            "enabled": false,
            "poll_interval_ms": 0
          },
          "description": "File-watch config-reload trigger (third trigger, alongside SIGHUP and `POST /admin/v1/config:reload`). Background task polls the `MCPG_CONFIG` source set on disk and triggers a hot-reload when contents change. Default disabled. See [`ConfigWatchConfig`] for tuning. Useful for bare-metal systemd deployments and K8s deployments without the MCPG operator (which already does cluster-level config propagation via `mcpg.dev/config-hash` annotation forcing rolling restart)."
        },
        "control_plane": {
          "anyOf": [
            {
              "$ref": "#/definitions/ControlPlaneAttachConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional Control Plane attachment. When set AND the `cp-attached` Cargo feature is built in, the gateway registers with the CP at boot, opens an agent Channel, and ships per-tool-call samples for centralized observability. When the feature isn't built in, this block is silently ignored."
        },
        "inspector": {
          "allOf": [
            {
              "$ref": "#/definitions/InspectorSidecarConfig"
            }
          ],
          "default": {
            "enabled": false
          },
          "description": "Supervised inspector sidecar (`mcpg --inspector`, or `enabled: true` here): the gateway spawns a sibling `mcpg-inspector serve` pre-wired against this gateway with a per-boot loopback credential."
        },
        "plugin_registry": {
          "allOf": [
            {
              "$ref": "#/definitions/PluginRegistryConfig"
            }
          ],
          "default": {
            "auth": {
              "docker_config_path": null,
              "password": null,
              "username": null
            },
            "cache_dir": null,
            "default_registry": "ghcr.io/mcpg-dev/source-code/plugins",
            "default_signature_policy": "enforce",
            "insecure_registries": [],
            "mirrors": [],
            "require_integrity_anchor": false,
            "revocation_list_path": null,
            "tls": {
              "ca_cert": null,
              "insecure": false
            }
          },
          "description": "OCI plugin-registry configuration. Lives here (rather than per-plugin) because it's gateway-process tuning — where to fetch plugin artifacts from — not per-plugin config. Per-plugin source auth/tls live inline in each plugin entry's `source.{auth,tls}:`. Only consulted when at least one plugin entry uses `source: { oci: ... }`."
        },
        "server": {
          "allOf": [
            {
              "$ref": "#/definitions/ServerConfig"
            }
          ],
          "default": {
            "access_log": true,
            "allow_private_backends": false,
            "allowed_origins": [],
            "anonymous_rate_limit_burst": 100,
            "anonymous_rate_limit_per_min": 600,
            "bind_address": "127.0.0.1:8787",
            "completion_rate_limit_per_sec": null,
            "enforce_modern_request_meta": false,
            "extra_resource_uri_schemes": [],
            "health_check": {
              "degraded_latency_threshold_ms": 1000,
              "enabled": true,
              "interval_ms": 30,
              "timeout_ms": 2000,
              "unhealthy_threshold": 3
            },
            "health_path": "/health",
            "max_request_body_mb": 4,
            "max_sessions_per_tenant": 0,
            "mcp_path": "/mcp",
            "relax_request_id_uniqueness": false,
            "replay_window_limit": 16,
            "request_timeout_ms": 30000,
            "revalidate_mutated_tool_arguments": false,
            "scrub_process_env_after_boot": false,
            "server_ping_interval_ms": null,
            "session_idle_timeout_ms": 900000,
            "shutdown_timeout_ms": 30000,
            "tls": null,
            "transport": "http",
            "trust_proxy_ip": false,
            "trust_subject_header": false,
            "unary_json_fast_path": false
          },
          "description": "Listener configuration — bind address, transport mode (HTTP / stdio / SSE), TLS, allowed origins, and per-request timeouts. The block is mandatory in practice (the listener won't bind without `bind_address`) but defaults to a localhost dev-mode shape so out-of-the-box `mcpg` boots without any config."
        }
      },
      "type": "object"
    },
    "GovernanceConfig": {
      "additionalProperties": false,
      "description": "Tool-call governance lifecycle: who → allowed? → extra gate → recorded → within limits.\n\nEvery child defaults to its own zero-value: an empty `governance:` block is valid YAML and produces a fully-default configuration (anonymous identity, untrusted-by-default policy, no human-gate signing key, audit channel disabled, no quotas declared).",
      "properties": {
        "access": {
          "allOf": [
            {
              "$ref": "#/definitions/AccessConfig"
            }
          ],
          "default": {
            "authorization_server": null,
            "jwks": null,
            "oidc_oauth": null,
            "resource_metadata": null
          },
          "description": "Inbound identity establishment — JWKS-backed JWT verification or OIDC discovery + introspection. When unset the gateway accepts unauthenticated callers and stamps every request with `identity_kind: anonymous` so the policy gate can deny them. `jwks` and `oidc_oauth` are mutually exclusive."
        },
        "approvals": {
          "allOf": [
            {
              "$ref": "#/definitions/ApprovalsConfig"
            }
          ],
          "default": {
            "callback_base_url": null,
            "callback_grace_ms": 0,
            "signing_key_env": null
          },
          "description": "Tool-gate human approval — signing key + callback base url + grace window. When unset, the runtime defaults to a random per-process signing key + empty callback base url (suitable for tests + dev only — production deploys must supply a stable signing key)."
        },
        "audit": {
          "allOf": [
            {
              "$ref": "#/definitions/AuditConfig"
            }
          ],
          "default": {
            "emit_tool_call_allowed": true,
            "emit_tool_call_completed": true,
            "enabled": true,
            "on_failure": "fail_closed",
            "required": true,
            "sinks": [
              {
                "config": {},
                "kind": "dev.mcpg.builtin.audit.local-file"
              }
            ]
          },
          "description": "Compliance-grade event sink fan-out (spec §9.12). Lives under `governance:` (rather than `observability:`) so the audit-as-evidence-of-governance story reads alongside access / policy / approvals."
        },
        "child_invoke": {
          "allOf": [
            {
              "$ref": "#/definitions/ChildInvokeConfig"
            }
          ],
          "default": {
            "enforce_gates": false
          },
          "description": "Authorization for agentic child tool calls — the backend-to-backend `invoke_tool` path an LLM Generator drives when it emits `tool_calls`. Off by default."
        },
        "policy": {
          "allOf": [
            {
              "$ref": "#/definitions/PolicyConfig"
            }
          ],
          "default": {
            "cache": {
              "enabled": false,
              "max_entries": 10000,
              "ttl_ms": 60000
            },
            "tool_access": {
              "cel_allow_if": null,
              "default_minimum_trust": "header_asserted",
              "rules": []
            }
          },
          "description": "Tool-access policy — default minimum trust level + per-tool override rules. Operators can also point at a Cedar / Casbin / OPA bundle plugin under `plugins[]` to delegate the actual decision; this block stays useful for the gateway-internal default-trust gate every tool flows through before any plugin policy fires."
        },
        "quotas": {
          "allOf": [
            {
              "$ref": "#/definitions/QuotasConfig"
            }
          ],
          "default": {
            "on_error": "",
            "store": {
              "kind": ""
            }
          },
          "description": "Registry of named rate-limit / budget / concurrency policies. Bindings opt into specific policies by id via their per-binding `quotas:` block. Storage backend is a `kind:` slot under `governance.quotas.store:`.\n\nEmpty by default — operators opt in when they need limit enforcement. The runtime quota gate short-circuits when no policies are declared."
        }
      },
      "type": "object"
    },
    "HealthCheckConfig": {
      "additionalProperties": false,
      "properties": {
        "degraded_latency_threshold_ms": {
          "default": 1000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "interval_ms": {
          "default": 30,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "timeout_ms": {
          "default": 2000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "unhealthy_threshold": {
          "default": 3,
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "HealthProbeConfig": {
      "additionalProperties": false,
      "description": "Periodic health-probe configuration. The probe is the only writer of `PluginState::Degraded`; without it plugins stay perpetually `Active` regardless of whether they're actually responding.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Probe plugins periodically. Default: `true`. Set `false` to turn off the prober entirely (the `Degraded` state then never flips — test-only or historical deployments).",
          "type": "boolean"
        },
        "failure_threshold": {
          "default": 3,
          "description": "Consecutive failures before flipping `Active` → `Degraded`. Default: 3.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "interval_ms": {
          "default": 30000,
          "description": "Milliseconds between probe cycles. Default: 30000 (30s).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "probe_timeout_ms": {
          "default": 5000,
          "description": "Per-probe deadline in milliseconds. A plugin whose FFI call exceeds this is counted as a failure. Default: 5000 (5s).",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "IdempotencyConfig": {
      "additionalProperties": false,
      "description": "`idempotency:` config — opt-in dedupe for `tools/call` and `tasks/create`. When `enabled: false` (the default), the gateway omits the `dev.mcpg/idempotency` extension from its `initialize` capability advertisement and silently ignores any `_meta[\"dev.mcpg/idempotency-key\"]` the caller sets.",
      "properties": {
        "conflict_policy": {
          "allOf": [
            {
              "$ref": "#/definitions/ConflictPolicy"
            }
          ],
          "default": "reject",
          "description": "Conflict policy — only `reject` for v1."
        },
        "default_ttl_ms": {
          "default": 86400000,
          "description": "Default TTL applied to any reservation. Default 86_400_000 (24 hours) — matches Stripe's window.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Master switch. Default `false` (opt-in, like `governance.quotas` was before it became cargo-feature-gated).",
          "type": "boolean"
        },
        "max_ttl_ms": {
          "default": 604800000,
          "description": "Hard upper bound on per-record TTL. Default 604_800_000 (7 days). Future per-binding `idempotency.ttl_ms` overrides saturate at this cap.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "replay_revalidation": {
          "default": false,
          "description": "When true, a completed-replay hit re-runs the full pre-dispatch authz stack (external policy chain + tool_gate plugins) before serving the cached envelope, so authorization revoked since the original call is honored within the record TTL. Default false: only the built-in trust-floor + CEL allow_if is re-checked on replay (the cheap, side-effect-free layer).",
          "type": "boolean"
        },
        "scope": {
          "allOf": [
            {
              "$ref": "#/definitions/IdempotencyScopeKind"
            }
          ],
          "default": "per_identity",
          "description": "Scope strategy — `per_identity` (default), `per_session`, or `per_tenant`."
        },
        "store": {
          "anyOf": [
            {
              "$ref": "#/definitions/StoreOverrideConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-capability `store:` override. Same shape as `tasks.store` / `sessions.store`. When unset, the idempotency KV inherits from the cluster coordinator's `key_value_store()` primitive."
        },
        "supported_methods": {
          "default": [
            "tools/call",
            "tasks/create"
          ],
          "description": "JSON-RPC methods this extension applies to. Default `[\"tools/call\", \"tasks/create\"]` — read-only methods (`resources/read`, `prompts/get`, `completion/complete`) are intentionally excluded as they're idempotent by nature.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "IdempotencyScopeKind": {
      "description": "Default scope strategy for idempotency records. Operator can widen to `per_tenant` (service-account dedupe) or narrow to `per_session` (ephemeral test harnesses).\n\nNote: `global` is intentionally NOT a variant — cross-tenant replay is a known anti-pattern, so we don't expose the footgun.",
      "oneOf": [
        {
          "description": "All requests sharing one MCP session share the namespace. Useful for ephemeral test harnesses; resets on every re-initialize.",
          "enum": [
            "per_session"
          ],
          "type": "string"
        },
        {
          "description": "All requests sharing one resolved identity (OIDC subject + auth provider) share the namespace. The default — matches Stripe / Square idempotency semantics.",
          "enum": [
            "per_identity"
          ],
          "type": "string"
        },
        {
          "description": "All requests sharing one tenant id share the namespace. Useful for service-to-service workloads where multiple service accounts retry the same operation.",
          "enum": [
            "per_tenant"
          ],
          "type": "string"
        }
      ]
    },
    "ImportConfig": {
      "additionalProperties": false,
      "description": "Which capability surfaces to import.",
      "properties": {
        "prompts": {
          "default": false,
          "type": "boolean"
        },
        "resource_templates": {
          "default": false,
          "type": "boolean"
        },
        "resources": {
          "default": false,
          "type": "boolean"
        },
        "tools": {
          "default": true,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "InspectorSidecarConfig": {
      "additionalProperties": false,
      "properties": {
        "bind": {
          "description": "host:port of the inspector's web UI + API (single origin). Defaults to the inspector's own `127.0.0.1:7846`.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Supervise an `mcpg-inspector` sidecar. `--inspector` flips this on for a single run.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "JwksConfig": {
      "additionalProperties": false,
      "properties": {
        "allow_missing_audience": {
          "default": false,
          "description": "Dev escape-hatch: allow tokens without audience binding. Production MUST set an audience.",
          "type": "boolean"
        },
        "audience": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "header_name": {
          "default": "authorization",
          "type": "string"
        },
        "header_prefix": {
          "default": "Bearer ",
          "type": "string"
        },
        "issuer": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "keys_json": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "default": "",
          "type": "string"
        }
      },
      "type": "object"
    },
    "KindRef": {
      "additionalProperties": false,
      "description": "Discriminator + config payload at every consumer slot. Operators write `{ kind: <value>, ...config }` in YAML; the gateway parses it as this type and resolves via [`resolve_kind`].\n\nThe `config:` field is a free-form JSON object passed to the resolved handle (built-in or plugin). The slot's resolver validates `kind`; the implementation validates `config`.",
      "properties": {
        "config": {
          "description": "Inline config forwarded to the resolved handle. Empty by default."
        },
        "kind": {
          "description": "Discriminator. One of: built-in keyword, full plugin id, short alias, or `cluster`.",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "LicenseConfig": {
      "additionalProperties": false,
      "properties": {
        "non_production_use": {
          "default": false,
          "description": "Declares this deployment non-production. Entitlement-gated plugins then load without a token under their license's free non-production grant (development, testing, evaluation, staging), with a boot warning naming them. Production use still requires an entitling token.",
          "type": "boolean"
        },
        "pubkey_pem": {
          "description": "Trusted license-signing public key (SPKI PEM, Ed25519) — the verification anchor for the configured token (`mcpg-license keygen --public-out`). Required when a token is configured; an unverifiable token refuses boot rather than silently degrading to community.",
          "type": [
            "string",
            "null"
          ]
        },
        "token": {
          "description": "The signed license JWT, inline (commonly `${env.MCPG_LICENSE}`). Exactly one of `token` / `token_file` may be set.",
          "type": [
            "string",
            "null"
          ]
        },
        "token_file": {
          "description": "Path to a file holding the signed license JWT (e.g. a mounted secret). Exactly one of `token` / `token_file` may be set.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "LogsConfig": {
      "additionalProperties": false,
      "description": "`observability.logs:` — the logs signal.\n\nGateway internals (every `tracing::info!()` / `warn!()` / `error!()` call) AND plugin-emitted log events both flow through the configured sink list. Default sinks ship one `stderr` JSON emitter — production deployments add `file`, `otlp`, or plugin sinks (Loki, Splunk, …) by appending entries to `sinks:`.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Master enable for the logs signal. When `false`, no log events are emitted regardless of `sinks:` content.",
          "type": "boolean"
        },
        "level": {
          "default": "info",
          "description": "Signal-level severity floor (`trace` / `debug` / `info` / `warn` / `error`). Per-sink `level:` can raise it further but can't lower it below this floor. Default: `info`.",
          "type": "string"
        },
        "sinks": {
          "default": [
            {
              "config": {
                "format": "json"
              },
              "kind": "stderr"
            }
          ],
          "description": "Sink fan-out. Each entry's `kind:` resolves to a built-in factory (`stderr` / `stdout` / `file` / `otlp`) or plugin id. Default: one `stderr` sink with JSON format.",
          "items": {
            "$ref": "#/definitions/SinkConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "MapAppConfig": {
      "additionalProperties": false,
      "description": "Map binding for `kind == map`.",
      "properties": {
        "geojson_path": {
          "description": "JSON-path to a GeoJSON FeatureCollection (alternative to lat/lng).",
          "type": [
            "string",
            "null"
          ]
        },
        "label_field": {
          "type": [
            "string",
            "null"
          ]
        },
        "lat_field": {
          "type": [
            "string",
            "null"
          ]
        },
        "lng_field": {
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/MapRenderMode"
            }
          ],
          "default": "plot",
          "description": "Render mode. `plot` (default) needs no network; `raster_tiles` fetches from `tile_url` and requires a CSP allowance."
        },
        "popup_field": {
          "type": [
            "string",
            "null"
          ]
        },
        "select_action": {
          "description": "Tool a region-draw selection invokes (geometry passed as args).",
          "type": [
            "string",
            "null"
          ]
        },
        "tile_url": {
          "description": "Raster tile template URL (raster mode only).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MapRenderMode": {
      "description": "Map rendering mode.",
      "oneOf": [
        {
          "description": "Coordinate plot on a canvas — zero network, no CSP delta.",
          "enum": [
            "plot"
          ],
          "type": "string"
        },
        {
          "description": "Raster basemap tiles — needs `tile_url` + a CSP allowance.",
          "enum": [
            "raster_tiles"
          ],
          "type": "string"
        }
      ]
    },
    "McpCapabilitiesConfig": {
      "additionalProperties": false,
      "description": "`mcp.capabilities:` — the MCP protocol-advertised surface.\n\nMatches the protocol's `initialize` handshake's `capabilities` vocabulary: tools / prompts / resources / resource_templates, plus the gateway-side feature configs that govern protocol behaviour (tasks, elicitation, sampling, roots).",
      "properties": {
        "elicitation": {
          "allOf": [
            {
              "$ref": "#/definitions/McpElicitationConfig"
            }
          ],
          "default": {
            "timeout_ms": 60000
          },
          "description": "MCP elicitation — server-initiated prompt requests the gateway emits during pipeline execution."
        },
        "prompts": {
          "default": [],
          "description": "Operator-declared prompts — the bindings that surface via `prompts/list` and `prompts/get`. Carry `prompt_arguments`.",
          "items": {
            "$ref": "#/definitions/BackendConfig"
          },
          "type": "array"
        },
        "resource_templates": {
          "default": [],
          "description": "Operator-declared resource templates — the bindings that surface via `resources/templates/list` and match `resources/read` URIs by template. Carry `uri_template` instead of `uri`.",
          "items": {
            "$ref": "#/definitions/BackendConfig"
          },
          "type": "array"
        },
        "resources": {
          "default": [],
          "description": "Operator-declared resources — the bindings that surface via `resources/list` and `resources/read`. Carry `uri`, `mime_type`, `mcp_app_url`, watch config.",
          "items": {
            "$ref": "#/definitions/BackendConfig"
          },
          "type": "array"
        },
        "roots": {
          "allOf": [
            {
              "$ref": "#/definitions/McpRootsConfig"
            }
          ],
          "default": {
            "timeout_ms": 30000
          },
          "description": "MCP roots-list — gateway requests roots from the client (e.g. for resource scoping)."
        },
        "sampling": {
          "allOf": [
            {
              "$ref": "#/definitions/McpSamplingConfig"
            }
          ],
          "default": {
            "timeout_ms": 60000
          },
          "description": "MCP sampling — server-initiated LLM completion requests forwarded back to the client."
        },
        "tasks": {
          "allOf": [
            {
              "$ref": "#/definitions/TasksConfig"
            }
          ],
          "default": {
            "default_ttl_ms": 1800000,
            "max_tasks_per_session": 256,
            "reaper_interval_ms": 60000,
            "result_wait_ms": 30000
          },
          "description": "MCP Tasks — task-augmented tool-call semantics. Carries the task store override, TTL/reaper tuning, and the task-supported tools list."
        },
        "tools": {
          "default": [],
          "description": "Operator-declared tools — the bindings that surface via `tools/list` and `tools/call`. Each entry has its own implementation `backend:` (HTTP / SQL / NATS / LLM / pipeline / …) plus tool-specific MCP fields (`annotations`, `task_support`).",
          "items": {
            "$ref": "#/definitions/BackendConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "McpConfig": {
      "additionalProperties": false,
      "description": "Top-level `mcp:` block — the MCP protocol surface.\n\nTwo children that mirror MCP's own vocabulary: - `capabilities:` — what the server advertises in the `initialize` handshake (tools, prompts, resources, resource_templates, tasks, elicitation, sampling, roots). - `configurations:` — runtime-emergent state handling (sessions, pipelines, subscriptions, delivery, cancellation). Operator only tunes persistence + constraints; the items themselves are created by clients at runtime.",
      "properties": {
        "capabilities": {
          "allOf": [
            {
              "$ref": "#/definitions/McpCapabilitiesConfig"
            }
          ],
          "default": {
            "elicitation": {
              "timeout_ms": 60000
            },
            "prompts": [],
            "resource_templates": [],
            "resources": [],
            "roots": {
              "timeout_ms": 30000
            },
            "sampling": {
              "timeout_ms": 60000
            },
            "tasks": {
              "default_ttl_ms": 1800000,
              "max_tasks_per_session": 256,
              "reaper_interval_ms": 60000,
              "result_wait_ms": 30000
            },
            "tools": []
          },
          "description": "What this MCP server advertises in the `initialize` handshake. Mirrors the protocol spec's `capabilities` vocabulary one-to-one."
        },
        "configurations": {
          "allOf": [
            {
              "$ref": "#/definitions/McpConfigurationsConfig"
            }
          ],
          "default": {
            "apps": {
              "allowed_permissions": [
                "camera",
                "microphone",
                "geolocation",
                "clipboard_write"
              ],
              "csp_policy": {
                "base_uri_domains": [
                  "self"
                ],
                "connect_domains": [
                  "*"
                ],
                "frame_domains": [
                  "self"
                ],
                "redirect_domains": [
                  "*"
                ],
                "resource_domains": [
                  "*"
                ]
              },
              "enabled": false,
              "strict": false
            },
            "cancellation": {
              "partition_by_principal": false
            },
            "delivery": {},
            "idempotency": {
              "conflict_policy": "reject",
              "default_ttl_ms": 86400000,
              "enabled": false,
              "max_ttl_ms": 604800000,
              "replay_revalidation": false,
              "scope": "per_identity",
              "supported_methods": [
                "tools/call",
                "tasks/create"
              ]
            },
            "pipelines": {},
            "request_state": {
              "strict_encryption": false
            },
            "sessions": {
              "bind_session_owner": false,
              "optional": false
            },
            "subscriptions": {
              "max_per_session": 100
            }
          },
          "description": "Runtime-emergent state handling. Operator only tunes persistence + constraints; the items themselves are created by clients at runtime."
        },
        "federations": {
          "default": [],
          "description": "Upstream MCP servers federated through this gateway. Each entry is a *capability source* (1:N): MCPG connects to the upstream, imports its capabilities, and re-serves them under a prefix. Default empty — existing configs are unaffected.",
          "items": {
            "$ref": "#/definitions/FederationConfig"
          },
          "type": "array"
        },
        "registries": {
          "default": [],
          "description": "MCP registries whose listed servers MCPG auto-federates: a background syncer crawls each registry's `/v0.1` API and materializes one federation per usable server, kept in sync as the registry changes. Default empty.",
          "items": {
            "$ref": "#/definitions/McpRegistryConfig"
          },
          "type": "array"
        },
        "registry": {
          "allOf": [
            {
              "$ref": "#/definitions/ServedRegistryConfig"
            }
          ],
          "default": {
            "enabled": false,
            "name": ""
          },
          "description": "The registry MCPG *serves* (contrast `registries`, the registries MCPG *consumes*): a v0.1 MCP-Registry view of this gateway — one entry describing the governed catalog — so registry-driven clients (e.g. Copilot's allowed-registry policy) can discover MCPG as their approved server. Off by default."
        }
      },
      "type": "object"
    },
    "McpConfigurationsConfig": {
      "additionalProperties": false,
      "description": "`mcp.configurations:` — runtime-emergent state handling.",
      "properties": {
        "apps": {
          "allOf": [
            {
              "$ref": "#/definitions/AppsConfig"
            }
          ],
          "default": {
            "allowed_permissions": [
              "camera",
              "microphone",
              "geolocation",
              "clipboard_write"
            ],
            "csp_policy": {
              "base_uri_domains": [
                "self"
              ],
              "connect_domains": [
                "*"
              ],
              "frame_domains": [
                "self"
              ],
              "redirect_domains": [
                "*"
              ],
              "resource_domains": [
                "*"
              ]
            },
            "enabled": false,
            "strict": false
          },
          "description": "`io.modelcontextprotocol/ui` (SEP-1865 MCP Apps) extension config. Off by default; `enabled: true` lights up the capability advertisement (downstream + upstream) and the tighten-only CSP/permission egress policy."
        },
        "cancellation": {
          "allOf": [
            {
              "$ref": "#/definitions/CancellationConfig"
            }
          ],
          "default": {
            "partition_by_principal": false
          }
        },
        "delivery": {
          "allOf": [
            {
              "$ref": "#/definitions/DeliveryConfig"
            }
          ],
          "default": {}
        },
        "idempotency": {
          "allOf": [
            {
              "$ref": "#/definitions/IdempotencyConfig"
            }
          ],
          "default": {
            "conflict_policy": "reject",
            "default_ttl_ms": 86400000,
            "enabled": false,
            "max_ttl_ms": 604800000,
            "replay_revalidation": false,
            "scope": "per_identity",
            "supported_methods": [
              "tools/call",
              "tasks/create"
            ]
          },
          "description": "`dev.mcpg/idempotency` extension config. Off by default; flipping `enabled: true` lights up the SEP-2133 capability advertisement and the dispatcher dedupe path."
        },
        "pipelines": {
          "allOf": [
            {
              "$ref": "#/definitions/PipelinesConfig"
            }
          ],
          "default": {}
        },
        "request_state": {
          "allOf": [
            {
              "$ref": "#/definitions/RequestStateConfig"
            }
          ],
          "default": {
            "strict_encryption": false
          },
          "description": "MRTR `requestState` codec configuration (2026-07-28 modern wire). Inert when no modern client connects; absent encryption_key the codec uses an ephemeral key at boot."
        },
        "sessions": {
          "allOf": [
            {
              "$ref": "#/definitions/SessionsConfig"
            }
          ],
          "default": {
            "bind_session_owner": false,
            "optional": false
          }
        },
        "subscriptions": {
          "allOf": [
            {
              "$ref": "#/definitions/SubscriptionsConfig"
            }
          ],
          "default": {
            "max_per_session": 100
          }
        }
      },
      "type": "object"
    },
    "McpElicitationConfig": {
      "additionalProperties": false,
      "description": "`mcp.elicitation:` — gateway behavior for server-initiated elicitation prompts. Today carries only `timeout_ms`; future per-elicitation-type config can grow here.",
      "properties": {
        "timeout_ms": {
          "default": 60000,
          "description": "Maximum time the gateway waits for the client's response to an elicitation prompt before giving up. Default 60 000.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "McpRegistryConfig": {
      "additionalProperties": false,
      "description": "One MCP registry to auto-federate.",
      "properties": {
        "auth": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistryAuthConfig"
            }
          ],
          "default": {
            "mode": "none"
          },
          "description": "How MCPG authenticates to the registry (consumer side)."
        },
        "defaults": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistryDefaultsConfig"
            }
          ],
          "default": {
            "auth": {
              "mode": "none"
            },
            "cache": {
              "capability_ttl_secs": 300
            },
            "governance": {
              "allow_if": null,
              "minimum_trust": "header_asserted"
            },
            "import": {
              "prompts": true,
              "resource_templates": true,
              "resources": true,
              "tools": true
            },
            "oauth_discovery": {
              "enabled": false
            },
            "synthesize": {
              "poll_interval_ms": 30000,
              "resources_updated": "auto"
            },
            "upstream_safety": {
              "allow_private_backends": false
            }
          },
          "description": "Applied to every synthesized federation."
        },
        "filter": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistryFilterConfig"
            }
          ],
          "default": {
            "exclude": [],
            "include": [
              "*"
            ],
            "namespaces": []
          },
          "description": "Which registry servers are eligible for federation."
        },
        "name": {
          "description": "Registry id. Prefixes every synthesized federation name (`<registry>--<server>`), so it must be stable and unique. Lowercase alphanumeric + `-`.",
          "type": "string"
        },
        "on_deprecated": {
          "allOf": [
            {
              "$ref": "#/definitions/OnDeprecated"
            }
          ],
          "default": "serve_and_warn",
          "description": "What happens to servers the registry marks `deprecated`. (`deleted` servers are always removed.)"
        },
        "registry_safety": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistrySafetyConfig"
            }
          ],
          "default": {
            "allow_insecure_http": false,
            "allow_private_registry": false
          },
          "description": "Network posture for the REGISTRY endpoint itself. Distinct from the per-server `defaults.upstream_safety`: a private registry URL is normal for enterprises, but stays an explicit opt-in."
        },
        "servers": {
          "additionalProperties": {
            "$ref": "#/definitions/RegistryServerOverride"
          },
          "default": {},
          "description": "Per-server overrides, keyed by the server's registry name (e.g. `com.acme/crm`).",
          "type": "object"
        },
        "sync": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistrySyncConfig"
            }
          ],
          "default": {
            "full_resync_hours": 24,
            "incremental": false,
            "interval_secs": 300,
            "max_servers": 100
          },
          "description": "Sync cadence + size bounds."
        },
        "url": {
          "description": "Registry base URL; the syncer appends the standard API paths (`/v0.1/servers`, …).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "url"
      ],
      "type": "object"
    },
    "McpRootsConfig": {
      "additionalProperties": false,
      "description": "`mcp.roots:` — gateway behavior for server-initiated roots-list requests forwarded to the client.",
      "properties": {
        "timeout_ms": {
          "default": 30000,
          "description": "Maximum time the gateway waits for the client's roots-list response. Default 30 000.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "McpSamplingConfig": {
      "additionalProperties": false,
      "description": "`mcp.sampling:` — gateway behavior for server-initiated sampling (LLM completion) requests forwarded to the client.",
      "properties": {
        "timeout_ms": {
          "default": 60000,
          "description": "Maximum time the gateway waits for the client's sampling response. Default 60 000.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "MetricsConfig": {
      "additionalProperties": false,
      "description": "`observability.metrics:` — the metrics signal.\n\nGateway internals (every `metrics::counter!()` / `gauge!()` / `histogram!()`) flow through the configured sink list. The canonical Prometheus exporter is a plugin: operators wire `kind: dev.mcpg.observability.prometheus`. The `sinks: []` list otherwise carries plugin ids (`dev.acme.observability.datadog`, etc.) — there are no built-in factory kinds for metrics.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Master enable for the metrics signal. When `false`, no metric recorders are installed.",
          "type": "boolean"
        },
        "sinks": {
          "default": [
            {
              "config": {},
              "kind": "dev.mcpg.observability.prometheus"
            }
          ],
          "description": "Sink fan-out. Every entry's `kind:` is a plugin id (there is no `kind: prometheus` / `kind: otlp` shorthand). Default: one `dev.mcpg.observability.prometheus` sink at `/metrics`.",
          "items": {
            "$ref": "#/definitions/SinkConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "NamingConfig": {
      "additionalProperties": false,
      "description": "Prefixes applied to imported capability names / URIs.",
      "properties": {
        "prompt_prefix": {
          "description": "Prepended to every imported prompt name.",
          "type": [
            "string",
            "null"
          ]
        },
        "resource_uri_prefix": {
          "description": "Prepended to every imported resource URI (e.g. `\"mcp://notion/\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "tool_prefix": {
          "description": "Prepended to every imported tool name (e.g. `\"notion.\"`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "NotificationFilterConfig": {
      "description": "Scoping filter for resource change notifications. Determines which subscribers receive `notifications/resources/updated` when the watch engine detects a change.",
      "oneOf": [
        {
          "description": "Fan-out to all subscribers (default, no filter).",
          "properties": {
            "scope": {
              "enum": [
                "all"
              ],
              "type": "string"
            }
          },
          "required": [
            "scope"
          ],
          "type": "object"
        },
        {
          "description": "Only notify subscribers whose `principal_id` matches the event's user context.",
          "properties": {
            "scope": {
              "enum": [
                "subject_id"
              ],
              "type": "string"
            }
          },
          "required": [
            "scope"
          ],
          "type": "object"
        },
        {
          "description": "Only notify the originating session.",
          "properties": {
            "scope": {
              "enum": [
                "session_id"
              ],
              "type": "string"
            }
          },
          "required": [
            "scope"
          ],
          "type": "object"
        },
        {
          "description": "CEL expression evaluated per subscriber. Variables: `subscriber.principal_id`, `subscriber.trust_level`, `subscriber.roles`, `subscriber.groups`, `subscriber.scopes`, `subscriber.attributes`, `event.uri`.",
          "properties": {
            "expression": {
              "type": "string"
            },
            "scope": {
              "enum": [
                "expression"
              ],
              "type": "string"
            }
          },
          "required": [
            "expression",
            "scope"
          ],
          "type": "object"
        }
      ]
    },
    "OAuthResourceMetadataConfig": {
      "additionalProperties": false,
      "description": "Configuration for the OAuth Protected Resource Metadata endpoint (RFC 9728).",
      "properties": {
        "allow_loopback_resource": {
          "default": false,
          "description": "Local-development escape hatch: permit a loopback `resource` (`localhost` / `127.0.0.1` / `[::1]`). A wildcard host (`0.0.0.0` / `[::]`) is NEVER a valid resource identifier and is refused even with this set. Production deployments leave this `false` and configure the canonical public URL.",
          "type": "boolean"
        },
        "authorization_servers": {
          "default": [],
          "description": "Authorization server URLs. If empty, derived from OIDC provider issuers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bearer_methods_supported": {
          "default": [
            "header"
          ],
          "description": "Bearer token presentation methods. Defaults to `[\"header\"]`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource": {
          "description": "The protected resource's canonical resource identifier (RFC 8707 `resource` / RFC 9728 `resource`). MUST be the real external, absolute URL clients reach the gateway at — the same value the authorization server binds tokens to as `aud`. A wildcard (`0.0.0.0`), bare loopback (`localhost`/`127.0.0.1`/`[::1]`), or derived `bind_address` value is refused at boot: it would publish a `resource` that does not match the audience the tokens carry, so audience-bound validation silently fails. Set the canonical public URL explicitly, or opt into the loopback form for local development with `allow_loopback_resource: true`.",
          "type": "string"
        },
        "scopes_supported": {
          "default": [],
          "description": "Scopes supported by this resource.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "resource"
      ],
      "type": "object"
    },
    "ObservabilityConfig": {
      "additionalProperties": false,
      "description": "Top-level `observability:` block — the OpenTelemetry signal triad (logs / metrics / traces) plus audit fan-out.\n\nEach signal carries a `sinks: [...]` list of [`SinkConfig`] entries. Each entry's `kind:` field dispatches to either a built-in sink factory (`stderr` / `stdout` / `file` / `otlp` / `prometheus`) or a plugin id resolved against the gateway's plugin registry at boot.\n\n**Master switch.** `enabled: false` (default `true`) silences every child regardless of their own `enabled:` flags — useful for embedded use cases where the host process owns observability or for minimal-footprint test runs. Each child also has its own `enabled:` for finer-grained control. The accessor helpers below (`is_logs_on()`, `is_metrics_on()`, `is_traces_on()`, `is_audit_on()`) implement the AND-fold so call sites can't forget either flag.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Master kill switch. When `false`, every child is treated as disabled regardless of its own `enabled:` field — no logs emitted, no metrics endpoint registered, no traces pipeline started, no audit fan-out wired. Default `true`.",
          "type": "boolean"
        },
        "logs": {
          "allOf": [
            {
              "$ref": "#/definitions/LogsConfig"
            }
          ],
          "default": {
            "enabled": true,
            "level": "info",
            "sinks": [
              {
                "config": {
                  "format": "json"
                },
                "kind": "stderr"
              }
            ]
          },
          "description": "Logs signal — gateway internals + plugin-emitted log events fanned to the configured sink list."
        },
        "metrics": {
          "allOf": [
            {
              "$ref": "#/definitions/MetricsConfig"
            }
          ],
          "default": {
            "enabled": true,
            "sinks": [
              {
                "config": {},
                "kind": "dev.mcpg.observability.prometheus"
              }
            ]
          },
          "description": "Metrics signal — gateway internals + plugin-emitted metric events fanned to the configured sink list."
        },
        "plugin_call_sampling_rate": {
          "description": "Per-call span sampling rate for native-plugin host-side spans. Range `[0.0, 1.0]`; `None` inherits the global subscriber sampler (no extra dampening).\n\nThe host wraps every plugin FFI call in a `tracing` span for attribution. On hot paths (tool-gate chain → 5–15 spans/call; metrics_sink emit → up to 50 spans/call) the per-span construction + drop overhead is ~5–20 µs each. Operators running plugin-heavy workloads with traces sampled at a low rate end-to-end can additionally dampen the plugin-call spans here without changing the global subscriber, dropping the host-side overhead to a small fraction.\n\n`Some(1.0)` is a no-op (all plugin call spans honour the global sampler). `Some(0.01)` keeps 1% of spans; the rest become disabled spans, which `tracing` short-circuits at near-zero cost.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "plugin_health_probe": {
          "allOf": [
            {
              "$ref": "#/definitions/HealthProbeConfig"
            }
          ],
          "default": {
            "enabled": true,
            "failure_threshold": 3,
            "interval_ms": 30000,
            "probe_timeout_ms": 5000
          },
          "description": "Plugin health probe. Lives under `observability:` because the probe is observability-shaped (it watches plugin liveness and writes `PluginState::Degraded` for monitoring consumers)."
        },
        "traces": {
          "allOf": [
            {
              "$ref": "#/definitions/TracesConfig"
            }
          ],
          "default": {
            "enabled": false,
            "propagate_context": true,
            "service_name": "mcpg",
            "sinks": []
          },
          "description": "Traces signal — span lifecycle events fanned to the configured sink list."
        }
      },
      "type": "object"
    },
    "OidcOAuthConfig": {
      "additionalProperties": false,
      "properties": {
        "providers": {
          "description": "One or more identity providers. At least one is required.",
          "items": {
            "$ref": "#/definitions/OidcProviderConfig"
          },
          "type": "array"
        },
        "token_source": {
          "allOf": [
            {
              "$ref": "#/definitions/TokenSourceConfig"
            }
          ],
          "default": {
            "header_name": null,
            "header_prefix": null,
            "kind": "authorization_bearer"
          },
          "description": "How to extract the bearer token from the request."
        }
      },
      "required": [
        "providers"
      ],
      "type": "object"
    },
    "OidcProviderConfig": {
      "additionalProperties": false,
      "properties": {
        "allow_any_audience": {
          "default": false,
          "description": "Explicit opt-in to SKIP audience (`aud`) validation. When false (the default) an empty `audiences` is a hard config error at boot — otherwise a mistyped `audiences` key would silently disable audience binding and accept tokens minted for any gateway. Production MUST leave this false and set `audiences`; only the rare provider that genuinely issues no `aud` claim should opt in.",
          "type": "boolean"
        },
        "allow_private_issuer": {
          "default": false,
          "description": "Dev escape hatch: permit private/loopback ranges in OIDC URLs. Production MUST leave this false.",
          "type": "boolean"
        },
        "allowed_issuer_hosts": {
          "default": [],
          "description": "Optional hostname allowlist for OIDC discovery and JWKS fetches. Empty means only the private-range blocklist applies.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "audiences": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "claim_mappings": {
          "allOf": [
            {
              "$ref": "#/definitions/ClaimMappingConfig"
            }
          ],
          "default": {
            "attribute_claim_mappings": {},
            "group_claim_paths": [],
            "role_claim_paths": [],
            "scope_claim_paths": [
              "scope",
              "scp"
            ],
            "subject_claim": "sub"
          }
        },
        "clock_skew_secs": {
          "default": 60,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "discovery_uri": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "issuer": {
          "type": "string"
        },
        "verification": {
          "$ref": "#/definitions/VerificationConfig"
        }
      },
      "required": [
        "issuer",
        "verification"
      ],
      "type": "object"
    },
    "OnDeprecated": {
      "description": "Policy for `deprecated` servers.",
      "oneOf": [
        {
          "description": "Keep federating; log + count the deprecation.",
          "enum": [
            "serve_and_warn"
          ],
          "type": "string"
        },
        {
          "description": "Drop the federation (clients see `list_changed`).",
          "enum": [
            "exclude"
          ],
          "type": "string"
        }
      ]
    },
    "PipelinesConfig": {
      "additionalProperties": false,
      "description": "`pipelines:` config — pipeline state store + per-capability `store:` override. When `store` is unset, the pipeline KV inherits from the cluster's `key_value_store()` primitive.",
      "properties": {
        "store": {
          "anyOf": [
            {
              "$ref": "#/definitions/StoreOverrideConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "PluginEntryConfig": {
      "additionalProperties": false,
      "description": "Configuration for a single plugin entry.",
      "properties": {
        "class": {
          "default": "tool_gate",
          "description": "Plugin class — the snake_case `PluginClass` variant the plugin implements. Must match the `class:` field in the plugin's own `plugin.yaml` manifest. Determines which plugin chain / slot the plugin is registered into. Valid values: `tool_gate`, `transform`, `identity_provider`, `backend`, `watch_strategy`, `http_route`, `audit_sink`, `store`, `cache`, `telemetry_sink`, `log_sink`, `metrics_sink`, `secret_provider`, `config_provider`, `transport`, `policy_engine`, `cluster`, `catalog_provider`, `credential_issuer`, `approval_notifier`, `content_store`.",
          "type": "string"
        },
        "config": {
          "default": null,
          "description": "Plugin-specific configuration passed to the plugin instance."
        },
        "disabled": {
          "default": false,
          "description": "When `true`, the plugin entry is parsed + validated but not loaded at boot. Useful for keeping a plugin's config in source control while temporarily turning it off without removing the entry. Default `false`.",
          "type": "boolean"
        },
        "enforce": {
          "default": true,
          "description": "When false, the plugin runs in shadow mode: evaluate and log, but override Deny/Challenge → Allow. Defaults to true (enforce).",
          "type": "boolean"
        },
        "ffi_limits": {
          "anyOf": [
            {
              "$ref": "#/definitions/PluginFfiLimitsConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-plugin FFI hardening overrides for native cdylib plugins. `None` = inherit the spec defaults (1s lifecycle / 5s control / 30s data / 256 KiB payload). Ignored for Wasm plugins (those use `limits.timeout_ms`)."
        },
        "granted_capabilities": {
          "description": "Per-plugin typed host capability grants. Each entry is one of [`mcpg_plugin_protocol::capability::Capability`]'s known variants. Two equivalent YAML shapes accepted:\n\n```yaml granted_capabilities: - \"network_outbound\"                          # bare string (no-args variants only) - { type: \"audit_write\" }                     # object form, no args - { type: \"filesystem_read\", paths: [\"/etc/myapp/\"] } - { type: \"secrets_read\", schemes: [\"vault\", \"aws-sm\"] } ```\n\nVariant-args kinds (`filesystem_read`, `filesystem_write`, `secrets_read`, `credential_issue`, `config_read`) require the object form — bare strings for them produce a parse error at config load time, citing the plugin id and the missing args field.",
          "items": true,
          "type": "array"
        },
        "http_route": {
          "anyOf": [
            {
              "$ref": "#/definitions/PluginHttpRouteConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "`http_route`-specific operator tuning. Ignored for non-`http_route` plugins. Absent = all defaults (enabled, namespaced mount, spec's own body cap + identity policy)."
        },
        "id": {
          "description": "Operator alias for this entry — unique within the gateway's `plugins[]` array. Used as the registry key, audit attribution, and per-plugin observability target. When `ref` is omitted, the alias doubles as the artifact's manifest id (the simple, single-instance case). When `ref` is set, the alias is a separate operator-chosen label (multi-instance pattern).",
          "type": "string"
        },
        "inline_dispatch": {
          "default": false,
          "description": "**Inline fast-slot dispatch.** When `true`, this plugin's hot-path slots are called **inline** — without the `spawn_blocking` ferry or per-call timeout: the typed/borrowed `*_fast` vtable path for Tier-1 slots (`tool_gate`, cutting dispatch ~33×), and the synchronous `execute` slot for `backend` plugins (which also lets the sync tool dispatch bridge resolve the call on its first poll and skip `block_in_place`). This is an explicit operator-trust decision: the plugin's slots MUST be fast, non-blocking, and bounded, because a hung/blocking slot now wedges a runtime worker with no backstop. Defaults to `false` (the safe, ferried path). Only enable for trusted, pure-compute / in-process first-party plugins.",
          "type": "boolean"
        },
        "kind": {
          "default": "native",
          "description": "Plugin tier: `\"native\"` or `\"wasm\"`.",
          "type": "string"
        },
        "limits": {
          "anyOf": [
            {
              "$ref": "#/definitions/PluginResourceLimitsConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Resource limits for Wasm plugins (ignored for native)."
        },
        "observability": {
          "anyOf": [
            {
              "$ref": "#/definitions/PluginObservabilityToggle"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-plugin observability triad override. `inherit` (default), `replace`, or `tee` semantics for each signal independently. Absent = all signals inherit the global `observability.{logs,metrics,traces}` config. Routing is keyed by `module_path_prefix` from the plugin manifest — events from this plugin's crate get the override; events from gateway code about a plugin call stay on the global path."
        },
        "ref": {
          "description": "Manifest id (artifact identity) — reverse-DNS, e.g. `dev.mcpg.policy.cedar`. Optional; defaults to `id` when absent.\n\nSet this when one artifact must run under multiple aliases in the same gateway (e.g. two `cedar` engines bound to different tenants), or when the alias should read more naturally than the reverse-DNS manifest id. The boot loader cross-checks this value against the plugin's descriptor.id; a mismatch fails the load.",
          "type": [
            "string",
            "null"
          ]
        },
        "signature": {
          "anyOf": [
            {
              "$ref": "#/definitions/SignatureConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Plugin signature checks. Consolidates the content hash, the per-entry-overridable verification policy, and the trusted Ed25519 keys this plugin's artifact must verify against (per-entry, no global trust pool).\n\n`None` means defaults: integrity hash unset (no pin), policy inherits `gateway.plugin_registry.default_signature_policy:`, no per-plugin trusted keys."
        },
        "source": {
          "allOf": [
            {
              "$ref": "#/definitions/PluginSourceConfig"
            }
          ],
          "default": {
            "oci": null,
            "path": null
          },
          "description": "Source path or reference for the plugin artifact."
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "PluginFfiLimitsConfig": {
      "additionalProperties": false,
      "description": "Per-plugin FFI hardening overrides for native cdylib plugins.\n\nNative plugin calls are wrapped by the host in `spawn_blocking` + `tokio::time::timeout` and bounded `RString` returns. Defaults are the spec-level constants in `mcpg_plugin_protocol::abi` (`FFI_{LIFECYCLE,CONTROL,DATA}_TIMEOUT_DEFAULT_MS`, `FFI_MAX_PAYLOAD_BYTES`). Operators set per-plugin overrides here to widen the budget for a known-slow plugin (e.g. a backend that proxies an upstream multi-second API) or to tighten the cap on a plugin that has a stricter SLO.\n\n`None` on any field means \"inherit the spec default\".",
      "properties": {
        "control_timeout_ms": {
          "description": "Control slot timeout override (ms). Applies to config-set, snapshot, version, register-profile, refresh, describe, list-peers, list-catalog, etc. Default: `FFI_CONTROL_TIMEOUT_DEFAULT_MS = 5_000`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "data_timeout_ms": {
          "description": "Data slot timeout override (ms). Applies to execute, evaluate, transform, dispatch, http_route, sink-emit, etc. Default: `FFI_DATA_TIMEOUT_DEFAULT_MS = 30_000`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "lifecycle_timeout_ms": {
          "description": "Lifecycle slot timeout override (ms). Applies to `make`, `manifest`, `shutdown`, `drop_instance`, health probes. Default: `FFI_LIFECYCLE_TIMEOUT_DEFAULT_MS = 1_000`.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_payload_bytes": {
          "description": "Max byte-length of any single `RString` returned by this plugin to the host. Overflow rejected with a slot-appropriate fallback + bumps `mcpg_plugin_payload_oversize_total`. Default: `FFI_MAX_PAYLOAD_BYTES = 262144` (256 KiB).",
          "format": "uint",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PluginHttpRouteConfig": {
      "additionalProperties": false,
      "description": "Operator-side tuning for an `http_route` plugin entry. Every field is optional; the struct is omitted entirely for plugins that don't need any override.",
      "properties": {
        "allow_path_override": {
          "default": false,
          "description": "When `true`, plugin routes mount at the top-level paths the plugin declared (override mode), instead of the namespaced `/plugins/{id}/{entity}/` mount (the `false` default). The gate is this operator-set flag alone; the gateway refuses two plugins that claim the same top-level path. Override-mode dispatch is not yet wired — this field is the gate the dispatcher will consult once that support lands.",
          "type": "boolean"
        },
        "disabled": {
          "default": false,
          "description": "When `true`, the plugin is not registered at all. Operators use this to swap out a gateway built-in (e.g. the built-in `dev.mcpg.builtin.http.status`) for a custom implementation without patching the gateway. The gateway logs a warning if a disabled plugin also appears elsewhere in the plugins list with conflicting settings — disable is authoritative.",
          "type": "boolean"
        },
        "max_body_bytes": {
          "description": "Per-entity override for `RouteSpec.max_body_bytes`. When set, the dispatcher uses this value instead of the plugin's declared cap — operator tightens (or relaxes) the plugin's spec without a plugin rebuild. `None` = use the plugin's declared value.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "requires_identity": {
          "description": "Per-entity override for `RouteSpec.requires_identity`. When set, the dispatcher enforces this instead of the plugin's declared value. Typical use: operator tightens an endpoint the plugin declared anonymous. `None` = use the plugin's declared value.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PluginObservabilityToggle": {
      "additionalProperties": false,
      "description": "Per-plugin observability toggle. Each signal is independent — operators can disable metrics for one plugin while leaving its logs and traces flowing. Events still route through the GLOBAL `observability.{logs,metrics,traces}.sinks` list when admitted; this struct only controls *whether* a plugin's events make it that far + at what level.",
      "properties": {
        "logs": {
          "anyOf": [
            {
              "$ref": "#/definitions/SignalToggle"
            },
            {
              "type": "null"
            }
          ],
          "description": "Logs toggle. `None` = inherit globals."
        },
        "metrics": {
          "anyOf": [
            {
              "$ref": "#/definitions/SignalToggle"
            },
            {
              "type": "null"
            }
          ],
          "description": "Metrics toggle. `None` = inherit globals. Note: metrics has no `level` (metrics-rs has no levels) — the field is accepted in YAML for forward compat but ignored today."
        },
        "traces": {
          "anyOf": [
            {
              "$ref": "#/definitions/SignalToggle"
            },
            {
              "type": "null"
            }
          ],
          "description": "Traces toggle. `None` = inherit globals."
        }
      },
      "type": "object"
    },
    "PluginRegistryAuthConfig": {
      "additionalProperties": false,
      "description": "Registry authentication configuration. At most one source of credentials is consulted at push/pull time: an explicit `username`+`password` pair (or env-interpolated variants), otherwise the docker config.json at `docker_config_path`, otherwise anonymous.",
      "properties": {
        "docker_config_path": {
          "default": null,
          "description": "Path to a docker config.json for credential helpers. Defaults to `~/.docker/config.json` when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "password": {
          "default": null,
          "description": "Literal password / bearer token (or `$VAR` / `env:VAR`). Wrapped in [`mcpg_sensitive::Sensitive`] so a stray `?config` log renders this field as `***` instead of the literal token.",
          "type": [
            "string",
            "null"
          ]
        },
        "username": {
          "default": null,
          "description": "Literal username (or `$VAR` / `env:VAR` for env-var interpolation).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PluginRegistryConfig": {
      "additionalProperties": false,
      "description": "Configuration for resolving plugin artifacts from OCI registries. Covers default registry, local cache, auth, TLS, and signature policy.\n\nThis section is only consulted when at least one plugin entry has `source.oci` set. For purely local deployments (every plugin loaded from `source.path`), all defaults apply and the registry subsystem does nothing.",
      "properties": {
        "auth": {
          "allOf": [
            {
              "$ref": "#/definitions/PluginRegistryAuthConfig"
            }
          ],
          "default": {
            "docker_config_path": null,
            "password": null,
            "username": null
          },
          "description": "Registry authentication strategy."
        },
        "cache_dir": {
          "default": null,
          "description": "Local cache directory for pulled OCI artefacts. Keyed by manifest digest so digest-pinned references skip the network on subsequent boots. When unset, defaults to `$XDG_CACHE_HOME/mcpg/plugins/oci` (or `/var/cache/mcpg/plugins/oci` for system deployments).",
          "type": [
            "string",
            "null"
          ]
        },
        "default_registry": {
          "default": "ghcr.io/mcpg-dev/source-code/plugins",
          "description": "Default registry when an `oci:` reference has no registry prefix. Example: `ghcr.io/mcpg-dev/source-code/plugins`.\n\nOmitting the key falls back to the `MCPG_DEFAULT_PLUGIN_REGISTRY` environment variable, then to the compiled-in default. Setting it here always wins over both.",
          "type": "string"
        },
        "default_signature_policy": {
          "allOf": [
            {
              "$ref": "#/definitions/SignaturePolicy"
            }
          ],
          "default": "enforce",
          "description": "Default signature verification policy applied to every `plugins[*]` entry that doesn't carry its own `signature.policy:` override. Defaults to `Warn` (log but don't fail) for first-rollout safety; flip to `Enforce` once trusted keys are wired up across all entries."
        },
        "insecure_registries": {
          "default": [],
          "description": "Hostnames (optionally `host:port`) that the OCI client should reach over plain HTTP instead of HTTPS. `localhost`, `127.0.0.1`, and `::1` are always implicit — operators only need to list this for other dev / air-gap registries.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mirrors": {
          "default": [],
          "description": "Mirror registries tried in order before the reference's source registry. Supports air-gap / pull-through caches.",
          "items": {
            "$ref": "#/definitions/PluginRegistryMirrorConfig"
          },
          "type": "array"
        },
        "require_integrity_anchor": {
          "default": false,
          "description": "When set, every `oci:`-sourced plugin entry must carry an integrity anchor the gateway can enforce independently of the transport: a digest-pinned reference (`…@sha256:<hex>`), a `signature.sha256` artifact-hash pin, or `signature.trusted_keys`. An entry pulled by bare tag with no anchor is refused at boot. Recommended whenever `mirrors` or `insecure_registries` are configured, since a tag pulled over a mirror / plain-HTTP hop is otherwise trusted on the registry's word alone. Defaults to `false` (every entry accepted; configured anchors are still enforced downstream).",
          "type": "boolean"
        },
        "revocation_list_path": {
          "default": null,
          "description": "Optional path to a JSON revocation list. When set, the gateway loads the file at startup, indexes the revoked artefact SHA-256s, and refuses to load any plugin whose hash matches an entry — even if its Ed25519 signature is valid. Format documented in [`mcpg_plugin_host::revocation::RevocationListFile`]. Absent means \"no revocation list\" — every signed plugin is allowed.",
          "type": [
            "string",
            "null"
          ]
        },
        "tls": {
          "allOf": [
            {
              "$ref": "#/definitions/PluginRegistryTlsConfig"
            }
          ],
          "default": {
            "ca_cert": null,
            "insecure": false
          },
          "description": "TLS knobs for registry connections."
        },
        "trusted_keys": {
          "description": "Gateway-wide Ed25519 trust anchors. An entry whose `signature.trusted_keys` is empty verifies against these (plus the built-in official mcpg release key); an entry that carries its own keys verifies against exactly those, so third-party vendors never pool into the global anchor set.",
          "items": {
            "$ref": "#/definitions/TrustedKeyConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "PluginRegistryMirrorConfig": {
      "additionalProperties": false,
      "description": "A mirror registry entry. Mirrors are consulted in order before the reference's source registry, matching the common pull-through cache / air-gap deployment pattern.",
      "properties": {
        "auth": {
          "anyOf": [
            {
              "$ref": "#/definitions/PluginRegistryAuthConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional auth override for this mirror. When absent, inherits the top-level `plugin_registry.auth`."
        },
        "url": {
          "description": "Mirror URL — a prefix that replaces the source registry in resolved pull URLs. Example: `harbor.internal.corp/mcpg-plugins`.",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "PluginRegistryTlsConfig": {
      "additionalProperties": false,
      "description": "TLS configuration for registry HTTPS connections.",
      "properties": {
        "ca_cert": {
          "default": null,
          "description": "Path to a PEM bundle with extra trusted root CAs. Useful for internal registries with private CAs.",
          "type": [
            "string",
            "null"
          ]
        },
        "insecure": {
          "default": false,
          "description": "Skip all TLS certificate verification. DANGEROUS — development-only escape hatch, emits a WARN at boot.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "PluginResourceLimitsConfig": {
      "additionalProperties": false,
      "description": "Resource limits for Wasm plugins.\n\nThese limits constrain the sandbox resources available to a Wasm plugin. If not specified, system defaults are used (64 MiB memory, 10M fuel, 100ms timeout).",
      "properties": {
        "fuel": {
          "default": null,
          "description": "Maximum fuel (instruction budget) per invocation (default: 10_000_000).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "memory_mb": {
          "default": null,
          "description": "Maximum linear memory in megabytes (default: 64).",
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "timeout_ms": {
          "default": null,
          "description": "Wall-clock timeout per invocation in milliseconds (default: 100).",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PluginSourceConfig": {
      "additionalProperties": false,
      "description": "Plugin artifact source configuration.\n\nExactly one of `path` / `oci` must be set. Both unset is invalid; both set is invalid. The source type determines how the gateway resolves the artifact at boot time.",
      "properties": {
        "oci": {
          "default": null,
          "description": "OCI reference (e.g. `ghcr.io/mcpg-dev/source-code/plugins/audit:1.0.0` or `plugins/audit@sha256:…`). At boot the gateway pulls the artifact, verifies the manifest digest, caches it to `plugin_registry.cache_dir`, and loads it through the same sidecar / packaged-zip path `path` would have taken. When the reference is missing a registry prefix, the `plugin_registry.default_registry` value is prepended — which is itself repointable per deployment via the `MCPG_DEFAULT_PLUGIN_REGISTRY` environment variable.\n\n**The reference is PLATFORM-AGNOSTIC.** Plugin artifacts are published per os/arch/libc (the CD tag suffix `-<os>[-musl]-<arch>`, plus `-wasi-wasm` for WASM), and the gateway resolves the right one for the host it runs on — you do NOT write the platform suffix: - **no tag** (`…/plugins/audit`) → tracks the floating `protocol-<major>` tag for the plugin protocol this gateway speaks (`…:protocol-1-linux-amd64` on a glibc x86-64 host, etc.). - **a version/tag** (`…/audit:1.0.0`, `…/audit:protocol-1`) → the host's platform suffix is appended (`…:1.0.0-darwin-arm64`, …). - **already platform-suffixed** (`…:1.0.0-linux-amd64`) or **`@sha256:` digest-pinned** → pulled verbatim (explicit pin; a digest is inherently one specific platform's manifest).\n\nIn every auto-resolved case the gateway prefers the native cdylib for its platform and transparently falls back to the WASM (`wasi-wasm`) build, so a bare reference works for native and WASM plugins alike.\n\n(Native plugins are ALSO published as a multi-platform OCI image index under the bare `:<version>` / `:protocol-<major>` tag — plus a separate `:<version>-musl` index — so generic OCI tooling (`docker pull`, `skopeo`, `crane`) resolves the right platform from one tag. The gateway does not use the index; it resolves the concrete per-platform tag above, which also pins libc, which an index cannot express.)",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "default": null,
          "description": "Path to the plugin artifact on the local filesystem. Accepts a raw `.so` / `.wasm` (with a sidecar `plugin.yaml`) or a packaged `.zip`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PolicyCacheConfig": {
      "additionalProperties": false,
      "description": "Configuration for the policy decision cache (L1 process-local).",
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "max_entries": {
          "default": 10000,
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "ttl_ms": {
          "default": 60000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "PolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "cache": {
          "allOf": [
            {
              "$ref": "#/definitions/PolicyCacheConfig"
            }
          ],
          "default": {
            "enabled": false,
            "max_entries": 10000,
            "ttl_ms": 60000
          }
        },
        "engine": {
          "description": "Ordered chain of policy engines consulted at every decision point (`tool.call.pre`, `plugin.lifecycle.register`, etc.). Each entry is a [`KindRef`] — `kind:` resolves to a built-in keyword (`yaml-rules`), a short alias (`cedar`, `opa`, `casbin` → `dev.mcpg.policy.<alias>`), or a full reverse-domain plugin id. Chain semantics: the host walks the list in order, short-circuiting on the first `Allow` / `Deny`; `NotApplicable` falls through to the next engine. An empty chain is equivalent to `NotApplicable` everywhere — callers (e.g. `enforce_plugin_registration_policy`) decide whether that means \"allow\" (default-allow gateway) or \"fail-closed\" per their own policy posture.\n\nOperators who want only the built-in YAML-rules engine write `engine: [{ kind: yaml-rules }]`. Multi-engine deployments that pair declarative authz with a richer rule engine write `engine: [{ kind: yaml-rules }, { kind: cedar, config: {...} }]`.",
          "items": {
            "$ref": "#/definitions/KindRef"
          },
          "type": "array"
        },
        "tool_access": {
          "allOf": [
            {
              "$ref": "#/definitions/ToolAccessPolicyConfig"
            }
          ],
          "default": {
            "cel_allow_if": null,
            "default_minimum_trust": "header_asserted",
            "rules": []
          }
        }
      },
      "type": "object"
    },
    "PromptArgumentConfig": {
      "properties": {
        "completions": {
          "default": null,
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "description": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "type": "string"
        },
        "required": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "QuotasConfig": {
      "additionalProperties": false,
      "description": "`governance.quotas:` registry.\n\nThree named-policy lists (rate_limits / budgets / concurrency) plus the storage backend that holds the runtime counters. Bindings opt into specific policies by id via their own per-binding `quotas:` block.",
      "properties": {
        "budgets": {
          "description": "Named cost / call-count / token-count budgets. Bindings reference by id.",
          "items": {
            "$ref": "#/definitions/BudgetPolicy"
          },
          "type": "array"
        },
        "concurrency": {
          "description": "Named concurrency caps. Bindings reference by id.",
          "items": {
            "$ref": "#/definitions/ConcurrencyPolicy"
          },
          "type": "array"
        },
        "on_error": {
          "default": "deny",
          "description": "Posture when the quota gate itself errors (e.g. a quota-store read/write failure): `deny` (default, fail-closed — refuse the call so a storage outage cannot silently disable rate-limit / budget / concurrency enforcement) or `allow` (fail-open — proceed without a permit). An empty value is treated as `deny`.",
          "type": "string"
        },
        "rate_limits": {
          "description": "Named rate-limit policies. Bindings reference by id.",
          "items": {
            "$ref": "#/definitions/RateLimitPolicy"
          },
          "type": "array"
        },
        "store": {
          "allOf": [
            {
              "$ref": "#/definitions/KindRef"
            }
          ],
          "default": {
            "kind": ""
          },
          "description": "Storage backend for quota counters / token-buckets / in-flight concurrency. Uses the standard `KindRef` discriminator. `kind: cluster` (default) routes through the cluster coordinator's KV role; `kind: memory` resets on restart (dev-only); `kind: <plugin-id>` pins to a loaded KV plugin."
        }
      },
      "type": "object"
    },
    "RateLimitPolicy": {
      "additionalProperties": false,
      "description": "One named rate-limit policy.",
      "properties": {
        "burst": {
          "description": "Bucket burst capacity — number of calls a caller can spend in quick succession before refill rate kicks in. Defaults to the per-second equivalent of `rate` (i.e., one second's worth).",
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "id": {
          "description": "Operator-chosen id. Bindings reference this via `tools[].quotas.rate_limit: <id>`.",
          "type": "string"
        },
        "identity_claim": {
          "description": "JWT claim path used to key the bucket when `scope: per_identity`. Required for that scope; rejected for others. Common values: `sub`, `org_id`, `email`.",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "default": "token_bucket",
          "description": "Algorithm. v1.0 ships `token_bucket` only; `leaky_bucket` and `sliding_window` are not implemented.",
          "type": "string"
        },
        "on_exceeded": {
          "default": "deny",
          "description": "Action when the bucket runs dry. `deny` returns a 429-style error; `queue` is reserved and currently aliases to `deny` for rate limits; `shed_load` drops silently.",
          "type": "string"
        },
        "rate": {
          "allOf": [
            {
              "$ref": "#/definitions/RateLimitRate"
            }
          ],
          "description": "Refill rate. Today only `calls_per_minute` is supported; future variants will land here."
        },
        "scope": {
          "default": "per_identity",
          "description": "Scope discriminator: how the bucket is keyed. `per_identity` keys by the JWT claim path in `identity_claim`; `global` shares one bucket across all callers; `per_session`, `per_tool` are also valid.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "rate"
      ],
      "type": "object"
    },
    "RateLimitRate": {
      "additionalProperties": false,
      "properties": {
        "calls_per_minute": {
          "description": "Calls allowed per minute. Bucket refills at this rate.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "calls_per_minute"
      ],
      "type": "object"
    },
    "RegistryAuthConfig": {
      "additionalProperties": false,
      "description": "Consumer auth presented to the registry API.",
      "properties": {
        "credential": {
          "description": "Credential-issuer reference for `cred`: a standard `cred://<plugin_id>/<target>` URI. The issuer mints + refreshes the registry bearer under the gateway's machine identity; no static token lives in the config.",
          "type": [
            "string",
            "null"
          ]
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Arbitrary request headers for `headers` (e.g. `X-API-Key`); values support `${env.X}`.",
          "type": "object"
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistryAuthMode"
            }
          ],
          "default": "none"
        },
        "token": {
          "description": "Bearer token for `bearer` (supports `${env.X}`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RegistryAuthMode": {
      "oneOf": [
        {
          "description": "Anonymous reads (the generic spec's default).",
          "enum": [
            "none"
          ],
          "type": "string"
        },
        {
          "description": "`Authorization: Bearer <token>`.",
          "enum": [
            "bearer"
          ],
          "type": "string"
        },
        {
          "description": "Arbitrary static headers (API-key style sub-registries).",
          "enum": [
            "headers"
          ],
          "type": "string"
        },
        {
          "description": "Bearer minted by a credential-issuer plugin (`credential` is a `cred://` URI) under the gateway's machine identity.",
          "enum": [
            "cred"
          ],
          "type": "string"
        }
      ]
    },
    "RegistryDefaultsConfig": {
      "additionalProperties": false,
      "description": "Defaults applied to every synthesized federation.",
      "properties": {
        "auth": {
          "allOf": [
            {
              "$ref": "#/definitions/AuthConfig"
            }
          ],
          "default": {
            "mode": "none"
          },
          "description": "Upstream auth for synthesized federations (same modes as a hand-written federation; per-server `servers.<name>.auth` overrides win)."
        },
        "cache": {
          "allOf": [
            {
              "$ref": "#/definitions/FederationCacheConfig"
            }
          ],
          "default": {
            "capability_ttl_secs": 300
          },
          "description": "Capability-cache TTL refresh."
        },
        "governance": {
          "allOf": [
            {
              "$ref": "#/definitions/BackendGovernanceConfig"
            }
          ],
          "default": {
            "allow_if": null,
            "minimum_trust": "header_asserted"
          },
          "description": "Governance inherited by every synthesized federation (trust floor + CEL), exactly like a hand-written federation's block."
        },
        "import": {
          "allOf": [
            {
              "$ref": "#/definitions/ImportConfig"
            }
          ],
          "default": {
            "prompts": true,
            "resource_templates": true,
            "resources": true,
            "tools": true
          },
          "description": "Surfaces to import. Defaults to everything — the point of auto-federation is the full surface; narrow it here if not."
        },
        "oauth_discovery": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistryOauthDiscoveryConfig"
            }
          ],
          "default": {
            "enabled": false
          },
          "description": "Sync-time OAuth discovery (RFC 9728 protected-resource metadata → RFC 8414 AS metadata) for synthesized federations whose auth uses an OAuth credential mode: derives each server's audience + token endpoint and injects them as the issuer's per-call config (`auth.credential_config`). Off by default."
        },
        "synthesize": {
          "allOf": [
            {
              "$ref": "#/definitions/SynthesizeConfig"
            }
          ],
          "default": {
            "poll_interval_ms": 30000,
            "resources_updated": "auto"
          },
          "description": "Change-notification synthesis for push-less servers."
        },
        "upstream_safety": {
          "allOf": [
            {
              "$ref": "#/definitions/RegistryUpstreamSafetyConfig"
            }
          ],
          "default": {
            "allow_private_backends": false
          },
          "description": "Per-server upstream network posture. Only `allow_private_backends` is honored — stdio and insecure HTTP stay denied for registry-driven servers regardless."
        }
      },
      "type": "object"
    },
    "RegistryFilterConfig": {
      "additionalProperties": false,
      "description": "Which registry servers are eligible.",
      "properties": {
        "exclude": {
          "default": [],
          "description": "Server-name globs to exclude (exclude wins).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include": {
          "default": [
            "*"
          ],
          "description": "Server-name globs to include (exact or trailing-`*`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "namespaces": {
          "default": [],
          "description": "Publisher-namespace allowlist (the part before `/`, e.g. `com.acme`). Empty = all namespaces. The anti-typosquatting rail: registry namespace ownership is publisher-verified, so pinning namespaces pins trust.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "RegistryOauthDiscoveryConfig": {
      "additionalProperties": false,
      "description": "Sync-time OAuth discovery for registry servers.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Fetch each OAuth-mode server's RFC 9728 + RFC 8414 metadata at sync time. Servers whose discovery fails (and that have no prior discovered snapshot) are skipped — a server that cannot be authenticated against would only fail at dispatch.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "RegistrySafetyConfig": {
      "additionalProperties": false,
      "description": "Network posture for the registry endpoint.",
      "properties": {
        "allow_insecure_http": {
          "default": false,
          "description": "Permit `http://` (non-TLS) registry endpoints.",
          "type": "boolean"
        },
        "allow_private_registry": {
          "default": false,
          "description": "Permit a private / loopback registry address (normal for enterprise sub-registries; still an explicit opt-in).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "RegistryServerOverride": {
      "additionalProperties": false,
      "description": "Per-server override, keyed by registry server name.",
      "properties": {
        "auth": {
          "anyOf": [
            {
              "$ref": "#/definitions/AuthConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Upstream auth override for this server."
        },
        "enabled": {
          "default": true,
          "description": "Set false to exclude this server regardless of filters.",
          "type": "boolean"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Values for the remote's declared request headers (secrets via `${env.X}`). Required-header declarations without a value here (or a registry-provided default) skip the server.",
          "type": "object"
        },
        "variables": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Values for `{variable}` templates in the server's remote URL.",
          "type": "object"
        },
        "version": {
          "description": "Pin an exact registry version (default: track the registry's latest).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RegistrySyncConfig": {
      "additionalProperties": false,
      "description": "Sync cadence + bounds.",
      "properties": {
        "full_resync_hours": {
          "default": 24,
          "description": "Hours between full crawls when `incremental` is on. Floor 1.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "incremental": {
          "default": false,
          "description": "Crawl with `updated_since=<watermark>` between periodic full crawls instead of listing everything each tick. Status flips (including deletions) bump `updatedAt`, so deltas carry tombstones too; the periodic full crawl is the backstop for anything missed. Engages only once the registry has yielded `updatedAt` timestamps.",
          "type": "boolean"
        },
        "interval_secs": {
          "default": 300,
          "description": "Seconds between crawls. Each crawl lists the registry's latest server versions in full (cursor-paginated), so deletions are observed without a separate backstop. Floor 30.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_servers": {
          "default": 100,
          "description": "Hard cap on federated servers from this registry. Servers beyond the cap (name-sorted) are skipped and reported — back-pressure against unbounded task/connection growth.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RegistryUpstreamSafetyConfig": {
      "additionalProperties": false,
      "description": "Upstream network posture defaults for synthesized federations. Deliberately narrower than a hand-written federation's `upstream_safety`: the registry chooses which servers exist, so the dangerous knobs are not registry-reachable.",
      "properties": {
        "allow_private_backends": {
          "default": false,
          "description": "Permit private / loopback server addresses (internal remotes — the common enterprise case; still an explicit opt-in).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "RequestStateConfig": {
      "additionalProperties": false,
      "description": "`request_state:` config — the MRTR `requestState` codec used by the modern wire's suspending `tools/call` arm to encrypt pipeline-resumption blobs.\n\nInert until a modern client connects. Lives under `mcp.configurations` because the codec manages runtime-emergent resumption state alongside sessions / pipelines / subscriptions / delivery / cancellation / idempotency — operator tunes the encryption key, the runtime mints + serves the rest.\n\n```yaml mcp: configurations: request_state: # 32-byte ChaCha20-Poly1305 key, base64-encoded. # Generate via: head -c 32 /dev/urandom | base64 encryption_key: \"<base64-32-byte-secret>\" ```\n\nAbsent the key the gateway mints an ephemeral one at boot (with a WARN log) — pending resumptions issued before a gateway restart become undecodable after restart.",
      "properties": {
        "encryption_key": {
          "description": "Base64-encoded 32-byte ChaCha20-Poly1305 secret. `None` ⇒ ephemeral key (random per process; resumptions lost on restart).\n\nIn a clustered deployment, if this is unset but `cluster.state_encryption_key_env` IS set, the codec key is derived (HMAC-SHA256, domain-separated) from that cluster-stable key so every replica decodes the same `requestState` blob — no separate secret needed.",
          "type": [
            "string",
            "null"
          ]
        },
        "strict_encryption": {
          "default": false,
          "description": "Fail-closed guard for clustered modern resume. When `true` and the deployment is clustered (`cluster.kind != single_node`), the gateway REFUSES to boot if the `requestState` codec would fall back to an ephemeral per-process key — i.e. neither `encryption_key` nor a derivable `cluster.state_encryption_key_env` is available. An ephemeral key is undecodable on a peer, so a clustered modern (≤8 KiB inline) resume on another replica silently fails; this turns that silent fail-open into a loud boot error. Default `false` to keep existing clustered (e.g. legacy-only) deployments booting unchanged.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ResourceWatchConfig": {
      "description": "Per-binding resource watch configuration. Defines how changes to a resource are detected for `notifications/resources/updated`.",
      "properties": {
        "notification_filter": {
          "anyOf": [
            {
              "$ref": "#/definitions/NotificationFilterConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Notification filter — controls which subscribers receive the `notifications/resources/updated` message when a change is detected. Defaults to fan-out to all subscribers when absent."
        },
        "strategy": {
          "allOf": [
            {
              "$ref": "#/definitions/WatchStrategyConfig"
            }
          ],
          "default": {
            "interval_ms": 60000,
            "type": "poll"
          },
          "description": "Strategy for detecting resource changes."
        }
      },
      "type": "object"
    },
    "ResponseCacheConfig": {
      "description": "Operator-facing config for the gateway-managed LLM response cache. The cache backs `BackendHost::cache_get` / `cache_put`; chat + embedding bindings opt in per-binding via their own `cache.enabled: true` knob, the cache only exists at all if this config is non-disabled.\n\n`kind: in_process` is the default — content-addressed BLAKE3 LRU cache with 64 MiB byte cap, lost on restart. `kind: disabled` turns the cache off gateway-wide; per-binding `cache.enabled` becomes a no-op.",
      "oneOf": [
        {
          "properties": {
            "kind": {
              "enum": [
                "in_process"
              ],
              "type": "string"
            },
            "max_bytes": {
              "default": 67108864,
              "description": "Maximum aggregate byte size kept in memory. LRU eviction past this. Default 64 MiB — embeddings are small (single-vec kilobytes) and chat outputs are modest (a few KB each), so a modest cap covers tens of thousands of cached calls.",
              "format": "uint",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "properties": {
            "kind": {
              "enum": [
                "disabled"
              ],
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "ResponseConfig": {
      "additionalProperties": false,
      "description": "Per-call response limits.",
      "properties": {
        "max_response_bytes": {
          "default": 2097152,
          "description": "Cap on a single upstream call's response, enforced gateway-side.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RetryConfig": {
      "description": "Per-binding retry configuration. Only applies to HTTP, gRPC, GraphQL, NATS, and Kafka bindings.",
      "properties": {
        "initial_backoff_ms": {
          "default": 200,
          "description": "Initial backoff delay in milliseconds. Doubled on each subsequent retry.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_attempts": {
          "default": 3,
          "description": "Maximum number of retry attempts (not counting the initial attempt).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "retry_on_status_codes": {
          "default": [
            429,
            502,
            503,
            504
          ],
          "description": "HTTP status codes that trigger a retry (only applicable to HTTP/gRPC/GraphQL bindings).",
          "items": {
            "format": "uint16",
            "minimum": 0.0,
            "type": "integer"
          },
          "type": "array"
        },
        "retry_on_transport_error": {
          "default": true,
          "description": "Whether to retry on transport/connection errors.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SchemaEntry": {
      "additionalProperties": false,
      "description": "A named schema entry in the registry. Exactly one source must be provided.",
      "properties": {
        "file": {
          "description": "Path to a local JSON Schema file (relative to the config file).",
          "type": [
            "string",
            "null"
          ]
        },
        "inline": {
          "description": "Inline JSON Schema definition."
        },
        "url": {
          "description": "URL to fetch the JSON Schema from at startup.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ServedRegistryConfig": {
      "additionalProperties": false,
      "description": "`mcp.registry:` — serve a v0.1 MCP-Registry view of this gateway. The catalog exposed is exactly one server entry: the gateway itself (its whole governed surface hangs off one MCP endpoint), so pointing a registry-driven client policy here yields \"the approved server is MCPG\".",
      "properties": {
        "description": {
          "description": "Human description shown by registry clients.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Serve `GET /v0.1/servers` (+ per-version fetches). Default off.",
          "type": "boolean"
        },
        "name": {
          "default": "",
          "description": "Published server name, reverse-DNS namespaced (`com.acme/gateway`). Required when enabled.",
          "type": "string"
        },
        "url": {
          "description": "Canonical external MCP endpoint published in the entry's `remotes[]`. Defaults to `governance.access.resource_metadata.resource` when unset.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ServerConfig": {
      "additionalProperties": false,
      "properties": {
        "aauth_resource_metadata": {
          "anyOf": [
            {
              "$ref": "#/definitions/AauthResourceMetadataConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "AAuth resource metadata. When set, the gateway serves the document at `/.well-known/aauth-resource.json` and attaches the `AAuth-Requirement: requirement=agent-token` challenge (plus `Accept-Signature-Scheme` / `Accept-Signature-Alg`) to authentication-required 401 responses, so AAuth-capable agents can discover that signing with an agent token would succeed. Pair with the `dev.mcpg.identity.aauth` identity plugin, which does the verifying."
        },
        "access_log": {
          "default": true,
          "description": "Emit the per-request access log (`request received` / `request completed` INFO events, one pair per request). Default `true` (the gateway logs every request's lifecycle). Set `false` to suppress the access log on latency/throughput-sensitive deployments: it removes two structured-log events — and their field formatting + sink write — from every request. Audit events, error/warn logs, metrics, and traces are unaffected. Leave `true` unless request-level access logging is provided elsewhere (an ingress/sidecar) or not required.",
          "type": "boolean"
        },
        "allow_private_backends": {
          "default": false,
          "description": "Allow outbound connections to private/loopback/link-local IPs. Default `false` enables the DNS rebinding guard. Set `true` for container-network deployments where backends live on RFC 1918.",
          "type": "boolean"
        },
        "allowed_origins": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "anonymous_rate_limit_burst": {
          "default": 100,
          "description": "Burst allowance for `anonymous_rate_limit_per_min`.",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "anonymous_rate_limit_per_min": {
          "default": 600,
          "description": "Per-IP request-rate cap on the MCP endpoint for requests below cryptographically-verified trust — i.e. anonymous AND header-asserted identities (sustained requests/minute, with `anonymous_rate_limit_burst` headroom). A self-asserted `x-mcpg-subject-id` does NOT buy an exemption. Only Verified traffic (a real OIDC/JWKS/identity-plugin credential) skips this — it is attributable and metered per tenant. Defaults generous (600/min = 10 rps sustained per client IP), far above interactive agent use; `0` disables (e.g. when an upstream WAF throttles, or for single-IP load testing).",
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "bind_address": {
          "default": "127.0.0.1:8787",
          "type": "string"
        },
        "completion_rate_limit_per_sec": {
          "default": null,
          "description": "Per-session rate limit on `completion/complete` requests (cap per second). `None` disables. Guards against broken autocomplete UIs.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "enforce_modern_request_meta": {
          "default": false,
          "description": "Enforce the SEP-2575 per-request `_meta` identity triple (`io.modelcontextprotocol/{protocolVersion, clientInfo, clientCapabilities}`) on EVERY id-bearing modern (`2026-07-28`) request, not just `server/discover`. When false (the default), only `server/discover` requires the triple and other modern methods may carry minimal `_meta`. Has no effect on the `2025-11-25` wire. Opt-in so existing modern clients are unaffected until they adopt the triple.",
          "type": "boolean"
        },
        "extra_resource_uri_schemes": {
          "default": [],
          "description": "Extra resource-URI schemes (beyond the built-in allow-list) treated as first-class by the resource normalizer. Matched case-insensitively.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "health_check": {
          "allOf": [
            {
              "$ref": "#/definitions/HealthCheckConfig"
            }
          ],
          "default": {
            "degraded_latency_threshold_ms": 1000,
            "enabled": true,
            "interval_ms": 30,
            "timeout_ms": 2000,
            "unhealthy_threshold": 3
          },
          "description": "Periodic prober for every binding's backend (SQL server reachability, gRPC endpoint, REST upstream, ...). Distinct from `health_path:` above — that's the gateway's own liveness endpoint for load balancers; this prober actively pings each binding's underlying service and updates `PluginState::{Active, Degraded}` based on results.\n\nLives under `server:` (rather than `observability:`) to reflect that it's binding-management infrastructure, not an observability concern."
        },
        "health_path": {
          "default": "/health",
          "type": "string"
        },
        "max_request_body_mb": {
          "default": 4,
          "description": "Maximum POST body accepted on the MCP endpoint, in MiB. Defaults to 4 MiB. `0` falls back to the default — an unbounded body is never acceptable on a public endpoint.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_sessions_per_tenant": {
          "default": 0,
          "description": "Per-tenant session quota. 0 = unlimited. The stricter of this and the global cap wins.\n\nA \"tenant\" is the trust-qualified principal, so the same subject string asserted by a header and verified by an IdP are separate tenants and cannot exhaust each other's quota.\n\nTwo limits worth knowing before setting this:\n\n- **It is a per-replica cap.** The counter lives in this process, so a fleet of N replicas admits up to N x this value per tenant, and a reconnect that lands on another replica starts fresh. Divide by the replica count if you need a fleet-wide ceiling. - **Anonymous callers share one bucket**, because an unauthenticated request carries nothing to separate them by. Aggregate anonymous usage is capped rather than per-caller usage; per-caller protection for anonymous traffic is `anonymous_rate_limit_per_min`, which meters by source IP.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "mcp_path": {
          "default": "/mcp",
          "type": "string"
        },
        "relax_request_id_uniqueness": {
          "default": false,
          "description": "Relax the per-session JSON-RPC request-id uniqueness rule. When false (default) a client-supplied `id` that has already been used on the same MCP session is rejected with `-32600` (JSON-RPC forbids id reuse). Set `true` only for load generators that replay a fixed request body (e.g. the fortio proxy-overhead benchmark in `tools/bench/fortio/`), where every request carries the same `id`. Never enable in production — it removes a duplicate-delivery / replay guard.",
          "type": "boolean"
        },
        "replay_window_limit": {
          "default": 16,
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "request_timeout_ms": {
          "default": 30000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "revalidate_mutated_tool_arguments": {
          "default": false,
          "description": "Re-validate tool arguments against the tool's inputSchema after a tool_gate / transform plugin rewrites them. When false (default) only the caller's original arguments are validated. Opt-in defense-in-depth — plugins are operator-signed, so a rewrite that diverges from the published schema is normally trusted.",
          "type": "boolean"
        },
        "scrub_process_env_after_boot": {
          "default": false,
          "description": "After boot (plugins loaded, config-origin `env://` / `${env.X}` secrets already captured by the env secret provider's snapshot), remove those referenced env vars from the live process environment so a loaded cdylib can no longer read them via `std::env::var` / shared-process env. Opt-in defense-in-depth, default off. NOTE: this is NOT a hard boundary — it does not clear `/proc/self/environ` (the exec-time copy), so a hostile in-process plugin can still recover the original values there; it raises the bar against accidental/casual exposure. Enable only once every plugin resolves its secrets via the host (`cred://` / `env://`) rather than reading env directly.",
          "type": "boolean"
        },
        "server_ping_interval_ms": {
          "default": null,
          "description": "Emit a server-initiated `ping` to each active session's SSE stream on this cadence. `None` or `0` disables. Reasonable value: 30s.",
          "format": "uint64",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "session_idle_timeout_ms": {
          "default": 900000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "shutdown_timeout_ms": {
          "default": 30000,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "tls": {
          "anyOf": [
            {
              "$ref": "#/definitions/TlsConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "transport": {
          "allOf": [
            {
              "$ref": "#/definitions/TransportMode"
            }
          ],
          "default": "http"
        },
        "transports": {
          "description": "Additional plugin-supplied transports started at boot alongside the primary HTTP / stdio listener (which continues to be governed by `transport:` and `bind_address:`). Each entry is a [`KindRef`] — `kind:` resolves to either a built-in transport keyword (today only `dev.mcpg.builtin.transport.memory` is wired; `builtin-http` / `builtin-stdio` map to the in-tree HTTP / stdio paths and don't need a list entry) or a registered Transport plugin id. The plugin's `Transport::start(config, dispatcher)` runs once per list entry; transports that fail to start halt the boot. Empty list = no extra transports beyond the primary listener — today's default.\n\nBeyond the primary HTTP/stdio listener, additional transport plugins (websocket, grpc, custom RPC) ship as cdylibs under `plugins[]` and are wired to a session loop via this list.",
          "items": {
            "$ref": "#/definitions/KindRef"
          },
          "type": "array"
        },
        "trust_proxy_ip": {
          "default": false,
          "description": "Trust `X-Forwarded-For` for the client IP used by the anonymous rate limit. Set ONLY when a trusted reverse proxy / edge fronts this gateway (the managed-cloud Envoy edge does) — the header is spoofable otherwise. When false (default) the TCP peer address is used.",
          "type": "boolean"
        },
        "trust_subject_header": {
          "default": false,
          "description": "Trust the `x-mcpg-subject-id` request header as a header-asserted identity. The header carries no proof of who the caller is, so when false (default) it is IGNORED and such requests resolve to Anonymous — only a verified credential (OIDC/JWKS/identity plugin) yields a non-anonymous principal. Set true ONLY behind a trusted upstream that authenticates the caller and injects this header.",
          "type": "boolean"
        },
        "tunnel": {
          "anyOf": [
            {
              "$ref": "#/definitions/TunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Reverse-tunnel egress: dial out to an MCPG-Cloud relay and serve this gateway's MCP surface through the tunnel. `mcpg --tunnel` populates this. Absent / `enabled: false` = no tunnel."
        },
        "tunnel_federation": {
          "anyOf": [
            {
              "$ref": "#/definitions/TunnelFederationConfig"
            },
            {
              "type": "null"
            }
          ],
          "description": "Reverse-federation ingress: how this gateway reaches same-org `tunnel://<name>` federation upstreams through the relay's federation ingress. Independent of `tunnel` (egress) — a gateway can federate other gateways' tunnels without dialing one of its own."
        },
        "unary_json_fast_path": {
          "default": false,
          "description": "On the legacy (`2025-11-25`) wire, answer a unary request whose result is immediately available and that emitted NO server→client notifications (`log` / `progress`) with a single `application/json` response instead of a one-frame `text/event-stream` reply. This is spec-permitted (Streamable HTTP lets the server pick JSON or SSE) and mirrors what the modern (`2026-07-28`) wire already does; it skips the per-request SSE stream bookkeeping (replay-window append, priming + logging frames, session snapshot) that otherwise runs under the session lock, which materially raises tool-call throughput. Default `false` (unchanged SSE behaviour). A request that DOES emit notifications, or suspends (MRTR), still streams regardless of this flag.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SessionConfig": {
      "additionalProperties": false,
      "description": "Upstream-session behaviour.",
      "properties": {
        "idle_timeout_secs": {
          "default": 600,
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "SessionsConfig": {
      "additionalProperties": false,
      "description": "`sessions:` config — session lifecycle store + per-capability `store:` override. When `store` is unset, the session KV inherits from the cluster's `key_value_store()` primitive; when set, the override pins to an in-process backend (memory / file).",
      "properties": {
        "bind_session_owner": {
          "default": false,
          "description": "Bind each session to the principal that created it. When true, session-scoped operations (GET→SSE stream, DELETE→terminate, subscriptions, POST→SSE continuation) require the caller's resolved principal to match the session's creator; a mismatch is refused as if the session did not exist (no existence leak). Default false (today's possession-only behaviour). An anonymous session (no creating principal) can only be driven anonymously.",
          "type": "boolean"
        },
        "optional": {
          "default": false,
          "description": "Make sessions optional on the legacy (`2025-11-25`) wire. When false (default), a legacy request without an `Mcp-Session-Id` header is rejected (`-32600`, HTTP 400) — a legacy client MUST `initialize` first. When true, such a request is instead served through an ephemeral, row-less session (the same lane the modern wire uses for anonymous stateless calls): the gateway does not issue a session, so it does not demand one. Spec-permitted (a server chooses whether to issue sessions), and lets fixed-tool-set proxy deployments skip the handshake round-trip. Features that inherently need a durable session — SSE resume cursors, server-initiated requests, cross-request task/subscription continuity — still require a real session; a session-less request that would need one gets a clear error. `initialize` continues to mint real sessions regardless.",
          "type": "boolean"
        },
        "store": {
          "anyOf": [
            {
              "$ref": "#/definitions/StoreOverrideConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "synthetic_session_key": {
          "description": "Base64-encoded 32-byte secret used to derive the per-principal synthetic session id minted for modern (2026-07-28) stateless requests that arrive without a session header.\n\nWhen set **identically across every replica**, two requests from the same authenticated principal — on any replica — resolve to the same synthetic session id (HMAC of the principal id under this key), so `tasks/create` on replica A is readable via `tasks/get` on B and modern subscriptions converge. The id is an HMAC (not a bare hash), so a client that knows another principal's id still cannot compute — and therefore cannot hijack — that principal's synthetic session.\n\n`None` ⇒ each replica mints a random per-instance synthetic session (no cross-replica continuity); the gateway logs a boot WARN when a distributed coordinator is configured without this key.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "SignalToggle": {
      "additionalProperties": false,
      "description": "Per-plugin per-signal toggle. Four knobs:\n\n- `enabled` (default `true`): when `false`, every event from this plugin's crate is dropped at the bridge before it reaches the sink fan-out — the \"silence this noisy plugin\" pattern. - `level` (logs / traces only): minimum severity an event must clear to be emitted. Composed into the bridge layer's permissive filter so per-plugin verbosity boosts AND suppressions both work. Accepted: `trace` / `debug` / `info` / `warn` / `error` (case-insensitive). - `mode` (default `inherit`): how to route events that pass the gate. `inherit` flows through the global sink list (the default behaviour). `replace` routes ONLY to the plugins listed under `sinks` — used for compliance carve-outs (\"audit logs go to my SIEM, never to stdout\"). `tee` fans out to BOTH the global sink list AND the per-plugin `sinks`. - `sinks`: plugin ids of the sink plugins to use under `mode: replace | tee`. Each id MUST match a registered sink plugin for the corresponding signal — log sink for `logs.sinks`, metrics sink for `metrics.sinks`, span sink for `traces.sinks`. If any id is unknown to the matching signal, the gateway refuses to boot (validated post-registration in `app::validate_per_plugin_sink_ids`). Listing a real log sink id under `metrics.sinks` is rejected — sink-kind crossover is a typo, not a feature.",
      "properties": {
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "level": {
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "$ref": "#/definitions/SinkMode"
        },
        "sinks": {
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SignatureConfig": {
      "additionalProperties": false,
      "description": "Per-plugin signature configuration.\n\nConsolidates three signature concerns into one per-plugin block: - The content-hash pin. - The verification policy — per-plugin overridable, with the global default in `gateway.plugin_registry.default_signature_policy:`. - The Ed25519 trusted keys this artifact must verify against — per-plugin so plugins from different vendors can carry different keys without pooling them in one trust anchor.",
      "properties": {
        "policy": {
          "anyOf": [
            {
              "$ref": "#/definitions/SignaturePolicy"
            },
            {
              "type": "null"
            }
          ],
          "description": "Verification policy for this plugin. `None` = inherit `gateway.plugin_registry.default_signature_policy:`."
        },
        "sha256": {
          "description": "SHA-256 content hash to pin (hex-encoded). When set, the gateway refuses to load the artifact if its computed hash doesn't match.",
          "type": [
            "string",
            "null"
          ]
        },
        "trusted_keys": {
          "description": "Ed25519 verification keys this artifact's signature must verify against. Empty = inherit gateway-wide defaults.",
          "items": {
            "$ref": "#/definitions/TrustedKeyConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SignaturePolicy": {
      "description": "Signature verification policy for native plugin artefacts. The Ed25519 signature attached to the artefact (`<artifact>.sig` or the packaged `plugin.sig`) is the primary check; this policy governs behaviour when the signature is missing or invalid. Set per-plugin via `plugins[*].signature.policy:`, or as a gateway-wide default via `gateway.plugin_registry.default_signature_policy:`.",
      "oneOf": [
        {
          "description": "Signature checks are skipped entirely. Development only; gateway emits a `governance.plugin.signature_policy_disabled` audit event for any entry that resolves to this policy so the choice is visible in the compliance trail.",
          "enum": [
            "disabled"
          ],
          "type": "string"
        },
        {
          "description": "Log a warning for missing or invalid signatures but proceed with the load — ONLY while no trusted keys are configured. The built-in official key means an inheriting entry always has keys, so this behaves like `enforce` unless a config empties the trust set.",
          "enum": [
            "warn"
          ],
          "type": "string"
        },
        {
          "description": "Refuse to load any artefact whose signature is missing or does not verify against the configured trusted keys. The default: a stock gateway loads only signed plugins.",
          "enum": [
            "enforce"
          ],
          "type": "string"
        }
      ]
    },
    "SinkConfig": {
      "additionalProperties": false,
      "description": "One sink in an observability signal's `sinks: [...]` list. The `kind:` field dispatches to a built-in factory (`stderr`, `stdout`, `file`, `otlp`, `prometheus`) or to a plugin id (any other value is looked up in the plugin registry at boot).\n\n`config:` is the sink-kind-specific config object. Built-in kinds validate their own `config:` shape at boot; for plugin sinks, the plugin's own config schema applies.\n\n`level:` is an optional per-sink severity floor. When `None`, the sink inherits the signal's `level:`. Useful for `stderr: warn, file: debug` setups where the console is quiet but a file captures everything. Per-sink level overrides are parsed but not yet enforced; today signal-level `level:` is the only enforced floor.",
      "properties": {
        "config": {
          "default": null,
          "description": "Sink-specific config object. Schema depends on `kind:`."
        },
        "kind": {
          "description": "Sink kind. Built-in keywords: `stderr`, `stdout`, `file`, `otlp`, `prometheus`. Anything else is resolved as a plugin id at boot.",
          "type": "string"
        },
        "level": {
          "description": "Per-sink severity floor. `None` = inherit signal-level `level:`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "SinkMode": {
      "description": "How to route events for a per-plugin signal toggle. Operator schema: `mode: inherit | replace | tee`.",
      "oneOf": [
        {
          "description": "Inherit the global sink list — the same routing every other plugin's events use. Default.",
          "enum": [
            "inherit"
          ],
          "type": "string"
        },
        {
          "description": "Route admitted events ONLY to the per-plugin `sinks` list. Skips the global sink fan-out entirely. Used for compliance carve-outs (audit logs stay inside the SIEM).",
          "enum": [
            "replace"
          ],
          "type": "string"
        },
        {
          "description": "Tee — admitted events flow to BOTH the global sink list AND the per-plugin `sinks` list. Useful when an operator wants to keep default routing but additionally mirror a noisy plugin's events to a debugging sink.",
          "enum": [
            "tee"
          ],
          "type": "string"
        }
      ]
    },
    "StorageConfig": {
      "additionalProperties": false,
      "description": "Top-level `storage:` block. Holds the gateway's content-store providers + the LLM response cache.",
      "properties": {
        "default": {
          "description": "Provider id that bindings without an explicit `content_storage:` field route to. When unset, the gateway falls back to a provider with the conventional id `default`. Validated at boot — an unknown id fails fast.",
          "type": [
            "string",
            "null"
          ]
        },
        "providers": {
          "default": [],
          "description": "Content-store provider entries. Each entry produces an `Arc<dyn ContentStore>` registered under `id` in the gateway's runtime registry. Bindings reference providers by `id` via their own `content_storage:` field.",
          "items": {
            "$ref": "#/definitions/StorageProviderConfig"
          },
          "type": "array"
        },
        "response_cache": {
          "allOf": [
            {
              "$ref": "#/definitions/ResponseCacheConfig"
            }
          ],
          "default": {
            "kind": "in_process",
            "max_bytes": 67108864
          },
          "description": "Gateway-managed LLM response cache. Lives here (rather than under `plugins:`) so all \"where bytes go to live\" config shares one home."
        }
      },
      "type": "object"
    },
    "StorageProviderConfig": {
      "additionalProperties": false,
      "description": "One operator-declared content-store provider, an entry in `storage.providers: [...]`. The `kind` field selects the storage plugin (`in_process` / `file_system` / `s3` / future plugins); `config` is the per-plugin configuration object whose schema is owned by the plugin (see each plugin's `plugin.yaml`).",
      "properties": {
        "config": {
          "default": null,
          "description": "Plugin-specific configuration JSON. Validated by the plugin at `build_profile` time; gateway boot fails fast if the shape doesn't match."
        },
        "id": {
          "description": "Operator-chosen id. Bindings reference providers via this id (`content_storage: <id>` on the binding entry). The conventional `default` id is the fallback when a binding doesn't specify its own AND `storage.default` is unset.",
          "type": "string"
        },
        "kind": {
          "description": "Storage plugin kind (e.g. `in_process`, `file_system`, `s3`). Resolved against the gateway's content-store plugin registry at boot.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "kind"
      ],
      "type": "object"
    },
    "StoreOverrideConfig": {
      "additionalProperties": true,
      "description": "`<capability>.store: { kind, … }` — produces an `Arc<dyn mcpg_cluster_api::KeyValueStore>` at boot.\n\nRecognised `kind` values: `cluster`, `memory`, `file`. (`redis` and `nats` are not accepted here — set `cluster.kind: redis | nats` and use `kind: cluster` here, or omit the override entirely.)",
      "properties": {
        "kind": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "SubscriptionsConfig": {
      "additionalProperties": false,
      "description": "`subscriptions:` config (resource subscriptions) — subscription store + per-capability `store:` override + per-session quota. When `store` is unset, the subscription KV inherits from the cluster's `key_value_store()` primitive.",
      "properties": {
        "max_per_session": {
          "default": 100,
          "description": "Maximum subscriptions per session (0 = unlimited).",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "store": {
          "anyOf": [
            {
              "$ref": "#/definitions/StoreOverrideConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "SynthesizeConfig": {
      "additionalProperties": false,
      "description": "Change-notification synthesis: when the upstream has no server→client push channel, the gateway can manufacture `notifications/resources/updated` for subscribed federated resources by polling them through the normal read path and hash-diffing the content (the watch engine's poll strategy). `list_changed` synthesis rides the existing capability TTL refresh (`cache.capability_ttl_secs`) and needs no knob here.",
      "properties": {
        "poll_interval_ms": {
          "default": 30000,
          "description": "Poll cadence for synthesized resource updates. Watchers are subscriber-gated: no subscribers, no polling.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "resources_updated": {
          "allOf": [
            {
              "$ref": "#/definitions/SynthesizeMode"
            }
          ],
          "default": "auto",
          "description": "When to poll-synthesize `resources/updated` for subscribed federated resources."
        }
      },
      "type": "object"
    },
    "SynthesizeMode": {
      "description": "Gate on synthesized change notifications.",
      "oneOf": [
        {
          "description": "Synthesize only when the upstream demonstrably cannot push resource updates: the modern (`2026-07-28`) wire and the `stdio` transport. A legacy streamable-http upstream keeps its GET-SSE push path and is not polled.",
          "enum": [
            "auto"
          ],
          "type": "string"
        },
        {
          "description": "Always poll, even for upstreams with a push channel (covers legacy servers that only emit `resources/updated` for subscriptions the gateway does not place upstream).",
          "enum": [
            "poll"
          ],
          "type": "string"
        },
        {
          "description": "Never synthesize.",
          "enum": [
            "off"
          ],
          "type": "string"
        }
      ]
    },
    "TaggedVariableCompletionSource": {
      "oneOf": [
        {
          "description": "Static list of completion values. Same shape as the shorthand bare-list but with explicit `kind` tag.",
          "properties": {
            "kind": {
              "enum": [
                "static"
              ],
              "type": "string"
            },
            "values": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "kind",
            "values"
          ],
          "type": "object"
        },
        {
          "description": "Dynamic dispatch: at completion time, the gateway calls [`crate::backends::CapabilityRegistry::complete_argument`], which routes to the named backend's `BackendPlugin::complete_template_variable(binding_name, variable_name, prefix, &config)`. The backend returns up to 100 completions matching the prefix.",
          "properties": {
            "backend": {
              "description": "Binding name that hosts the lookup. Must resolve to a registered backend at boot — dangling names log a warning at registration and the variable falls through to the empty-completion path (spec-valid empty result).",
              "type": "string"
            },
            "config": {
              "default": null,
              "description": "Backend-specific opaque config. SQL passes `{ query: \"SELECT DISTINCT … LIKE :prefix || '%' …\", max_results?: u32 }`; future HTTP would pass `{ endpoint: \"…\" }`. The gateway does not inspect these fields — it forwards them to the backend, which routes internally."
            },
            "kind": {
              "enum": [
                "dynamic"
              ],
              "type": "string"
            }
          },
          "required": [
            "backend",
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "TasksConfig": {
      "additionalProperties": false,
      "description": "`tasks:` config (MCP 2025-11-25 tasks system) — task store + per-capability `store:` override + retention tuning. When `store` is unset, the task KV inherits from the cluster's `key_value_store()` primitive. Tuning fields (`default_ttl_ms`, `reaper_interval_ms`, `max_tasks_per_session`, `result_wait_ms`) are orthogonal and apply to whichever backend resolves.",
      "properties": {
        "default_ttl_ms": {
          "default": 1800000,
          "description": "Default TTL applied to any task created without an explicit `task.ttl` from the client. Used by `tasks/create` and the reaper.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_tasks_per_session": {
          "default": 256,
          "description": "Maximum concurrent tasks per session. Creation above this quota is rejected with JSON-RPC `-32603 Internal error` rather than silently succeeding. `0` disables the quota.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "reaper_interval_ms": {
          "default": 60000,
          "description": "Background reaper sweep interval. The reaper deletes records whose `created_at + ttl` has elapsed.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "result_wait_ms": {
          "default": 30000,
          "description": "Upper bound on a single `tasks/result` HTTP blocking wait. Clients that need longer-running tasks reconnect via GET SSE and `Last-Event-Id` until the task goes terminal.",
          "format": "uint64",
          "minimum": 0.0,
          "type": "integer"
        },
        "store": {
          "anyOf": [
            {
              "$ref": "#/definitions/StoreOverrideConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "TlsConfig": {
      "additionalProperties": false,
      "description": "TLS configuration for the HTTP transport.",
      "properties": {
        "cert_path": {
          "type": "string"
        },
        "client_ca_certs_path": {
          "default": null,
          "description": "Optional path to a PEM bundle of CA certs that gate-keep client cert acceptance for mTLS. Required whenever `client_cert_required` is `\"optional\"` or `\"mandatory\"`; must be empty / absent when `\"none\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "client_cert_required": {
          "allOf": [
            {
              "$ref": "#/definitions/ClientCertMode"
            }
          ],
          "default": "none",
          "description": "Client cert acceptance mode for mTLS connections:\n\n- `\"none\"` (default) — server-only TLS, no client certs. - `\"optional\"` — present-cert is verified against `client_ca_certs_path`; no-cert is allowed and surfaces as `client_cert_present: false` in `TlsInfo`. Operators layer policy plugins to decide what to do with anonymous connections. - `\"mandatory\"` — handshake fails when no client cert is presented; surface is `client_cert_present: true` for any successful connection.\n\nThe mode populates the gateway's `transport_listen` capability bit, which identity plugins like `dev.mcpg.identity.workload` and `dev.mcpg.identity.mtls` (direct_mtls source) require."
        },
        "key_path": {
          "type": "string"
        },
        "min_tls_version": {
          "default": "1.2",
          "description": "Minimum TLS version: `\"1.2\"` or `\"1.3\"`. Default `\"1.2\"`.",
          "type": "string"
        }
      },
      "required": [
        "cert_path",
        "key_path"
      ],
      "type": "object"
    },
    "TokenSourceConfig": {
      "additionalProperties": false,
      "properties": {
        "header_name": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "header_prefix": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "allOf": [
            {
              "$ref": "#/definitions/TokenSourceKind"
            }
          ],
          "default": "authorization_bearer"
        }
      },
      "type": "object"
    },
    "TokenSourceKind": {
      "enum": [
        "authorization_bearer",
        "custom_header"
      ],
      "type": "string"
    },
    "ToolAccessPolicyConfig": {
      "additionalProperties": false,
      "properties": {
        "cel_allow_if": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "default_minimum_trust": {
          "allOf": [
            {
              "$ref": "#/definitions/TrustLevelConfig"
            }
          ],
          "default": "header_asserted"
        },
        "rules": {
          "default": [],
          "items": {
            "$ref": "#/definitions/ToolTrustRuleConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ToolTrustRuleConfig": {
      "additionalProperties": false,
      "properties": {
        "cel_allow_if": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "minimum_trust": {
          "$ref": "#/definitions/TrustLevelConfig"
        },
        "required_scopes": {
          "description": "OAuth scopes the caller's token MUST carry to invoke this tool (SEP-2350). A caller authenticated but lacking any of these is denied with HTTP 403 + a `WWW-Authenticate: Bearer error=\"insufficient_scope\", scope=\"…\"` step-up challenge naming the missing scopes, rather than a bare 403 — so a capability-aware client can request the additional scopes and retry. Empty (the default) means no scope requirement.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tool_name": {
          "type": "string"
        }
      },
      "required": [
        "minimum_trust",
        "tool_name"
      ],
      "type": "object"
    },
    "TracesConfig": {
      "additionalProperties": false,
      "description": "`observability.traces:` — the traces signal.\n\nSpan lifecycle events (every `tracing::info_span!()` / `debug_span!()` and every plugin-emitted span) flow through the configured sink list. The canonical sink is `otlp` (exports to an OpenTelemetry Collector). Default: traces disabled (operators opt in by setting `enabled: true` and adding sinks).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Master enable for the traces signal. Default `false` — tracing has non-trivial overhead so operators opt in.",
          "type": "boolean"
        },
        "propagate_context": {
          "default": true,
          "description": "Propagate W3C trace context (`traceparent` / `tracestate`) headers to outbound binding calls. Defaults to `true` — downstream services join the same trace.",
          "type": "boolean"
        },
        "service_name": {
          "default": "mcpg",
          "description": "Service name advertised to OTel collectors. Default `\"mcpg\"`.",
          "type": "string"
        },
        "sinks": {
          "default": [],
          "description": "Sink fan-out. Each entry's `kind:` resolves to a built-in factory (`otlp`) or plugin id. Default: empty — operators add an `otlp` sink to ship to a collector.",
          "items": {
            "$ref": "#/definitions/SinkConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "TransportMode": {
      "description": "Transport mode determines whether MCPG runs as an HTTP server or a stdio JSON-RPC process.",
      "enum": [
        "http",
        "stdio"
      ],
      "type": "string"
    },
    "TrustLevelConfig": {
      "enum": [
        "unauthenticated",
        "header_asserted",
        "verified"
      ],
      "type": "string"
    },
    "TrustedIdpConfig": {
      "additionalProperties": false,
      "description": "One enterprise IdP trusted to issue ID-JAGs.",
      "properties": {
        "allow_private_network": {
          "default": false,
          "description": "Local-development escape hatch: permit `http://` and private/loopback IdP addresses. Production deployments leave this `false`.",
          "type": "boolean"
        },
        "allowed_hosts": {
          "default": [],
          "description": "Optional host allowlist for discovery/JWKS fetches (exact or subdomain match). Empty = any public host.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "issuer": {
          "description": "The IdP's issuer identifier, compared exactly against the ID-JAG `iss` claim.",
          "type": "string"
        },
        "jwks_uri": {
          "default": null,
          "description": "JWKS endpoint override. When omitted, the JWKS URI is taken from the IdP's OIDC discovery document (`{issuer}/.well-known/openid-configuration`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "issuer"
      ],
      "type": "object"
    },
    "TrustedKeyConfig": {
      "additionalProperties": false,
      "description": "One trusted-key entry inside `SignatureConfig.trusted_keys`.",
      "properties": {
        "id": {
          "description": "Operator-chosen id for the key (audit-trail label).",
          "type": "string"
        },
        "pem": {
          "description": "PEM-encoded public key. Multi-line literal in YAML.",
          "type": "string"
        }
      },
      "required": [
        "id",
        "pem"
      ],
      "type": "object"
    },
    "TunnelConfig": {
      "additionalProperties": false,
      "description": "Reverse-tunnel egress config. The gateway dials out to a relay and answers tunnelled MCP traffic through its own request path.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Master switch. `false` (default) dials no tunnel.",
          "type": "boolean"
        },
        "exposure": {
          "allOf": [
            {
              "$ref": "#/definitions/TunnelExposure"
            }
          ],
          "default": "public",
          "description": "`public` allocates a `<id>.tunnels.mcpg.cloud` URL (dev preview, third-party MCP clients); `private` (federation-only) allocates no public address and is reachable only as a `tunnel://` federation upstream from the same org."
        },
        "mode": {
          "allOf": [
            {
              "$ref": "#/definitions/TunnelTrustMode"
            }
          ],
          "default": "relay_terminated",
          "description": "`relay_terminated` (the relay sees plaintext) or `e2ee` (relay splices ciphertext — requires `private` exposure, mcpg-to-mcpg only)."
        },
        "name": {
          "description": "Optional stable tunnel name; the relay allocates one when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "relay_url": {
          "default": "wss://relay.tunnels.mcpg.cloud",
          "description": "Relay endpoint to dial (e.g. `wss://relay.tunnels.mcpg.cloud`).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TunnelExposure": {
      "description": "Whether a tunnel gets a public hostname.",
      "enum": [
        "public",
        "private"
      ],
      "type": "string"
    },
    "TunnelFederationConfig": {
      "additionalProperties": false,
      "description": "Reverse-federation ingress config. A `tunnel://<name>/<path>` federation upstream resolves through the relay's federation ingress to `<relay_ingress_url>/federate/<name>/<path>`. This gateway authenticates its ORG to the relay with the `token` field below (carried in the `X-MCPG-Tunnel-Token` header, which the relay consumes and never forwards); the end-user's `Authorization` bearer flows through, untouched, to the tunnelled gateway as the MCP caller identity.",
      "properties": {
        "relay_ingress_url": {
          "description": "Relay federation-ingress base URL, e.g. `https://relay.tunnels.mcpg.cloud`. Must be `http(s)`.",
          "type": "string"
        },
        "token": {
          "description": "Org token presented to the relay in `X-MCPG-Tunnel-Token`. When unset, the gateway falls back to the `MCPG_TUNNEL_TOKEN` environment variable (the same org token used for egress dial), so a gateway that both dials and federates needs the token in one place. Supports `${env.X}`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "relay_ingress_url"
      ],
      "type": "object"
    },
    "TunnelTrustMode": {
      "description": "Who can read tunnelled payloads.",
      "enum": [
        "relay_terminated",
        "e2ee"
      ],
      "type": "string"
    },
    "UiGroup": {
      "additionalProperties": false,
      "description": "A labelled group of fields in a form/detail layout.",
      "properties": {
        "fields": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "label": {
          "type": "string"
        }
      },
      "required": [
        "fields",
        "label"
      ],
      "type": "object"
    },
    "UiSchema": {
      "additionalProperties": false,
      "description": "Widget/layout overlay — the tight mcpg-native uiSchema subset.",
      "properties": {
        "groups": {
          "description": "Labelled field groups / sections.",
          "items": {
            "$ref": "#/definitions/UiGroup"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "order": {
          "description": "Explicit field render order.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "widgets": {
          "additionalProperties": {
            "$ref": "#/definitions/UiWidget"
          },
          "description": "field-path → widget specification.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "UiWidget": {
      "additionalProperties": false,
      "description": "How a single field renders, plus its client-evaluated rules.",
      "properties": {
        "enum_from": {
          "anyOf": [
            {
              "$ref": "#/definitions/EnumSource"
            },
            {
              "type": "null"
            }
          ],
          "description": "Options sourced from a sibling tool result (via the action proxy)."
        },
        "help": {
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        },
        "placeholder": {
          "type": [
            "string",
            "null"
          ]
        },
        "required_if": {
          "description": "Client-evaluated conditional-required expression.",
          "type": [
            "string",
            "null"
          ]
        },
        "visible_if": {
          "description": "Client-evaluated visibility expression.",
          "type": [
            "string",
            "null"
          ]
        },
        "widget": {
          "$ref": "#/definitions/WidgetKind"
        }
      },
      "required": [
        "widget"
      ],
      "type": "object"
    },
    "UpstreamConfig": {
      "additionalProperties": false,
      "description": "Upstream connection details.",
      "properties": {
        "args": {
          "default": [],
          "description": "Arguments passed to the stdio `command`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "auth": {
          "allOf": [
            {
              "$ref": "#/definitions/AuthConfig"
            }
          ],
          "default": {
            "mode": "none"
          },
          "description": "How MCPG authenticates to the upstream."
        },
        "command": {
          "default": null,
          "description": "Command to spawn for the `stdio` transport (ignored otherwise).",
          "type": [
            "string",
            "null"
          ]
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Extra environment for the stdio `command`.",
          "type": "object"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Static request headers sent on every upstream call (API-key style upstreams, e.g. `X-API-Key`); values support `${env.X}`. Reserved protocol headers (`authorization`, `mcp-*`, `content-type`, `accept`) are rejected — auth goes through `auth`, the wire headers stay MCPG's.",
          "type": "object"
        },
        "protocol_version": {
          "allOf": [
            {
              "$ref": "#/definitions/UpstreamProtocolVersion"
            }
          ],
          "default": "auto",
          "description": "MCP wire revision MCPG speaks to this upstream as a client.\n\nDefault `v2025_11_25` (the session-bound wire) keeps the federation client byte-identical to legacy traffic. Set `v2026_07_28` to speak the stateless modern client wire: no `initialize` handshake, no `Mcp-Session-Id`, per-request `_meta` identity, and the SEP-2243 `Mcp-Method` / `Mcp-Name` / `Mcp-Param-{Name}` routing headers. Only the `streamable_http` transport honors `v2026_07_28`."
        },
        "transport": {
          "allOf": [
            {
              "$ref": "#/definitions/UpstreamTransport"
            }
          ],
          "default": "streamable_http",
          "description": "Wire transport."
        },
        "upstream_safety": {
          "allOf": [
            {
              "$ref": "#/definitions/UpstreamSafetyConfig"
            }
          ],
          "default": {
            "allow_insecure_http": false,
            "allow_private_backends": false,
            "allow_stdio": false
          },
          "description": "SSRF / DNS-rebinding posture (http) + local-exec posture (stdio)."
        },
        "url": {
          "default": "",
          "description": "Base MCP endpoint URL (the upstream's `/mcp`). Required for the `streamable_http` transport; unused (empty) for `stdio`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "UpstreamProtocolVersion": {
      "description": "MCP wire revision the federation client speaks to an upstream.",
      "oneOf": [
        {
          "description": "Detect the upstream's wire at connect time (the default): attempt the modern `server/discover`, and fall back to the legacy `initialize` handshake when the peer rejects it (the SEP-2575 backward-compatibility probe). The detected wire is cached per federation for the engine's lifetime. Pin one of the dated revisions to skip probing. The `stdio` transport never probes (it is legacy-only).",
          "enum": [
            "auto"
          ],
          "type": "string"
        },
        {
          "description": "Session-bound `2025-11-25` wire (`initialize` handshake, `Mcp-Session-Id`, no SEP-2243 headers) — byte-identical to the legacy federation client.",
          "enum": [
            "2025-11-25"
          ],
          "type": "string"
        },
        {
          "description": "Stateless `2026-07-28` wire (no handshake / session, per-request `_meta` identity, SEP-2243 routing headers).",
          "enum": [
            "2026-07-28"
          ],
          "type": "string"
        }
      ]
    },
    "UpstreamSafetyConfig": {
      "additionalProperties": false,
      "description": "SSRF / DNS-rebinding posture for the upstream URL. Mirrors the HTTP binding's guard (`runtime/safe_dns.rs`).",
      "properties": {
        "allow_insecure_http": {
          "default": false,
          "description": "Permit `http://` (non-TLS) upstreams.",
          "type": "boolean"
        },
        "allow_private_backends": {
          "default": false,
          "description": "Permit private / loopback upstream addresses.",
          "type": "boolean"
        },
        "allow_stdio": {
          "default": false,
          "description": "Permit the `stdio` transport, which spawns a local child process (arbitrary local execution — default-deny).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "UpstreamTransport": {
      "description": "Upstream wire transport.",
      "oneOf": [
        {
          "description": "MCP Streamable HTTP (POST + SSE).",
          "enum": [
            "streamable_http"
          ],
          "type": "string"
        },
        {
          "description": "Local stdio child process.",
          "enum": [
            "stdio"
          ],
          "type": "string"
        }
      ]
    },
    "UsageReportingConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "default": false,
          "description": "Send the anonymous adoption ping. Defaults to `false`. `DO_NOT_TRACK=1` / `MCPG_TELEMETRY=off` disable regardless.",
          "type": "boolean"
        },
        "endpoint": {
          "default": "https://telemetry.mcpg.dev/v1/usage",
          "description": "Ingest endpoint (HTTPS). Self-hostable — point it at your own collector.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "VariableCompletionSource": {
      "anyOf": [
        {
          "description": "Shorthand for `{ kind: static, values: [...] }`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "allOf": [
            {
              "$ref": "#/definitions/TaggedVariableCompletionSource"
            }
          ],
          "description": "Discriminated form. New shape; `kind: dynamic` introduces dispatch to a backend's `complete_template_variable` method."
        }
      ],
      "description": "Per-template-variable completion source.\n\nThe bare-list shorthand stays valid (post-launch shipping shape): `variable_completions: { region: [\"us-east-1\", \"us-west-2\"] }` is read by the `BareList` arm and treated as if the operator had written `{ kind: static, values: [...] }`. The tagged form is the new shape — `kind: dynamic` introduces backend dispatch."
    },
    "VerificationConfig": {
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "allow_hmac": {
              "default": false,
              "description": "Security: HMAC algorithms share a secret across the trust boundary and are unsuitable for most OIDC deployments. Operators must explicitly opt in.",
              "type": "boolean"
            },
            "allowed_algs": {
              "default": [
                "RS256"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "kind": {
              "enum": [
                "oidc_jwks"
              ],
              "type": "string"
            },
            "max_staleness_secs": {
              "default": 3600,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "refresh_interval_secs": {
              "default": 300,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "timeout_ms": {
              "default": 2000,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "client_id": {
              "type": "string"
            },
            "client_secret_ref": {
              "type": "string"
            },
            "introspection_url": {
              "type": "string"
            },
            "kind": {
              "enum": [
                "oauth_introspection"
              ],
              "type": "string"
            },
            "timeout_ms": {
              "default": 2000,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "client_id",
            "client_secret_ref",
            "introspection_url",
            "kind"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "JWTs are verified against the JWKS; opaque tokens are introspected.\n\nThe two paths are selected by the token's *shape*, not by whether the first one accepted it. Introspection has no header to apply `allowed_algs` to, so a token that is a JWS is adjudicated by the JWT path and that verdict stands — a rejection there is never retried against the authorization server.",
          "properties": {
            "allow_hmac": {
              "default": false,
              "description": "See `OidcJwks.allow_hmac`.",
              "type": "boolean"
            },
            "allowed_algs": {
              "default": [
                "RS256"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "client_id": {
              "type": "string"
            },
            "client_secret_ref": {
              "type": "string"
            },
            "introspection_timeout_ms": {
              "default": 2000,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "introspection_url": {
              "type": "string"
            },
            "kind": {
              "enum": [
                "hybrid"
              ],
              "type": "string"
            },
            "max_staleness_secs": {
              "default": 3600,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "refresh_interval_secs": {
              "default": 300,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "timeout_ms": {
              "default": 2000,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "client_id",
            "client_secret_ref",
            "introspection_url",
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "WatchStrategyConfig": {
      "oneOf": [
        {
          "description": "Poll the resource periodically and compare SHA-256 hash.",
          "properties": {
            "interval_ms": {
              "default": 60000,
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "type": {
              "enum": [
                "poll"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Subscribe to a NATS subject — any message means the resource changed.",
          "properties": {
            "subject": {
              "type": "string"
            },
            "type": {
              "enum": [
                "nats_topic"
              ],
              "type": "string"
            }
          },
          "required": [
            "subject",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Subscribe to a Kafka topic — any message means the resource changed.",
          "properties": {
            "group_id": {
              "default": "mcpg-resource-watcher",
              "type": "string"
            },
            "topic": {
              "type": "string"
            },
            "type": {
              "enum": [
                "kafka_topic"
              ],
              "type": "string"
            }
          },
          "required": [
            "topic",
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Receive webhook POSTs from 3rd-party systems. MCPG exposes `/webhooks/resource-updated/{token}` and triggers `notifications/resources/updated` when a POST is received.",
          "properties": {
            "previous_tokens": {
              "description": "Tokens still accepted while senders migrate to `token`.\n\nA sender is re-registered out of band and cannot be switched at the same instant the gateway is, so rotating a single token drops every event in between. Carrying the old value here keeps both live for one deploy: promote the new token, re-register senders, then empty this list.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "token": {
              "default": "",
              "description": "Shared secret token that must appear in the URL path. If empty, a random UUID-v4 is generated on startup.",
              "type": "string"
            },
            "type": {
              "enum": [
                "webhook"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "SQL polling watch — `dev.mcpg.watch.sql_polling` plugin runs a scalar tracking query on a cadence and emits an event when the returned scalar advances. Spec mirrors the `[bindings.sql]` shape (`driver`, `url`, optional `pool` / `session_vars`, required `query` block, `interval_ms`); see the SQL binding plugin docs for the full field list. Pass-through here keeps the spec the single source of truth in the plugin crate.",
          "properties": {
            "type": {
              "enum": [
                "sql_polling"
              ],
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        },
        {
          "description": "Postgres LISTEN/NOTIFY watch — `dev.mcpg.watch.postgres_listen_notify` plugin holds one dedicated connection per watch and re-emits NOTIFY payloads. Far lower overhead than polling for change-feed-style sources.",
          "properties": {
            "channel": {
              "description": "Channel name to LISTEN on (`NOTIFY <channel>, payload`). ASCII alphanumeric + underscore.",
              "type": "string"
            },
            "type": {
              "enum": [
                "postgres_listen_notify"
              ],
              "type": "string"
            },
            "url": {
              "description": "Postgres connection URL. Credential interpolation via the gateway's `${env.VAR}` happens before the spec reaches the plugin.",
              "type": "string"
            }
          },
          "required": [
            "channel",
            "type",
            "url"
          ],
          "type": "object"
        },
        {
          "description": "Generic escape hatch — delegate to ANY loaded `watch_strategy` plugin by its `kind()` discriminator. Use this for custom watch plugins that have no dedicated typed variant above (e.g. the Twilio plugin's `twilio_inbound` strategy). The remaining fields flatten into the spec passed verbatim to the plugin's `watch()`, so the plugin owns and validates its own spec schema.\n\n`{ type: plugin, kind: twilio_inbound, kinds: [sms, voice] }`",
          "properties": {
            "kind": {
              "description": "The target plugin's `WatchStrategyPlugin::kind()` string.",
              "type": "string"
            },
            "type": {
              "enum": [
                "plugin"
              ],
              "type": "string"
            }
          },
          "required": [
            "kind",
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "WidgetKind": {
      "description": "The closed widget vocabulary.",
      "oneOf": [
        {
          "enum": [
            "text",
            "textarea",
            "select",
            "multiselect",
            "date",
            "datetime",
            "number",
            "currency",
            "slider",
            "toggle",
            "radio",
            "file",
            "color",
            "hidden",
            "markdown",
            "json"
          ],
          "type": "string"
        },
        {
          "description": "Repeatable list of scalar inputs → a JSON array value.",
          "enum": [
            "array"
          ],
          "type": "string"
        }
      ]
    }
  },
  "description": "Top-level gateway configuration. Loaded from YAML file and/or `MCPG_` environment variables via figment. Bindings live under `mcp.capabilities.{tools,prompts,resources,resource_templates}[]`. Each binding carries an explicit nested `backend:` block that picks the implementation, discriminated by `kind:` (`kind: http`, `kind: sql`, `kind: openai_chat`, …). Env-var expansion (`${env.X}`) happens at startup time via CEL.\n\n`deny_unknown_fields` is set so a typo at the root (or a stale renamed block left in an operator's YAML) fails parsing instead of silently parsing to defaults. The same strictness applies to every typed sub-config; this flag closes the gap at the root.",
  "properties": {
    "cloud": {
      "allOf": [
        {
          "$ref": "#/definitions/CloudConfig"
        }
      ],
      "default": {
        "allow_anonymous": false,
        "isolation": "shared",
        "provenance": {},
        "tier": "unspecified"
      },
      "description": "`cloud:` — managed-fleet (mcpg.cloud) identity + placement. Absent for self-host; inert when present-but-empty, so the gateway binary is byte-identical whether or not it runs in the cloud. Server-managed fields (`instance_id`, `subdomain`, `provenance.*`) are stamped by the provisioner/operator and ignored if hand-written."
    },
    "cluster": {
      "allOf": [
        {
          "$ref": "#/definitions/ClusterConfig"
        }
      ],
      "default": {
        "allow_degraded_boot": false,
        "allow_insecure_transport": false,
        "kind": "single_node",
        "readiness_gate": "off",
        "state_encryption_allow_plaintext_reads": false
      },
      "description": "Cluster coordinator. Singleton: the operator picks one coordinator and configures it inline. Default is the built-in single-node coordinator — safe for single-instance deployments. Other kinds map to `mcpg-plugin-cluster-*` cdylib plugins; the cdylib must still be declared under `plugins[]` for the gateway to load it. The inline `cluster` block is the single source of truth for the coordinator's runtime config — it overrides any `config:` block on the matching `plugins[]` row."
    },
    "credentials": {
      "allOf": [
        {
          "$ref": "#/definitions/CredentialsConfig"
        }
      ],
      "default": {
        "cluster": {
          "allow_plaintext": false,
          "enabled": false
        },
        "max_cache_ttl_ms": 3600000,
        "max_entries": 10000
      },
      "description": "`credentials:` — the gateway-side L1 credential cache for `cred://` URI substitution (sizing, per-entry TTL cap, the `key_attributes` cache-key dimension) plus the optional cluster pub/sub wrapper. Defaults are safe for single-node; a multi-instance deploy issuing per-caller dynamic credentials configures `credentials.cluster` to keep peer caches consistent."
    },
    "debug": {
      "allOf": [
        {
          "$ref": "#/definitions/DebugConfig"
        }
      ],
      "default": {
        "tools": {
          "bindings": {
            "command_probe_profile": "default_command_probe",
            "network_json_call_profile": "default_network_probe",
            "network_probe_profile": "default_network_probe"
          },
          "command_profiles": {
            "default_command_probe": {
              "args": [
                "mcpg-debug-command\n"
              ],
              "command": "printf",
              "max_output_bytes": 4096,
              "timeout_ms": 2000
            }
          },
          "exposure": {
            "command_probe": true,
            "network_json_call": false,
            "network_probe": true,
            "operational_overview_prompt": true,
            "runtime_overview_resource": true
          },
          "network_profiles": {
            "default_network_probe": {
              "expected_status_codes": [
                200
              ],
              "headers": {},
              "max_response_bytes": 4096,
              "require_json_response": false,
              "timeout_ms": 2000,
              "url": "http://127.0.0.1:8787/health"
            }
          }
        }
      },
      "description": "Operator-defined diagnostic tools (`mcpg.command.*` / `mcpg.network.*`) plus their probe profiles. The block is fully ignored unless `feature_flags.debug_tools_enabled` is `true`; production deploys keep that flag off and treat this block as scaffolding for CI / dev rollouts."
    },
    "feature_flags": {
      "allOf": [
        {
          "$ref": "#/definitions/FeatureFlagsConfig"
        }
      ],
      "default": {
        "allow_header_passthrough": false,
        "debug_tools_enabled": false,
        "sep2260_panic_on_orphan": false
      },
      "description": "Operator-controlled strictness / compatibility flags. Every flag defaults off; flipping one is an explicit acknowledgement that the operator is taking on the risk the default protects against. Collapsing them into this block lets them show up in the curated reference + JSON Schema and audit-emit when active."
    },
    "gateway": {
      "allOf": [
        {
          "$ref": "#/definitions/GatewayConfig"
        }
      ],
      "default": {
        "admin": {
          "auth": {
            "type": "disabled"
          },
          "base_path": "/admin/v1",
          "bind_address": "127.0.0.1:9090",
          "disclosure": "summary",
          "enabled": false
        },
        "config_watch": {
          "enabled": false,
          "poll_interval_ms": 0
        },
        "control_plane": null,
        "inspector": {
          "enabled": false
        },
        "plugin_registry": {
          "auth": {
            "docker_config_path": null,
            "password": null,
            "username": null
          },
          "cache_dir": null,
          "default_registry": "ghcr.io/mcpg-dev/source-code/plugins",
          "default_signature_policy": "enforce",
          "insecure_registries": [],
          "mirrors": [],
          "require_integrity_anchor": false,
          "revocation_list_path": null,
          "tls": {
            "ca_cert": null,
            "insecure": false
          }
        },
        "server": {
          "access_log": true,
          "allow_private_backends": false,
          "allowed_origins": [],
          "anonymous_rate_limit_burst": 100,
          "anonymous_rate_limit_per_min": 600,
          "bind_address": "127.0.0.1:8787",
          "completion_rate_limit_per_sec": null,
          "enforce_modern_request_meta": false,
          "extra_resource_uri_schemes": [],
          "health_check": {
            "degraded_latency_threshold_ms": 1000,
            "enabled": true,
            "interval_ms": 30,
            "timeout_ms": 2000,
            "unhealthy_threshold": 3
          },
          "health_path": "/health",
          "max_request_body_mb": 4,
          "max_sessions_per_tenant": 0,
          "mcp_path": "/mcp",
          "relax_request_id_uniqueness": false,
          "replay_window_limit": 16,
          "request_timeout_ms": 30000,
          "revalidate_mutated_tool_arguments": false,
          "scrub_process_env_after_boot": false,
          "server_ping_interval_ms": null,
          "session_idle_timeout_ms": 900000,
          "shutdown_timeout_ms": 30000,
          "tls": null,
          "transport": "http",
          "trust_proxy_ip": false,
          "trust_subject_header": false,
          "unary_json_fast_path": false
        }
      },
      "description": "`gateway:` umbrella — the binary's network face: listener (`server`), admin surface (`admin`), Control Plane attachment (`control_plane`)."
    },
    "governance": {
      "allOf": [
        {
          "$ref": "#/definitions/GovernanceConfig"
        }
      ],
      "default": {
        "access": {
          "authorization_server": null,
          "jwks": null,
          "oidc_oauth": null,
          "resource_metadata": null
        },
        "approvals": {
          "callback_base_url": null,
          "callback_grace_ms": 0,
          "signing_key_env": null
        },
        "audit": {
          "emit_tool_call_allowed": true,
          "emit_tool_call_completed": true,
          "enabled": true,
          "on_failure": "fail_closed",
          "required": true,
          "sinks": [
            {
              "config": {},
              "kind": "dev.mcpg.builtin.audit.local-file"
            }
          ]
        },
        "child_invoke": {
          "enforce_gates": false
        },
        "policy": {
          "cache": {
            "enabled": false,
            "max_entries": 10000,
            "ttl_ms": 60000
          },
          "tool_access": {
            "cel_allow_if": null,
            "default_minimum_trust": "header_asserted",
            "rules": []
          }
        },
        "quotas": {
          "on_error": "",
          "store": {
            "kind": ""
          }
        }
      },
      "description": "`governance:` umbrella — tool-call lifecycle: identity (`access`) → authorization (`policy`) → human gate (`approvals`) → evidence (`audit`). Co-located under one umbrella so the governance story reads as a coherent block."
    },
    "license": {
      "allOf": [
        {
          "$ref": "#/definitions/LicenseConfig"
        }
      ],
      "default": {
        "non_production_use": false
      },
      "description": "`license:` — offline license token (or the non-production declaration) for standalone deployments; the plugin load gate refuses entitlement-gated plugins the resolved envelope does not admit. Ignored when `gateway.control_plane` is attached."
    },
    "mcp": {
      "allOf": [
        {
          "$ref": "#/definitions/McpConfig"
        }
      ],
      "default": {
        "capabilities": {
          "elicitation": {
            "timeout_ms": 60000
          },
          "prompts": [],
          "resource_templates": [],
          "resources": [],
          "roots": {
            "timeout_ms": 30000
          },
          "sampling": {
            "timeout_ms": 60000
          },
          "tasks": {
            "default_ttl_ms": 1800000,
            "max_tasks_per_session": 256,
            "reaper_interval_ms": 60000,
            "result_wait_ms": 30000
          },
          "tools": []
        },
        "configurations": {
          "apps": {
            "allowed_permissions": [
              "camera",
              "microphone",
              "geolocation",
              "clipboard_write"
            ],
            "csp_policy": {
              "base_uri_domains": [
                "self"
              ],
              "connect_domains": [
                "*"
              ],
              "frame_domains": [
                "self"
              ],
              "redirect_domains": [
                "*"
              ],
              "resource_domains": [
                "*"
              ]
            },
            "enabled": false,
            "strict": false
          },
          "cancellation": {
            "partition_by_principal": false
          },
          "delivery": {},
          "idempotency": {
            "conflict_policy": "reject",
            "default_ttl_ms": 86400000,
            "enabled": false,
            "max_ttl_ms": 604800000,
            "replay_revalidation": false,
            "scope": "per_identity",
            "supported_methods": [
              "tools/call",
              "tasks/create"
            ]
          },
          "pipelines": {},
          "request_state": {
            "strict_encryption": false
          },
          "sessions": {
            "bind_session_owner": false,
            "optional": false
          },
          "subscriptions": {
            "max_per_session": 100
          }
        },
        "federations": [],
        "registries": [],
        "registry": {
          "enabled": false,
          "name": ""
        }
      },
      "description": "`mcp:` namespace — the MCP protocol surface. Two children: `capabilities:` (tools / prompts / resources / resource_templates / tasks / elicitation / sampling / roots — what the server advertises in `initialize`) and `configurations:` (sessions / pipelines / subscriptions / delivery / cancellation — runtime-emergent state). Capability persistence (`store:` / `bus:`) defaults to `kind: cluster` — the cluster coordinator's primitive — and can be overridden per capability with `kind: memory` / `file`."
    },
    "observability": {
      "allOf": [
        {
          "$ref": "#/definitions/ObservabilityConfig"
        }
      ],
      "default": {
        "enabled": true,
        "logs": {
          "enabled": true,
          "level": "info",
          "sinks": [
            {
              "config": {
                "format": "json"
              },
              "kind": "stderr"
            }
          ]
        },
        "metrics": {
          "enabled": true,
          "sinks": [
            {
              "config": {},
              "kind": "dev.mcpg.observability.prometheus"
            }
          ]
        },
        "plugin_health_probe": {
          "enabled": true,
          "failure_threshold": 3,
          "interval_ms": 30000,
          "probe_timeout_ms": 5000
        },
        "traces": {
          "enabled": false,
          "propagate_context": true,
          "service_name": "mcpg",
          "sinks": []
        }
      },
      "description": "All observability concerns — log/metric/trace emission, the binding-backend health prober, and the sink fan-out routing for telemetry / log events. Sub-fields all default to safe single-node values so the block is fully optional."
    },
    "plugins": {
      "default": [],
      "description": "Loaded plugin entries — flat array, no wrapper. Each entry is self-contained (id / class / source / signature / config / limits / enforce / granted_capabilities / observability / http_route / disabled). Identity / policy / credential / catalog / cluster plugins all dispatch via the `class:` field. An empty array is the kill switch — no plugins are loaded.\n\nCompanion concerns live in dedicated homes: - `plugins:` is this flat array. - Per-entry wiring lives on each entry itself (`granted_capabilities` / `signature` / `limits` / `observability` / `http_route`). - `gateway.plugin_registry:` holds OCI defaults. - `gateway.config_overlay:` holds the bootstrap URI list. - `observability.plugin_health_probe:` holds prober tuning.",
      "items": {
        "$ref": "#/definitions/PluginEntryConfig"
      },
      "type": "array"
    },
    "schema_registry": {
      "additionalProperties": {
        "$ref": "#/definitions/SchemaEntry"
      },
      "default": {},
      "description": "Named JSON Schemas operator-declared once, referenced by `{\"$schema_ref\": \"<name>\"}` in any binding's `input_schema:` / `output_schema:`. Named `schema_registry:` (rather than `schemas:`) to disambiguate from the per-binding schema fields (`input_schema:`, `output_schema:`). Each entry (`SchemaEntry`) is inline / file / url.",
      "type": "object"
    },
    "storage": {
      "allOf": [
        {
          "$ref": "#/definitions/StorageConfig"
        }
      ],
      "default": {
        "providers": [],
        "response_cache": {
          "kind": "in_process",
          "max_bytes": 67108864
        }
      },
      "description": "`storage:` block. Holds operator-declared content-store providers AND the gateway-managed LLM response cache. Each provider entry produces a named `ContentStore` in the gateway's registry; bindings reference providers by id via their own `content_storage:` field. When `providers` is empty AND no binding declares a `content_storage:` route, the gateway auto-creates a single in-process provider with id `default` and the standard 256 MiB cap.\n\nThe LLM response cache (`storage.response_cache:`) lives under `storage:` (rather than `plugins:`) so all \"where bytes go to live\" config shares one home."
    },
    "usage_reporting": {
      "allOf": [
        {
          "$ref": "#/definitions/UsageReportingConfig"
        }
      ],
      "default": {
        "enabled": false,
        "endpoint": "https://telemetry.mcpg.dev/v1/usage"
      },
      "description": "`usage_reporting:` — anonymous adoption ping. A minimal, vendor-facing, opt-out signal (product version + first-party plugin set) so we can see how the community grows. Wholly distinct from `observability:` (the operator's own OTel/metrics/log sinks). Fail-open, schema-pinned, and self-suppressing when air-gapped / licensed / CP-attached / CI; also disabled by `DO_NOT_TRACK` / `MCPG_TELEMETRY=off`."
    }
  },
  "title": "AppConfig",
  "type": "object"
}
